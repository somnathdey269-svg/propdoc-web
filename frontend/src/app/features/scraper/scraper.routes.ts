import { Routes } from '@angular/router';
import { PortalManagerComponent } from './portal-manager.component';

export const SCRAPER_ROUTES: Routes = [
  { path: '', component: PortalManagerComponent }
];
