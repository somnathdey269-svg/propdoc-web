export * from 'primeng/types/treetable';
import * as i0 from '@angular/core';
import { Injectable, InjectionToken, input, booleanAttribute, inject, computed, ViewEncapsulation, Component, viewChild, effect, model, numberAttribute, output, contentChild, signal, untracked, HostListener, Directive, ChangeDetectionStrategy, NgModule } from '@angular/core';
import { BaseStyle } from 'primeng/base';
import { Subject } from 'rxjs';
import { NgTemplateOutlet, isPlatformBrowser } from '@angular/common';
import { addClass, findSingle, calculateScrollbarWidth, calculateScrollbarHeight, resolveFieldData, isEmpty, getOffset, addStyle, getIndex, getHiddenElementOuterWidth, getHiddenElementOuterHeight, reorderArray, isClickable, equals, find, removeClass, clearSelection } from '@primeuix/utils';
import { FilterService, SharedModule } from 'primeng/api';
import { BaseComponent, PARENT_INSTANCE } from 'primeng/basecomponent';
import * as i1 from 'primeng/bind';
import { Bind } from 'primeng/bind';
import { DomHandler } from 'primeng/dom';
import { ArrowDown } from '@primeicons/angular/arrow-down';
import { ArrowUp } from '@primeicons/angular/arrow-up';
import { Spinner } from '@primeicons/angular/spinner';
import * as i2 from 'primeng/paginator';
import { PaginatorModule } from 'primeng/paginator';
import { takeUntilDestroyed } from '@angular/core/rxjs-interop';
import { Scroller } from 'primeng/scroller';
import * as i1$1 from 'primeng/badge';
import { BadgeModule } from 'primeng/badge';
import { SortAlt } from '@primeicons/angular/sort-alt';
import { SortAmountDown } from '@primeicons/angular/sort-amount-down';
import { SortAmountUpAlt } from '@primeicons/angular/sort-amount-up-alt';
import * as i1$2 from '@angular/forms';
import { FormsModule } from '@angular/forms';
import { Checkbox } from 'primeng/checkbox';
import { ChevronDown } from '@primeicons/angular/chevron-down';
import { ChevronRight } from '@primeicons/angular/chevron-right';
import { Ripple } from 'primeng/ripple';

const style = /*css*/ `
/* For PrimeNG */
.p-treetable {
    position: relative;
}

.p-treetable table {
    border-collapse: collapse;
    width: 100%;
    table-layout: fixed;
}

.p-treetable .p-sortable-column {
    cursor: pointer;
    user-select: none;
}

.p-treetable .p-sortable-column .p-column-title,
.p-treetable .p-sortable-column .p-sortable-column-icon,
.p-treetable .p-sortable-column .p-sortable-column-badge {
    vertical-align: middle;
}

.p-treetable-sort-icon {
    color: dt('treetable.sort.icon.color');
    font-size: dt('treetable.sort.icon.size');
    width: dt('treetable.sort.icon.size');
    height: dt('treetable.sort.icon.size');
    transition: color dt('treetable.transition.duration');
}

.p-treetable .p-sortable-column .p-sortable-column-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
}

.p-treetable-auto-layout>.p-treetable-wrapper {
    overflow-x: auto;
}

.p-treetable-auto-layout>.p-treetable-wrapper>table {
    table-layout: auto;
}

.p-treetable-hoverable-rows .p-treetable-tbody>tr {
    cursor: pointer;
}

.p-treetable-toggler {
    cursor: pointer;
    user-select: none;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    vertical-align: middle;
    overflow: hidden;
    position: relative;
}


/* Scrollable */
.p-treetable-scrollable-wrapper {
    position: relative;
}

.p-treetable-scrollable-header,
.p-treetable-scrollable-footer {
    overflow: hidden;
    flex-shrink: 0;
}

.p-treetable-scrollable-body {
    overflow: auto;
    position: relative;
}

.p-treetable-virtual-table {
    position: absolute;
}

/* Frozen Columns */
.p-treetable-frozen-view .p-treetable-scrollable-body {
    overflow: hidden;
}

.p-treetable-frozen-view>.p-treetable-scrollable-body>table>.p-treetable-tbody>tr>td:last-child {
    border-right: 0 none;
}

.p-treetable-unfrozen-view {
    position: absolute;
    top: 0;
}

/* Flex Scrollable */
.p-treetable-flex-scrollable {
    display: flex;
    flex-direction: column;
    flex: 1;
    height: 100%;
}

.p-treetable-flex-scrollable .p-treetable-scrollable-wrapper,
.p-treetable-flex-scrollable .p-treetable-scrollable-view {
    display: flex;
    flex-direction: column;
    flex: 1;
    height: 100%;
}

.p-treetable-flex-scrollable .p-treetable-virtual-scrollable-body {
    flex: 1;
}

/* Resizable */
.p-treetable-resizable>.p-treetable-wrapper {
    overflow-x: auto;
}

.p-treetable-resizable .p-treetable-thead>tr>th,
.p-treetable-resizable .p-treetable-tfoot>tr>td,
.p-treetable-resizable .p-treetable-tbody>tr>td {
    overflow: hidden;
}

.p-treetable-resizable .p-resizable-column {
    background-clip: padding-box;
    position: relative;
}

.p-treetable-resizable-fit .p-resizable-column:last-child .p-column-resizer {
    display: none;
}

.p-treetable .p-column-resizer {
    display: block;
    position: absolute;
    top: 0;
    right: 0;
    margin: 0;
    width: dt('treetable.column.resizer.width');
    height: 100%;
    padding: 0px;
    cursor: col-resize;
    border: 1px solid transparent;
}

.p-treetable .p-column-resizer-helper {
    width: dt('treetable.resize.indicator.width');
    position: absolute;
    z-index: 10;
    display: none;
    background: dt('treetable.resize.indicator.color');
}

.p-treetable .p-row-editor-init,
.p-treetable .p-row-editor-save,
.p-treetable .p-row-editor-cancel {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    position: relative;
}


/* Reorder */
.p-treetable-reorder-indicator-up,
.p-treetable-reorder-indicator-down {
    position: absolute;
    display: none;
}

[ttReorderableColumn] {
    cursor: move;
}

/* Loader */
.p-treetable-mask {
    position: absolute !important;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 2;
}

.p-treetable-loading-icon {
    font-size: dt('treetable.loading.icon.size');
    width: dt('treetable.loading.icon.size');
    height: dt('treetable.loading.icon.size');
}

/* Virtual Scroll */
.p-treetable .p-scroller-loading {
    transform: none !important;
    min-height: 0;
    position: sticky;
    top: 0;
    left: 0;
}

.p-treetable .p-paginator-top {
    border-color: dt('treetable.paginator.top.border.color');
    border-style: solid;
    border-width: dt('treetable.paginator.top.border.width');
}

.p-treetable .p-paginator-bottom {
    border-color: dt('treetable.paginator.bottom.border.color');
    border-style: solid;
    border-width: dt('treetable.paginator.bottom.border.width');
}

.p-treetable .p-treetable-header {
    background: dt('treetable.header.background');
    color: dt('treetable.header.color');
    border-color: dt('treetable.header.border.color');
    border-style: solid;
    border-width: dt('treetable.header.border.width');
    padding: dt('treetable.header.padding');
    font-weight: dt('treetable.column.title.font.weight');
}

.p-treetable .p-treetable-footer {
    background: dt('treetable.footer.background');
    color: dt('treetable.footer.color');
    border-color: dt('treetable.footer.border.color');
    border-style: solid;
    border-width: dt('treetable.footer.border.width');
    padding: dt('treetable.footer.padding');
    font-weight: dt('treetable.column.footer.font.weight');
}

.p-treetable .p-treetable-thead>tr>th {
    padding: dt('treetable.header.cell.padding');
    background: dt('treetable.header.cell.background');
    border-color: dt('treetable.header.cell.border.color');
    border-style: solid;
    border-width: 0 0 1px 0;
    color: dt('treetable.header.cell.color');
    font-weight: dt('treetable.column.title.font.weight');
    text-align: start;
    transition: background dt('treetable.transition.duration'), color dt('treetable.transition.duration'), border-color dt('treetable.transition.duration'),
            outline-color dt('treetable.transition.duration'), box-shadow dt('treetable.transition.duration');
}

.p-treetable .p-treetable-tfoot>tr>td {
    text-align: start;
    padding: dt('treetable.footer.cell.padding');
    border-color: dt('treetable.footer.cell.border.color');
    border-style: solid;
    border-width: 0 0 1px 0;
    color: dt('treetable.footer.cell.color');
    background: dt('treetable.footer.cell.background');
    font-weight: dt('treetable.column.footer.font.weight');
}

.p-treetable .p-sortable-column {
    cursor: pointer;
    user-select: none;
    outline-color: transparent;
    vertical-align: middle;
}

.p-treetable .p-sortable-column .p-sortable-column-icon {
    color: dt('treetable.sort.icon.color');
    transition: color dt('treetable.transition.duration');
}


.p-treetable .p-sortable-column:not(.p-treetable-column-sorted):hover {
    background: dt('treetable.header.cell.hover.background');
    color: dt('treetable.header.cell.hover.color');
}

.p-treetable .p-sortable-column:not(.p-treetable-column-sorted):hover .p-treetable-sort-icon {
    color: dt('treetable.sort.icon.hover.color');
}

.p-treetable .p-sortable-column.p-treetable-column-sorted {
    background: dt('treetable.header.cell.selected.background');
    color: dt('treetable.header.cell.selected.color');
}

.p-treetable .p-sortable-column.p-treetable-column-sorted .p-treetable-sort-icon {
    color: dt('treetable.header.cell.selected.color');
}

.p-treetable .p-sortable-column:focus-visible {
    box-shadow: dt('treetable.header.cell.focus.ring.shadow');
    outline: dt('treetable.header.cell.focus.ring.width') dt('treetable.header.cell.focus.ring.style') dt('treetable.header.cell.focus.ring.color');
    outline-offset: dt('treetable.header.cell.focus.ring.offset');
}

.p-treetable-hoverable .p-treetable-selectable-row {
    cursor: pointer;
}

.p-treetable .p-treetable-tbody > tr {
    outline-color: transparent;
    background: dt('treetable.row.background');
    color: dt('treetable.row.color');
}

.p-treetable .p-treetable-tbody>tr>td {
    text-align: start;
    border-color: dt('treetable.body.cell.border.color');
    border-style: solid;
    border-width: 0 0 1px 0;
    padding: dt('treetable.body.cell.padding');
    font-weight: dt('treetable.body.cell.font.weight');
    font-size: dt('treetable.body.cell.font.size');
}

.p-treetable .p-treetable-tbody>tr>td .p-treetable-toggler {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    position: relative;
    width: dt('treetable.node.toggle.button.size');
    height: dt('treetable.node.toggle.button.size');
    color: dt('treetable.node.toggle.button.color');
    border: 0 none;
    background: transparent;
    cursor: pointer;
    border-radius: dt('treetable.node.toggle.button.border.radius');
    transition: background dt('treetable.transition.duration'), color dt('treetable.transition.duration'), border-color dt('treetable.transition.duration'),
            outline-color dt('treetable.transition.duration'), box-shadow dt('treetable.transition.duration');
    outline-color: transparent;
    user-select: none;
}

.p-treetable .p-treetable-tbody>tr>td .p-treetable-toggler:enabled:hover {
    color: dt('treetable.node.toggle.button.hover.color');
    background: dt('treetable.node.toggle.button.hover.background');
}

.p-treetable .p-treetable-tbody>tr>tr.treetable-row-selected .p-treetable-toggler:hover {
    background: dt('treetable.node.toggle.button.selected.hover.background');
    color: dt('treetable.node.toggle.button.selected.hover.color');
}

.p-treetable .p-treetable-tbody>tr>td .p-treetable-toggler:focus-visible {
    box-shadow: dt('treetable.node.toggle.button.focus.ring.shadow');
    outline: dt('treetable.node.toggle.button.focus.ring.width') dt('treetable.node.toggle.button.focus.ring.style') dt('treetable.node.toggle.button.focus.ring.color');
    outline-offset: dt('treetable.node.toggle.button.focus.ring.offset');
}


.p-treetable .p-treetable-tbody>tr.p-treetable-row-selected {
    background: dt('treetable.row.selected.background');
    color: dt('treetable.row.selected.color');
}

.p-treetable-tbody > tr:focus-visible,
.p-treetable-tbody > tr.p-treetable-contextmenu-row-selected {
    box-shadow: dt('treetable.row.focus.ring.shadow');
    outline: dt('treetable.row.focus.ring.width') dt('treetable.row.focus.ring.style') dt('treetable.row.focus.ring.color');
    outline-offset: dt('treetable.row.focus.ring.offset');
}

.p-treetable .p-treetable-tbody>tr.p-treetable-row-selected .p-treetable-toggler {
    color: inherit;
}

.p-treetable .p-treetable-tbody>tr.p-treetable-row-selected .p-treetable-toggler:hover {
    background: dt('treetable.node.toggle.button.selected.hover.background');
    color: dt('treetable.node.toggle.button.selected.hover.color');
}

.p-treetable.p-treetable-hoverable-rows .p-treetable-tbody>tr:not(.p-treetable-row-selected):hover {
    background: dt('treetable.row.hover.background');
    color: dt('treetable.row.hover.color');
}

.p-treetable-gridlines .p-treetable-header {
    border-width: 1px 1px 0 1px;
}

.p-treetable-gridlines .p-treetable-footer {
    border-width: 0 1px 1px 1px;
}

.p-treetable-gridlines .p-treetable-paginator-top {
    border-width: 1px 1px 0 1px;
}

.p-treetable-gridlines .p-treetable-paginator-bottom {
    border-width: 0 1px 1px 1px;
}

.p-treetable-gridlines .p-treetable-thead > tr > th {
    border-width: 1px 0 1px 1px;
}

.p-treetable-gridlines .p-treetable-thead > tr > th:last-child {
    border-width: 1px;
}

.p-treetable-gridlines .p-treetable-tbody > tr > td {
    border-width: 1px 0 0 1px;
}

.p-treetable-gridlines .p-treetable-tbody > tr > td:last-child {
    border-width: 1px 1px 0 1px;
}

.p-treetable-gridlines .p-treetable-tbody > tr:last-child > td {
    border-width: 1px 0 1px 1px;
}

.p-treetable-gridlines .p-treetable-tbody > tr:last-child > td:last-child {
    border-width: 1px;
}

.p-treetable-gridlines .p-treetable-tfoot > tr > td {
    border-width: 1px 0 1px 1px;
}

.p-treetable-gridlines .p-treetable-tfoot > tr > td:last-child {
    border-width: 1px 1px 1px 1px;
}

.p-treetable.p-treetable-gridlines .p-treetable-thead + .p-treetable-tfoot > tr > td {
    border-width: 0 0 1px 1px;
}

.p-treetable.p-treetable-gridlines .p-treetable-thead + .p-treetable-tfoot > tr > td:last-child {
    border-width: 0 1px 1px 1px;
}

.p-treetable.p-treetable-gridlines:has(.p-treetable-thead):has(.p-treetable-tbody) .p-treetable-tbody > tr > td {
    border-width: 0 0 1px 1px;
}

.p-treetable.p-treetable-gridlines:has(.p-treetable-thead):has(.p-treetable-tbody) .p-treetable-tbody > tr > td:last-child {
    border-width: 0 1px 1px 1px;
}

.p-treetable.p-treetable-gridlines:has(.p-treetable-tbody):has(.p-treetable-tfoot > tr) .p-treetable-tbody > tr:last-child > td {
    border-width: 0 0 0 1px;
}

.p-treetable.p-treetable-gridlines:has(.p-treetable-tbody):has(.p-treetable-tfoot > tr) .p-treetable-tbody > tr:last-child > td:last-child {
    border-width: 0 1px 0 1px;
}

.p-treetable.p-treetable-sm .p-treetable-header {
    padding: 0.65625rem 0.875rem;
}

.p-treetable.p-treetable-sm .p-treetable-thead>tr>th {
    padding: 0.375rem 0.5rem;
}

.p-treetable.p-treetable-sm .p-treetable-tbody>tr>td {
    padding: 0.375rem 0.5rem;
}

.p-treetable.p-treetable-sm .p-treetable-tfoot>tr>td {
    padding: 0.375rem 0.5rem;
}

.p-treetable.p-treetable-sm .p-treetable-footer {
    padding: 0.375rem 0.5rem;
}

.p-treetable.p-treetable-lg .p-treetable-header {
    padding: 0.9375rem 1.25rem;
}

.p-treetable.p-treetable-lg .p-treetable-thead>tr>th {
    padding: 0.9375rem 1.25rem;
}

.p-treetable.p-treetable-lg .p-treetable-tbody>tr>td {
    padding: 0.9375rem 1.25rem;
}

.p-treetable.p-treetable-lg .p-treetable-tfoot>tr>td {
    padding: 0.9375rem 1.25rem;
}

.p-treetable.p-treetable-lg .p-treetable-footer {
    padding: 0.9375rem 1.25rem;
}

p-treetable-checkbox {
    display: inline-flex;
}

p-treetabletoggler + p-treetable-checkbox .p-checkbox,
p-treetable-toggler + p-treetable-checkbox .p-checkbox,
p-treetable-toggler + p-tree-table-checkbox .p-checkbox,
p-treetabletoggler + p-tree-table-checkbox .p-checkbox {
    vertical-align: middle;
}

p-treetabletoggler + p-treetable-checkbox + span,
p-treetable-toggler + p-treetable-checkbox + span,
p-treetable-toggler + p-tree-table-checkbox + span,
p-treetabletoggler + p-tree-table-checkbox + span {
    vertical-align: middle;
}

p-treetable-sort-icon {
    display: inline-flex;
    align-items: center;
    gap: dt('treetable.header.cell.gap');
}
`;
const classes = {
    root: ({ instance }) => {
        const showGridlines = instance.showGridlines();
        const rowHover = instance.rowHover();
        const selectionMode = instance.selectionMode();
        const autoLayout = instance.autoLayout();
        const resizableColumns = instance.resizableColumns();
        const columnResizeMode = instance.columnResizeMode();
        const scrollable = instance.scrollable();
        const scrollHeight = instance.scrollHeight();
        return [
            'p-treetable p-component',
            {
                'p-treetable-gridlines': showGridlines,
                'p-treetable-hoverable-rows': rowHover || selectionMode === 'single' || selectionMode === 'multiple',
                'p-treetable-auto-layout': autoLayout,
                'p-treetable-resizable': resizableColumns,
                'p-treetable-resizable-fit': resizableColumns && columnResizeMode === 'fit',
                'p-treetable-flex-scrollable': scrollable && scrollHeight === 'flex'
            }
        ];
    },
    loading: 'p-treetable-loading',
    mask: 'p-treetable-mask p-overlay-mask',
    loadingIcon: 'p-treetable-loading-icon',
    header: 'p-treetable-header',
    pcPaginator: ({ instance }) => ['p-treetable-paginator-' + instance.paginatorPosition(), instance.paginatorStyleClass()],
    tableContainer: 'p-treetable-table-container',
    table: ({ instance }) => {
        const scrollable = instance.scrollable();
        const resizableColumns = instance.resizableColumns();
        const columnResizeMode = instance.columnResizeMode();
        return {
            'p-treetable-table': true,
            'p-treetable-scrollable-table': scrollable,
            'p-treetable-resizable-table': resizableColumns,
            'p-treetable-resizable-table-fit': resizableColumns && columnResizeMode === 'fit'
        };
    },
    thead: 'p-treetable-thead',
    sortableColumn: ({ instance }) => ({
        'p-sortable-column': instance.isEnabled(),
        'p-treetable-column-sorted': instance.sorted()
    }),
    sortableColumnIcon: 'p-treetable-sort-icon',
    sortableColumnBadge: 'p-sortable-column-badge',
    columnResizer: 'p-treetable-column-resizer',
    columnHeaderContent: 'p-treetable-column-header-content',
    columnTitle: 'p-treetable-column-title',
    sortIcon: 'p-treetable-sort-icon',
    pcSortBadge: 'p-treetable-sort-badge',
    tbody: 'p-treetable-tbody',
    row: ({ instance }) => ({
        'p-treetable-row-selected': instance.selected()
    }),
    contextMenuRow: ({ instance }) => ({
        'p-treetable-contextmenu-row-selected': instance.selected()
    }),
    toggler: 'p-treetable-toggler',
    nodeToggleButton: 'p-treetable-node-toggle-button',
    nodeToggleIcon: 'p-treetable-node-toggle-icon',
    pcNodeCheckbox: 'p-treetable-node-checkbox',
    tfoot: 'p-treetable-tfoot',
    footerCell: ({ instance }) => ({
        'p-treetable-frozen-column': instance.columnProp('frozen')
    }),
    footer: 'p-treetable-footer',
    columnResizeIndicator: 'p-treetable-column-resize-indicator',
    wrapper: 'p-treetable-wrapper',
    scrollableWrapper: 'p-treetable-scrollable-wrapper',
    scrollableView: 'p-treetable-scrollable-view',
    frozenView: 'p-treetable-frozen-view',
    columnResizerHelper: 'p-column-resizer-helper',
    reorderIndicatorUp: 'p-treetable-reorder-indicator-up',
    reorderIndicatorDown: 'p-treetable-reorder-indicator-down',
    scrollableHeader: 'p-treetable-scrollable-header',
    scrollableHeaderBox: 'p-treetable-scrollable-header-box',
    scrollableHeaderTable: 'p-treetable-scrollable-header-table',
    scrollableBody: 'p-treetable-scrollable-body',
    scrollableFooter: 'p-treetable-scrollable-footer',
    scrollableFooterBox: 'p-treetable-scrollable-footer-box',
    scrollableFooterTable: 'p-treetable-scrollable-footer-table'
};
const inlineStyles = {
    wrapper: { overflow: 'auto' }
};
class TreeTableStyle extends BaseStyle {
    name = 'treetable';
    style = style;
    classes = classes;
    inlineStyles = inlineStyles;
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTableStyle, deps: null, target: i0.ɵɵFactoryTarget.Injectable });
    static ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTableStyle });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTableStyle, decorators: [{
            type: Injectable
        }] });
/**
 *
 * TreeTable is used to display hierarchical data in tabular format.
 *
 * [Live Demo](https://www.primeng.org/treetable/)
 *
 * @module treetablestyle
 *
 */
var TreeTableClasses;
(function (TreeTableClasses) {
    /**
     * Class name of the root element
     */
    TreeTableClasses["root"] = "p-treetable";
    /**
     * Class name of the loading element
     */
    TreeTableClasses["loading"] = "p-treetable-loading";
    /**
     * Class name of the mask element
     */
    TreeTableClasses["mask"] = "p-treetable-mask";
    /**
     * Class name of the loading icon element
     */
    TreeTableClasses["loadingIcon"] = "p-treetable-loading-icon";
    /**
     * Class name of the header element
     */
    TreeTableClasses["header"] = "p-treetable-header";
    /**
     * Class name of the paginator element
     */
    TreeTableClasses["pcPaginator"] = "p-treetable-paginator-[position]";
    /**
     * Class name of the table container element
     */
    TreeTableClasses["tableContainer"] = "p-treetable-table-container";
    /**
     * Class name of the table element
     */
    TreeTableClasses["table"] = "p-treetable-table";
    /**
     * Class name of the thead element
     */
    TreeTableClasses["thead"] = "p-treetable-thead";
    /**
     * Class name of the column resizer element
     */
    TreeTableClasses["columnResizer"] = "p-treetable-column-resizer";
    /**
     * Class name of the column title element
     */
    TreeTableClasses["columnTitle"] = "p-treetable-column-title";
    /**
     * Class name of the sort icon element
     */
    TreeTableClasses["sortIcon"] = "p-treetable-sort-icon";
    /**
     * Class name of the sort badge element
     */
    TreeTableClasses["pcSortBadge"] = "p-treetable-sort-badge";
    /**
     * Class name of the tbody element
     */
    TreeTableClasses["tbody"] = "p-treetable-tbody";
    /**
     * Class name of the node toggle button element
     */
    TreeTableClasses["nodeToggleButton"] = "p-treetable-node-toggle-button";
    /**
     * Class name of the node toggle icon element
     */
    TreeTableClasses["nodeToggleIcon"] = "p-treetable-node-toggle-icon";
    /**
     * Class name of the node checkbox element
     */
    TreeTableClasses["pcNodeCheckbox"] = "p-treetable-node-checkbox";
    /**
     * Class name of the empty message element
     */
    TreeTableClasses["emptyMessage"] = "p-treetable-empty-message";
    /**
     * Class name of the tfoot element
     */
    TreeTableClasses["tfoot"] = "p-treetable-tfoot";
    /**
     * Class name of the footer element
     */
    TreeTableClasses["footer"] = "p-treetable-footer";
    /**
     * Class name of the column resize indicator element
     */
    TreeTableClasses["columnResizeIndicator"] = "p-treetable-column-resize-indicator";
    /**
     * Class name of the wrapper element
     */
    TreeTableClasses["wrapper"] = "p-treetable-wrapper";
    /**
     * Class name of the scrollable wrapper element
     */
    TreeTableClasses["scrollableWrapper"] = "p-treetable-scrollable-wrapper";
    /**
     * Class name of the scrollable view element
     */
    TreeTableClasses["scrollableView"] = "p-treetable-scrollable-view";
    /**
     * Class name of the frozen view element
     */
    TreeTableClasses["frozenView"] = "p-treetable-frozen-view";
    /**
     * Class name of the column resizer helper element
     */
    TreeTableClasses["columnResizerHelper"] = "p-treetable-column-resizer-helper";
    /**
     * Class name of the reorder indicator up element
     */
    TreeTableClasses["reorderIndicatorUp"] = "p-treetable-reorder-indicator-up";
    /**
     * Class name of the reorder indicator down element
     */
    TreeTableClasses["reorderIndicatorDown"] = "p-treetable-reorder-indicator-down";
    /**
     * Class name of the scrollable header element
     */
    TreeTableClasses["scrollableHeader"] = "p-treetable-scrollable-header";
    /**
     * Class name of the scrollable header box element
     */
    TreeTableClasses["scrollableHeaderBox"] = "p-treetable-scrollable-header-box";
    /**
     * Class name of the scrollable header table element
     */
    TreeTableClasses["scrollableHeaderTable"] = "p-treetable-scrollable-header-table";
    /**
     * Class name of the scrollable body element
     */
    TreeTableClasses["scrollableBody"] = "p-treetable-scrollable-body";
    /**
     * Class name of the scrollable footer element
     */
    TreeTableClasses["scrollableFooter"] = "p-treetable-scrollable-footer";
    /**
     * Class name of the scrollable footer box element
     */
    TreeTableClasses["scrollableFooterBox"] = "p-treetable-scrollable-footer-box";
    /**
     * Class name of the scrollable footer table element
     */
    TreeTableClasses["scrollableFooterTable"] = "p-treetable-scrollable-footer-table";
    /**
     * Class name of the sortable column icon element
     */
    TreeTableClasses["sortableColumnIcon"] = "p-sortable-column-icon";
})(TreeTableClasses || (TreeTableClasses = {}));

const TREETABLE_INSTANCE = new InjectionToken('TREETABLE_INSTANCE');
class TreeTableService {
    sortSource = new Subject();
    selectionSource = new Subject();
    contextMenuSource = new Subject();
    uiUpdateSource = new Subject();
    totalRecordsSource = new Subject();
    sortSource$ = this.sortSource.asObservable();
    selectionSource$ = this.selectionSource.asObservable();
    contextMenuSource$ = this.contextMenuSource.asObservable();
    uiUpdateSource$ = this.uiUpdateSource.asObservable();
    totalRecordsSource$ = this.totalRecordsSource.asObservable();
    onSort(sortMeta) {
        this.sortSource.next(sortMeta);
    }
    onSelectionChange() {
        this.selectionSource.next(null);
    }
    onContextMenu(node) {
        this.contextMenuSource.next(node);
    }
    onUIUpdate(value) {
        this.uiUpdateSource.next(value);
    }
    onTotalRecordsChange(value) {
        this.totalRecordsSource.next(value);
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTableService, deps: [], target: i0.ɵɵFactoryTarget.Injectable });
    static ɵprov = i0.ɵɵngDeclareInjectable({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTableService });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTableService, decorators: [{
            type: Injectable
        }] });

class TTBody extends BaseComponent {
    columns = input(undefined, { ...(ngDevMode ? { debugName: "columns" } : /* istanbul ignore next */ {}), alias: 'pTreeTableBody' });
    template = input(undefined, { ...(ngDevMode ? { debugName: "template" } : /* istanbul ignore next */ {}), alias: 'pTreeTableBodyTemplate' });
    frozen = input(undefined, { ...(ngDevMode ? { debugName: "frozen" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    serializedNodes = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "serializedNodes" }] : /* istanbul ignore next */ []));
    scrollerOptions = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "scrollerOptions" }] : /* istanbul ignore next */ []));
    tt = inject(TREETABLE_INSTANCE);
    emptyMessageContext = computed(() => ({ $implicit: this.columns(), frozen: this.frozen() }), /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "emptyMessageContext" }] : /* istanbul ignore next */ []));
    constructor() {
        super();
        this.tt.tableService.uiUpdateSource$.pipe(takeUntilDestroyed()).subscribe(() => {
            if (this.tt.virtualScroll()) {
                this.cd.detectChanges();
            }
        });
    }
    getBodyTemplateContext(serializedNode) {
        return {
            $implicit: serializedNode,
            node: serializedNode.node,
            rowData: serializedNode.node.data,
            columns: this.columns()
        };
    }
    getScrollerOption(option, options) {
        if (this.tt.virtualScroll()) {
            options = options || this.scrollerOptions();
            return options ? options[option] : null;
        }
        return null;
    }
    getRowIndex(rowIndex) {
        const getItemOptions = this.getScrollerOption('getItemOptions');
        return getItemOptions ? getItemOptions(rowIndex).index : rowIndex;
    }
    get dataP() {
        return this.cn({
            hoverable: this.tt.rowHover() || this.tt.selectionMode(),
            frozen: this.frozen()
        });
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTBody, deps: [], target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.0.0", version: "22.0.6", type: TTBody, isStandalone: true, selector: "[pTreeTableBody]", inputs: { columns: { classPropertyName: "columns", publicName: "pTreeTableBody", isSignal: true, isRequired: false, transformFunction: null }, template: { classPropertyName: "template", publicName: "pTreeTableBodyTemplate", isSignal: true, isRequired: false, transformFunction: null }, frozen: { classPropertyName: "frozen", publicName: "frozen", isSignal: true, isRequired: false, transformFunction: null }, serializedNodes: { classPropertyName: "serializedNodes", publicName: "serializedNodes", isSignal: true, isRequired: false, transformFunction: null }, scrollerOptions: { classPropertyName: "scrollerOptions", publicName: "scrollerOptions", isSignal: true, isRequired: false, transformFunction: null } }, host: { properties: { "attr.data-p": "dataP" } }, usesInheritance: true, ngImport: i0, template: `
        @for (serializedNode of serializedNodes() || tt.serializedValue; track tt.rowTrackBy()($index, serializedNode)) {
            @if (serializedNode.visible) {
                <ng-container *ngTemplateOutlet="template(); context: getBodyTemplateContext(serializedNode)"></ng-container>
            }
        }
        @if (tt.isEmpty()) {
            <ng-container *ngTemplateOutlet="tt.emptyMessageTemplate(); context: emptyMessageContext()"></ng-container>
        }
    `, isInline: true, dependencies: [{ kind: "directive", type: NgTemplateOutlet, selector: "[ngTemplateOutlet]", inputs: ["ngTemplateOutletContext", "ngTemplateOutlet", "ngTemplateOutletInjector"] }], encapsulation: i0.ViewEncapsulation.None });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTBody, decorators: [{
            type: Component,
            args: [{
                    selector: '[pTreeTableBody]',
                    standalone: true,
                    imports: [NgTemplateOutlet],
                    template: `
        @for (serializedNode of serializedNodes() || tt.serializedValue; track tt.rowTrackBy()($index, serializedNode)) {
            @if (serializedNode.visible) {
                <ng-container *ngTemplateOutlet="template(); context: getBodyTemplateContext(serializedNode)"></ng-container>
            }
        }
        @if (tt.isEmpty()) {
            <ng-container *ngTemplateOutlet="tt.emptyMessageTemplate(); context: emptyMessageContext()"></ng-container>
        }
    `,
                    encapsulation: ViewEncapsulation.None,
                    host: {
                        '[attr.data-p]': 'dataP'
                    }
                }]
        }], ctorParameters: () => [], propDecorators: { columns: [{ type: i0.Input, args: [{ isSignal: true, alias: "pTreeTableBody", required: false }] }], template: [{ type: i0.Input, args: [{ isSignal: true, alias: "pTreeTableBodyTemplate", required: false }] }], frozen: [{ type: i0.Input, args: [{ isSignal: true, alias: "frozen", required: false }] }], serializedNodes: [{ type: i0.Input, args: [{ isSignal: true, alias: "serializedNodes", required: false }] }], scrollerOptions: [{ type: i0.Input, args: [{ isSignal: true, alias: "scrollerOptions", required: false }] }] } });

class TTScrollableView extends BaseComponent {
    hostName = 'TreeTable';
    columns = input(undefined, { ...(ngDevMode ? { debugName: "columns" } : /* istanbul ignore next */ {}), alias: 'ttScrollableView' });
    frozen = input(undefined, { ...(ngDevMode ? { debugName: "frozen" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    scrollHeight = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "scrollHeight" }] : /* istanbul ignore next */ []));
    scrollHeaderViewChild = viewChild('scrollHeader', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "scrollHeaderViewChild" }] : /* istanbul ignore next */ []));
    scrollHeaderBoxViewChild = viewChild('scrollHeaderBox', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "scrollHeaderBoxViewChild" }] : /* istanbul ignore next */ []));
    scrollBodyViewChild = viewChild('scrollBody', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "scrollBodyViewChild" }] : /* istanbul ignore next */ []));
    scrollTableViewChild = viewChild('scrollTable', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "scrollTableViewChild" }] : /* istanbul ignore next */ []));
    scrollLoadingTableViewChild = viewChild('loadingTable', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "scrollLoadingTableViewChild" }] : /* istanbul ignore next */ []));
    scrollFooterViewChild = viewChild('scrollFooter', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "scrollFooterViewChild" }] : /* istanbul ignore next */ []));
    scrollFooterBoxViewChild = viewChild('scrollFooterBox', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "scrollFooterBoxViewChild" }] : /* istanbul ignore next */ []));
    scrollableAlignerViewChild = viewChild('scrollableAligner', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "scrollableAlignerViewChild" }] : /* istanbul ignore next */ []));
    scroller = viewChild('scroller', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "scroller" }] : /* istanbul ignore next */ []));
    headerScrollListener;
    bodyScrollListener;
    footerScrollListener;
    frozenSiblingBody;
    preventBodyScrollPropagation;
    _componentStyle = inject(TreeTableStyle);
    tt = inject(TREETABLE_INSTANCE);
    colGroupTemplate = computed(() => (this.frozen() ? this.tt.frozenColGroupTemplate() || this.tt.colGroupTemplate() : this.tt.colGroupTemplate()), /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "colGroupTemplate" }] : /* istanbul ignore next */ []));
    $headerTemplate = computed(() => (this.frozen() ? this.tt.frozenHeaderTemplate() || this.tt.headerTemplate() : this.tt.headerTemplate()), /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "$headerTemplate" }] : /* istanbul ignore next */ []));
    $footerTemplate = computed(() => (this.frozen() ? this.tt.frozenFooterTemplate() || this.tt.footerTemplate() : this.tt.footerTemplate()), /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "$footerTemplate" }] : /* istanbul ignore next */ []));
    $bodyTemplate = computed(() => (this.frozen() ? this.tt.frozenBodyTemplate() || this.tt.bodyTemplate() : this.tt.bodyTemplate()), /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "$bodyTemplate" }] : /* istanbul ignore next */ []));
    scrollerStyle = computed(() => ({ height: this.tt.scrollHeight() !== 'flex' ? this.tt.scrollHeight() : undefined }), /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "scrollerStyle" }] : /* istanbul ignore next */ []));
    $scrollerScrollHeight = computed(() => (this.scrollHeight() !== 'flex' ? undefined : '100%'), /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "$scrollerScrollHeight" }] : /* istanbul ignore next */ []));
    scrollBodyStyle = computed(() => ({
        'max-height': this.tt.scrollHeight() !== 'flex' ? this.scrollHeight() : undefined,
        'overflow-y': !this.frozen() && this.tt.scrollHeight() ? 'scroll' : undefined
    }), /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "scrollBodyStyle" }] : /* istanbul ignore next */ []));
    constructor() {
        super();
        effect(() => {
            const val = this.scrollHeight();
            if (val != null && (val.includes('%') || val.includes('calc'))) {
                console.log('Percentage scroll height calculation is removed in favor of the more performant CSS based flex mode, use scrollHeight="flex" instead.');
            }
        });
    }
    get serializedValue() {
        return this.tt.serializedValue;
    }
    onAfterViewInit() {
        if (isPlatformBrowser(this.platformId)) {
            if (!this.frozen()) {
                if (this.tt.frozenColumns() || this.tt.frozenBodyTemplate()) {
                    addClass(this.el.nativeElement, 'p-treetable-unfrozen-view');
                }
                let frozenView = this.el.nativeElement.previousElementSibling;
                if (frozenView) {
                    if (this.tt.virtualScroll())
                        this.frozenSiblingBody = findSingle(frozenView, '[data-pc-name="virtualscroller"]');
                    else
                        this.frozenSiblingBody = findSingle(frozenView, '[data-pc-section="scrollablebody"]');
                }
                if (this.scrollHeight()) {
                    let scrollBarWidth = calculateScrollbarWidth();
                    if (this.scrollHeaderBoxViewChild()?.nativeElement) {
                        this.scrollHeaderBoxViewChild().nativeElement.style.paddingRight = scrollBarWidth + 'px';
                    }
                    if (this.scrollFooterBoxViewChild()?.nativeElement) {
                        this.scrollFooterBoxViewChild().nativeElement.style.paddingRight = scrollBarWidth + 'px';
                    }
                }
            }
            else {
                if (this.scrollableAlignerViewChild()?.nativeElement) {
                    this.scrollableAlignerViewChild().nativeElement.style.height = calculateScrollbarHeight() + 'px';
                }
            }
            this.bindEvents();
        }
    }
    bindEvents() {
        if (isPlatformBrowser(this.platformId)) {
            if (this.scrollHeaderViewChild()?.nativeElement) {
                this.headerScrollListener = this.renderer.listen(this.scrollHeaderBoxViewChild()?.nativeElement, 'scroll', this.onHeaderScroll.bind(this));
            }
            if (this.scrollFooterViewChild()?.nativeElement) {
                this.footerScrollListener = this.renderer.listen(this.scrollFooterViewChild().nativeElement, 'scroll', this.onFooterScroll.bind(this));
            }
            if (!this.frozen()) {
                if (this.tt.virtualScroll()) {
                    this.bodyScrollListener = this.renderer.listen((this.scroller()?.getElementRef()).nativeElement, 'scroll', this.onBodyScroll.bind(this));
                }
                else {
                    this.bodyScrollListener = this.renderer.listen(this.scrollBodyViewChild()?.nativeElement, 'scroll', this.onBodyScroll.bind(this));
                }
            }
        }
    }
    unbindEvents() {
        if (isPlatformBrowser(this.platformId)) {
            if (this.scrollHeaderViewChild()?.nativeElement) {
                if (this.headerScrollListener) {
                    this.headerScrollListener();
                    this.headerScrollListener = null;
                }
            }
            if (this.scrollFooterViewChild()?.nativeElement) {
                if (this.footerScrollListener) {
                    this.footerScrollListener();
                    this.footerScrollListener = null;
                }
            }
            if (this.scrollBodyViewChild()?.nativeElement) {
                if (this.bodyScrollListener) {
                    this.bodyScrollListener();
                    this.bodyScrollListener = null;
                }
            }
            if (this.scroller()?.getElementRef()) {
                if (this.bodyScrollListener) {
                    this.bodyScrollListener();
                    this.bodyScrollListener = null;
                }
            }
        }
    }
    onHeaderScroll() {
        const scrollLeft = this.scrollHeaderViewChild()?.nativeElement.scrollLeft;
        this.scrollBodyViewChild().nativeElement.scrollLeft = scrollLeft;
        if (this.scrollFooterViewChild()?.nativeElement) {
            this.scrollFooterViewChild().nativeElement.scrollLeft = scrollLeft;
        }
        this.preventBodyScrollPropagation = true;
    }
    onFooterScroll() {
        const scrollLeft = this.scrollFooterViewChild()?.nativeElement.scrollLeft;
        this.scrollBodyViewChild().nativeElement.scrollLeft = scrollLeft;
        if (this.scrollHeaderViewChild()?.nativeElement) {
            this.scrollHeaderViewChild().nativeElement.scrollLeft = scrollLeft;
        }
        this.preventBodyScrollPropagation = true;
    }
    onBodyScroll(event) {
        if (this.preventBodyScrollPropagation) {
            this.preventBodyScrollPropagation = false;
            return;
        }
        if (this.scrollHeaderViewChild()?.nativeElement) {
            this.scrollHeaderBoxViewChild().nativeElement.style.marginLeft = -1 * event.target.scrollLeft + 'px';
        }
        if (this.scrollFooterViewChild()?.nativeElement) {
            this.scrollFooterBoxViewChild().nativeElement.style.marginLeft = -1 * event.target.scrollLeft + 'px';
        }
        if (this.frozenSiblingBody) {
            this.frozenSiblingBody.scrollTop = event.target.scrollTop;
        }
    }
    scrollToVirtualIndex(index) {
        this.scroller()?.scrollToIndex(index);
    }
    scrollTo(options) {
        if (this.scroller()) {
            this.scroller().scrollTo(options);
        }
        else {
            if (this.scrollBodyViewChild()?.nativeElement.scrollTo) {
                this.scrollBodyViewChild().nativeElement.scrollTo(options);
            }
            else {
                this.scrollBodyViewChild().nativeElement.scrollLeft = options.left;
                this.scrollBodyViewChild().nativeElement.scrollTop = options.top;
            }
        }
    }
    getScrollableTableStyle(contentStyle) {
        return { ...this.tt.tableStyle(), ...contentStyle };
    }
    onDestroy() {
        this.unbindEvents();
        this.frozenSiblingBody = null;
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTScrollableView, deps: [], target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.0.0", version: "22.0.6", type: TTScrollableView, isStandalone: true, selector: "[ttScrollableView]", inputs: { columns: { classPropertyName: "columns", publicName: "ttScrollableView", isSignal: true, isRequired: false, transformFunction: null }, frozen: { classPropertyName: "frozen", publicName: "frozen", isSignal: true, isRequired: false, transformFunction: null }, scrollHeight: { classPropertyName: "scrollHeight", publicName: "scrollHeight", isSignal: true, isRequired: false, transformFunction: null } }, providers: [TreeTableStyle], viewQueries: [{ propertyName: "scrollHeaderViewChild", first: true, predicate: ["scrollHeader"], descendants: true, isSignal: true }, { propertyName: "scrollHeaderBoxViewChild", first: true, predicate: ["scrollHeaderBox"], descendants: true, isSignal: true }, { propertyName: "scrollBodyViewChild", first: true, predicate: ["scrollBody"], descendants: true, isSignal: true }, { propertyName: "scrollTableViewChild", first: true, predicate: ["scrollTable"], descendants: true, isSignal: true }, { propertyName: "scrollLoadingTableViewChild", first: true, predicate: ["loadingTable"], descendants: true, isSignal: true }, { propertyName: "scrollFooterViewChild", first: true, predicate: ["scrollFooter"], descendants: true, isSignal: true }, { propertyName: "scrollFooterBoxViewChild", first: true, predicate: ["scrollFooterBox"], descendants: true, isSignal: true }, { propertyName: "scrollableAlignerViewChild", first: true, predicate: ["scrollableAligner"], descendants: true, isSignal: true }, { propertyName: "scroller", first: true, predicate: ["scroller"], descendants: true, isSignal: true }], usesInheritance: true, ngImport: i0, template: `
        <div #scrollHeader [class]="cx('scrollableHeader')" [pBind]="ptm('scrollableHeader')">
            <div #scrollHeaderBox [class]="cx('scrollableHeaderBox')" [pBind]="ptm('scrollableHeaderBox')">
                <table [class]="cn(cx('scrollableHeaderTable'), tt.tableStyleClass())" [pBind]="ptm('scrollableHeaderTable')" [style]="tt.tableStyle()">
                    <ng-container *ngTemplateOutlet="colGroupTemplate(); context: { $implicit: columns() }"></ng-container>
                    <thead role="rowgroup" [class]="cx('thead')" [pBind]="ptm('thead')">
                        <ng-container *ngTemplateOutlet="$headerTemplate(); context: { $implicit: columns() }"></ng-container>
                    </thead>
                </table>
            </div>
        </div>

        @if (tt.virtualScroll()) {
            <p-scroller
                #scroller
                [items]="tt.serializedValue"
                [styleClass]="cx('scrollableBody')"
                [style]="scrollerStyle()"
                [scrollHeight]="$scrollerScrollHeight()"
                [itemSize]="tt.virtualScrollItemSize()"
                [lazy]="tt.lazy()"
                (onLazyLoad)="tt.onLazyItemLoad($event)"
                [options]="tt.virtualScrollOptions()"
                [pt]="ptm('virtualScroller')"
            >
                <ng-template #content let-items let-scrollerOptions="options">
                    <ng-container *ngTemplateOutlet="buildInItems; context: { $implicit: items, options: scrollerOptions }"></ng-container>
                </ng-template>
                @if (tt.loaderTemplate()) {
                    <ng-template #loader let-scrollerOptions="options">
                        <ng-container *ngTemplateOutlet="tt.loaderTemplate(); context: { options: scrollerOptions }"></ng-container>
                    </ng-template>
                }
            </p-scroller>
        }
        @if (!tt.virtualScroll()) {
            <div #scrollBody [class]="cx('scrollableBody')" [pBind]="ptm('scrollableBody')" [style]="scrollBodyStyle()">
                <ng-container *ngTemplateOutlet="buildInItems; context: { $implicit: serializedValue, options: {} }"></ng-container>
            </div>
        }

        <ng-template #buildInItems let-items let-scrollerOptions="options">
            <table role="treegrid" #scrollTable [pBind]="ptm('table')" [class]="cn(tt.tableStyleClass(), scrollerOptions.contentStyleClass)" [style]="getScrollableTableStyle(scrollerOptions.contentStyle)">
                <ng-container *ngTemplateOutlet="colGroupTemplate(); context: { $implicit: columns() }"></ng-container>
                <tbody [pBind]="ptm('tbody')" role="rowgroup" [class]="cx('tbody')" [pTreeTableBody]="columns()" [unstyled]="unstyled()" [pTreeTableBodyTemplate]="$bodyTemplate()" [serializedNodes]="items" [frozen]="frozen()"></tbody>
            </table>
            @if (frozen()) {
                <div #scrollableAligner [style.background-color]="'transparent'"></div>
            }
        </ng-template>

        @if (tt.footerTemplate()) {
            <div #scrollFooter [class]="cx('scrollableFooter')" [pBind]="ptm('scrollableFooter')">
                <div #scrollFooterBox [class]="cx('scrollableFooterBox')" [pBind]="ptm('scrollableFooterBox')">
                    <table [class]="cn(cx('scrollableFooterTable'), tt.tableStyleClass())" [style]="tt.tableStyle()" [pBind]="ptm('scrollableFooterTable')">
                        <ng-container *ngTemplateOutlet="colGroupTemplate(); context: { $implicit: columns() }"></ng-container>
                        <tfoot role="rowgroup" [class]="cx('tfoot')" [pBind]="ptm('tfoot')">
                            <ng-container *ngTemplateOutlet="$footerTemplate(); context: { $implicit: columns() }"></ng-container>
                        </tfoot>
                    </table>
                </div>
            </div>
        }
    `, isInline: true, dependencies: [{ kind: "directive", type: NgTemplateOutlet, selector: "[ngTemplateOutlet]", inputs: ["ngTemplateOutletContext", "ngTemplateOutlet", "ngTemplateOutletInjector"] }, { kind: "directive", type: Bind, selector: "[pBind]", inputs: ["pBind"] }, { kind: "component", type: Scroller, selector: "p-scroller, p-virtualscroller, p-virtual-scroller", inputs: ["hostName", "id", "style", "styleClass", "tabindex", "items", "itemSize", "scrollHeight", "scrollWidth", "orientation", "step", "delay", "resizeDelay", "appendOnly", "inline", "lazy", "disabled", "loaderDisabled", "columns", "showSpacer", "showLoader", "numToleratedItems", "loading", "autoSize", "trackBy", "options"], outputs: ["onLazyLoad", "onScroll", "onScrollIndexChange"] }, { kind: "component", type: TTBody, selector: "[pTreeTableBody]", inputs: ["pTreeTableBody", "pTreeTableBodyTemplate", "frozen", "serializedNodes", "scrollerOptions"] }], encapsulation: i0.ViewEncapsulation.None });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTScrollableView, decorators: [{
            type: Component,
            args: [{
                    selector: '[ttScrollableView]',
                    standalone: true,
                    imports: [NgTemplateOutlet, Bind, Scroller, TTBody],
                    template: `
        <div #scrollHeader [class]="cx('scrollableHeader')" [pBind]="ptm('scrollableHeader')">
            <div #scrollHeaderBox [class]="cx('scrollableHeaderBox')" [pBind]="ptm('scrollableHeaderBox')">
                <table [class]="cn(cx('scrollableHeaderTable'), tt.tableStyleClass())" [pBind]="ptm('scrollableHeaderTable')" [style]="tt.tableStyle()">
                    <ng-container *ngTemplateOutlet="colGroupTemplate(); context: { $implicit: columns() }"></ng-container>
                    <thead role="rowgroup" [class]="cx('thead')" [pBind]="ptm('thead')">
                        <ng-container *ngTemplateOutlet="$headerTemplate(); context: { $implicit: columns() }"></ng-container>
                    </thead>
                </table>
            </div>
        </div>

        @if (tt.virtualScroll()) {
            <p-scroller
                #scroller
                [items]="tt.serializedValue"
                [styleClass]="cx('scrollableBody')"
                [style]="scrollerStyle()"
                [scrollHeight]="$scrollerScrollHeight()"
                [itemSize]="tt.virtualScrollItemSize()"
                [lazy]="tt.lazy()"
                (onLazyLoad)="tt.onLazyItemLoad($event)"
                [options]="tt.virtualScrollOptions()"
                [pt]="ptm('virtualScroller')"
            >
                <ng-template #content let-items let-scrollerOptions="options">
                    <ng-container *ngTemplateOutlet="buildInItems; context: { $implicit: items, options: scrollerOptions }"></ng-container>
                </ng-template>
                @if (tt.loaderTemplate()) {
                    <ng-template #loader let-scrollerOptions="options">
                        <ng-container *ngTemplateOutlet="tt.loaderTemplate(); context: { options: scrollerOptions }"></ng-container>
                    </ng-template>
                }
            </p-scroller>
        }
        @if (!tt.virtualScroll()) {
            <div #scrollBody [class]="cx('scrollableBody')" [pBind]="ptm('scrollableBody')" [style]="scrollBodyStyle()">
                <ng-container *ngTemplateOutlet="buildInItems; context: { $implicit: serializedValue, options: {} }"></ng-container>
            </div>
        }

        <ng-template #buildInItems let-items let-scrollerOptions="options">
            <table role="treegrid" #scrollTable [pBind]="ptm('table')" [class]="cn(tt.tableStyleClass(), scrollerOptions.contentStyleClass)" [style]="getScrollableTableStyle(scrollerOptions.contentStyle)">
                <ng-container *ngTemplateOutlet="colGroupTemplate(); context: { $implicit: columns() }"></ng-container>
                <tbody [pBind]="ptm('tbody')" role="rowgroup" [class]="cx('tbody')" [pTreeTableBody]="columns()" [unstyled]="unstyled()" [pTreeTableBodyTemplate]="$bodyTemplate()" [serializedNodes]="items" [frozen]="frozen()"></tbody>
            </table>
            @if (frozen()) {
                <div #scrollableAligner [style.background-color]="'transparent'"></div>
            }
        </ng-template>

        @if (tt.footerTemplate()) {
            <div #scrollFooter [class]="cx('scrollableFooter')" [pBind]="ptm('scrollableFooter')">
                <div #scrollFooterBox [class]="cx('scrollableFooterBox')" [pBind]="ptm('scrollableFooterBox')">
                    <table [class]="cn(cx('scrollableFooterTable'), tt.tableStyleClass())" [style]="tt.tableStyle()" [pBind]="ptm('scrollableFooterTable')">
                        <ng-container *ngTemplateOutlet="colGroupTemplate(); context: { $implicit: columns() }"></ng-container>
                        <tfoot role="rowgroup" [class]="cx('tfoot')" [pBind]="ptm('tfoot')">
                            <ng-container *ngTemplateOutlet="$footerTemplate(); context: { $implicit: columns() }"></ng-container>
                        </tfoot>
                    </table>
                </div>
            </div>
        }
    `,
                    encapsulation: ViewEncapsulation.None,
                    providers: [TreeTableStyle]
                }]
        }], ctorParameters: () => [], propDecorators: { columns: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttScrollableView", required: false }] }], frozen: [{ type: i0.Input, args: [{ isSignal: true, alias: "frozen", required: false }] }], scrollHeight: [{ type: i0.Input, args: [{ isSignal: true, alias: "scrollHeight", required: false }] }], scrollHeaderViewChild: [{ type: i0.ViewChild, args: ['scrollHeader', { isSignal: true }] }], scrollHeaderBoxViewChild: [{ type: i0.ViewChild, args: ['scrollHeaderBox', { isSignal: true }] }], scrollBodyViewChild: [{ type: i0.ViewChild, args: ['scrollBody', { isSignal: true }] }], scrollTableViewChild: [{ type: i0.ViewChild, args: ['scrollTable', { isSignal: true }] }], scrollLoadingTableViewChild: [{ type: i0.ViewChild, args: ['loadingTable', { isSignal: true }] }], scrollFooterViewChild: [{ type: i0.ViewChild, args: ['scrollFooter', { isSignal: true }] }], scrollFooterBoxViewChild: [{ type: i0.ViewChild, args: ['scrollFooterBox', { isSignal: true }] }], scrollableAlignerViewChild: [{ type: i0.ViewChild, args: ['scrollableAligner', { isSignal: true }] }], scroller: [{ type: i0.ViewChild, args: ['scroller', { isSignal: true }] }] } });

/**
 * TreeTable is used to display hierarchical data in tabular format.
 * @group Components
 */
class TreeTable extends BaseComponent {
    componentName = 'TreeTable';
    _componentStyle = inject(TreeTableStyle);
    bindDirectiveInstance = inject(Bind, { self: true });
    onAfterViewChecked() {
        this.bindDirectiveInstance.setAttrs(this.ptms(['host', 'root']));
    }
    /**
     * An array of objects to represent dynamic columns.
     * @group Props
     */
    columns = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "columns" }] : /* istanbul ignore next */ []));
    /**
     * Inline style of the table.
     * @group Props
     */
    tableStyle = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "tableStyle" }] : /* istanbul ignore next */ []));
    /**
     * Style class of the table.
     * @group Props
     */
    tableStyleClass = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "tableStyleClass" }] : /* istanbul ignore next */ []));
    /**
     * Whether the cell widths scale according to their content or not.
     * @group Props
     */
    autoLayout = input(undefined, { ...(ngDevMode ? { debugName: "autoLayout" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Defines if data is loaded and interacted with in lazy manner.
     * @group Props
     */
    lazy = input(false, { ...(ngDevMode ? { debugName: "lazy" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Whether to call lazy loading on initialization.
     * @group Props
     */
    lazyLoadOnInit = input(true, { ...(ngDevMode ? { debugName: "lazyLoadOnInit" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * When specified as true, enables the pagination.
     * @group Props
     */
    paginator = input(undefined, { ...(ngDevMode ? { debugName: "paginator" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Number of rows to display per page.
     * @group Props
     */
    rows = model(undefined, /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "rows" }] : /* istanbul ignore next */ []));
    /**
     * Index of the first row to be displayed.
     * @group Props
     */
    firstInput = input(0, { ...(ngDevMode ? { debugName: "firstInput" } : /* istanbul ignore next */ {}), alias: 'first', transform: numberAttribute });
    /**
     * Number of page links to display in paginator.
     * @group Props
     */
    pageLinks = input(5, { ...(ngDevMode ? { debugName: "pageLinks" } : /* istanbul ignore next */ {}), transform: numberAttribute });
    /**
     * Array of integer/object values to display inside rows per page dropdown of paginator
     * @group Props
     */
    rowsPerPageOptions = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "rowsPerPageOptions" }] : /* istanbul ignore next */ []));
    /**
     * Whether to show it even there is only one page.
     * @group Props
     */
    alwaysShowPaginator = input(true, { ...(ngDevMode ? { debugName: "alwaysShowPaginator" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Position of the paginator.
     * @group Props
     */
    paginatorPosition = input('bottom', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "paginatorPosition" }] : /* istanbul ignore next */ []));
    /**
     * Custom style class for paginator
     * @group Props
     */
    paginatorStyleClass = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "paginatorStyleClass" }] : /* istanbul ignore next */ []));
    /**
     * Target element to attach the paginator dropdown overlay, valid values are "body" or a local ng-template variable of another element (note: use binding with brackets for template variables, e.g. [appendTo]="mydiv" for a div element having #mydiv as variable name).
     * @group Props
     */
    paginatorDropdownAppendTo = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "paginatorDropdownAppendTo" }] : /* istanbul ignore next */ []));
    /**
     * Template of the current page report element. Available placeholders are {currentPage},{totalPages},{rows},{first},{last} and {totalRecords}
     * @group Props
     */
    currentPageReportTemplate = input('{currentPage} of {totalPages}', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "currentPageReportTemplate" }] : /* istanbul ignore next */ []));
    /**
     * Whether to display current page report.
     * @group Props
     */
    showCurrentPageReport = input(undefined, { ...(ngDevMode ? { debugName: "showCurrentPageReport" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Whether to display a dropdown to navigate to any page.
     * @group Props
     */
    showJumpToPageDropdown = input(undefined, { ...(ngDevMode ? { debugName: "showJumpToPageDropdown" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * When enabled, icons are displayed on paginator to go first and last page.
     * @group Props
     */
    showFirstLastIcon = input(true, { ...(ngDevMode ? { debugName: "showFirstLastIcon" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Whether to show page links.
     * @group Props
     */
    showPageLinks = input(true, { ...(ngDevMode ? { debugName: "showPageLinks" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Sort order to use when an unsorted column gets sorted by user interaction.
     * @group Props
     */
    defaultSortOrder = input(1, { ...(ngDevMode ? { debugName: "defaultSortOrder" } : /* istanbul ignore next */ {}), transform: numberAttribute });
    /**
     * Defines whether sorting works on single column or on multiple columns.
     * @group Props
     */
    sortMode = input('single', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "sortMode" }] : /* istanbul ignore next */ []));
    /**
     * When true, resets paginator to first page after sorting.
     * @group Props
     */
    resetPageOnSort = input(true, { ...(ngDevMode ? { debugName: "resetPageOnSort" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Whether to use the default sorting or a custom one using sortFunction.
     * @group Props
     */
    customSort = input(undefined, { ...(ngDevMode ? { debugName: "customSort" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Specifies the selection mode, valid values are "single" and "multiple".
     * @group Props
     */
    selectionMode = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "selectionMode" }] : /* istanbul ignore next */ []));
    /**
     * Selected row with a context menu.
     * @group Props
     */
    contextMenuSelection = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "contextMenuSelection" }] : /* istanbul ignore next */ []));
    /**
     * A property to uniquely identify a record in data.
     * @group Props
     */
    dataKey = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "dataKey" }] : /* istanbul ignore next */ []));
    /**
     * Defines whether metaKey is should be considered for the selection. On touch enabled devices, metaKeySelection is turned off automatically.
     * @group Props
     */
    metaKeySelection = input(false, { ...(ngDevMode ? { debugName: "metaKeySelection" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Algorithm to define if a row is selected, valid values are "equals" that compares by reference and "deepEquals" that compares all fields.
     * @group Props
     */
    compareSelectionBy = input('deepEquals', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "compareSelectionBy" }] : /* istanbul ignore next */ []));
    /**
     * Adds hover effect to rows without the need for selectionMode.
     * @group Props
     */
    rowHover = input(undefined, { ...(ngDevMode ? { debugName: "rowHover" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Displays a loader to indicate data load is in progress.
     * @group Props
     */
    loading = input(undefined, { ...(ngDevMode ? { debugName: "loading" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * The icon to show while indicating data load is in progress.
     * @group Props
     */
    loadingIcon = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "loadingIcon" }] : /* istanbul ignore next */ []));
    /**
     * Whether to show the loading mask when loading property is true.
     * @group Props
     */
    showLoader = input(true, { ...(ngDevMode ? { debugName: "showLoader" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * When specified, enables horizontal and/or vertical scrolling.
     * @group Props
     */
    scrollable = input(undefined, { ...(ngDevMode ? { debugName: "scrollable" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Height of the scroll viewport in fixed pixels or the "flex" keyword for a dynamic size.
     * @group Props
     */
    scrollHeight = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "scrollHeight" }] : /* istanbul ignore next */ []));
    /**
     * Whether the data should be loaded on demand during scroll.
     * @group Props
     */
    virtualScroll = input(undefined, { ...(ngDevMode ? { debugName: "virtualScroll" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Height of a row to use in calculations of virtual scrolling.
     * @group Props
     */
    virtualScrollItemSize = input(undefined, { ...(ngDevMode ? { debugName: "virtualScrollItemSize" } : /* istanbul ignore next */ {}), transform: numberAttribute });
    /**
     * Whether to use the scroller feature. The properties of scroller component can be used like an object in it.
     * @group Props
     */
    virtualScrollOptions = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "virtualScrollOptions" }] : /* istanbul ignore next */ []));
    /**
     * The delay (in milliseconds) before triggering the virtual scroll. This determines the time gap between the user's scroll action and the actual rendering of the next set of items in the virtual scroll.
     * @group Props
     */
    virtualScrollDelay = input(150, { ...(ngDevMode ? { debugName: "virtualScrollDelay" } : /* istanbul ignore next */ {}), transform: numberAttribute });
    /**
     * Width of the frozen columns container.
     * @group Props
     */
    frozenWidth = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "frozenWidth" }] : /* istanbul ignore next */ []));
    /**
     * An array of objects to represent dynamic columns that are frozen.
     * @group Props
     */
    frozenColumns = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "frozenColumns" }] : /* istanbul ignore next */ []));
    /**
     * When enabled, columns can be resized using drag and drop.
     * @group Props
     */
    resizableColumns = input(undefined, { ...(ngDevMode ? { debugName: "resizableColumns" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Defines whether the overall table width should change on column resize, valid values are "fit" and "expand".
     * @group Props
     */
    columnResizeMode = input('fit', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "columnResizeMode" }] : /* istanbul ignore next */ []));
    /**
     * When enabled, columns can be reordered using drag and drop.
     * @group Props
     */
    reorderableColumns = input(undefined, { ...(ngDevMode ? { debugName: "reorderableColumns" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Local ng-template varilable of a ContextMenu.
     * @group Props
     */
    contextMenu = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "contextMenu" }] : /* istanbul ignore next */ []));
    /**
     * Function to optimize the dom operations by delegating to ngForTrackBy, default algorithm checks for object identity.
     * @group Props
     */
    rowTrackBy = input((index, item) => item, /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "rowTrackBy" }] : /* istanbul ignore next */ []));
    /**
     * An array of FilterMetadata objects to provide external filters.
     * @group Props
     */
    filtersInput = input({}, { ...(ngDevMode ? { debugName: "filtersInput" } : /* istanbul ignore next */ {}), alias: 'filters' });
    /**
     * An array of fields as string to use in global filtering.
     * @group Props
     */
    globalFilterFields = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "globalFilterFields" }] : /* istanbul ignore next */ []));
    /**
     * Delay in milliseconds before filtering the data.
     * @group Props
     */
    filterDelay = input(300, { ...(ngDevMode ? { debugName: "filterDelay" } : /* istanbul ignore next */ {}), transform: numberAttribute });
    /**
     * Mode for filtering valid values are "lenient" and "strict". Default is lenient.
     * @group Props
     */
    filterMode = input('lenient', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "filterMode" }] : /* istanbul ignore next */ []));
    /**
     * Locale to use in filtering. The default locale is the host environment's current locale.
     * @group Props
     */
    filterLocale = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "filterLocale" }] : /* istanbul ignore next */ []));
    /**
     * Locale to be used in paginator formatting.
     * @group Props
     */
    paginatorLocale = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "paginatorLocale" }] : /* istanbul ignore next */ []));
    /**
     * Number of total records, defaults to length of value when not defined.
     * @group Props
     */
    totalRecordsInput = input(0, { ...(ngDevMode ? { debugName: "totalRecordsInput" } : /* istanbul ignore next */ {}), alias: 'totalRecords' });
    /**
     * Name of the field to sort data by default.
     * @group Props
     */
    sortFieldInput = input(undefined, { ...(ngDevMode ? { debugName: "sortFieldInput" } : /* istanbul ignore next */ {}), alias: 'sortField' });
    /**
     * Order to sort when default sorting is enabled.
     * @defaultValue 1
     * @group Props
     */
    sortOrderInput = input(1, { ...(ngDevMode ? { debugName: "sortOrderInput" } : /* istanbul ignore next */ {}), alias: 'sortOrder' });
    /**
     * An array of SortMeta objects to sort the data by default in multiple sort mode.
     * @defaultValue null
     * @group Props
     */
    multiSortMetaInput = input(undefined, { ...(ngDevMode ? { debugName: "multiSortMetaInput" } : /* istanbul ignore next */ {}), alias: 'multiSortMeta' });
    /**
     * Selected row in single mode or an array of values in multiple mode.
     * @defaultValue null
     * @deprecated since v22, use `selectionKeys` instead for key-based selection state.
     * @group Props
     */
    selectionInput = input(undefined, { ...(ngDevMode ? { debugName: "selectionInput" } : /* istanbul ignore next */ {}), alias: 'selection' });
    /**
     * An array of objects to display.
     * @defaultValue null
     * @group Props
     */
    valueInput = input(undefined, { ...(ngDevMode ? { debugName: "valueInput" } : /* istanbul ignore next */ {}), alias: 'value' });
    /**
     * A map of keys to control the selection state.
     * @group Props
     */
    selectionKeys = input(undefined, /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "selectionKeys" }] : /* istanbul ignore next */ []));
    /**
     * A map of keys (by `dataKey` value) whose value is `true` for collapsed rows. Two-way bindable.
     * When provided it controls row expansion (a row is expanded unless its key maps to `true`); leave it
     * unset to keep driving expansion from each node's `expanded` flag (default, non-breaking).
     * @group Props
     */
    collapsedKeys = input(undefined, /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "collapsedKeys" }] : /* istanbul ignore next */ []));
    /**
     * Whether to show grid lines between cells.
     * @defaultValue false
     * @group Props
     */
    showGridlines = input(false, { ...(ngDevMode ? { debugName: "showGridlines" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    /**
     * Callback to invoke on selected node change.
     * @param {TreeTableNode} object - Node instance.
     * @group Emits
     */
    selectionChange = output();
    /**
     * Callback to invoke on context menu selection change.
     * @param {TreeTableNode} object - Node instance.
     * @group Emits
     */
    contextMenuSelectionChange = output();
    /**
     * Callback to invoke when data is filtered.
     * @param {TreeTableFilterEvent} event - Custom filter event.
     * @group Emits
     */
    onFilter = output();
    /**
     * Callback to invoke when a node is expanded.
     * @param {TreeTableNodeExpandEvent} event - Node expand event.
     * @group Emits
     */
    onNodeExpand = output();
    /**
     * Callback to invoke when a node is collapsed.
     * @param {TreeTableNodeCollapseEvent} event - Node collapse event.
     * @group Emits
     */
    onNodeCollapse = output();
    /**
     * Callback to invoke when pagination occurs.
     * @param {TreeTablePaginatorState} object - Paginator state.
     * @group Emits
     */
    onPage = output();
    /**
     * Callback to invoke when a column gets sorted.
     * @param {Object} Object - Sort data.
     * @group Emits
     */
    onSort = output();
    /**
     * Callback to invoke when paging, sorting or filtering happens in lazy mode.
     * @param {TreeTableLazyLoadEvent} event - Custom lazy load event.
     * @group Emits
     */
    onLazyLoad = output();
    /**
     * An event emitter to invoke on custom sorting, refer to sorting section for details.
     * @param {TreeTableSortEvent} event - Custom sort event.
     * @group Emits
     */
    sortFunction = output();
    /**
     * Callback to invoke when a column is resized.
     * @param {TreeTableColResizeEvent} event - Custom column resize event.
     * @group Emits
     */
    onColResize = output();
    /**
     * Callback to invoke when a column is reordered.
     * @param {TreeTableColumnReorderEvent} event - Custom column reorder.
     * @group Emits
     */
    onColReorder = output();
    /**
     * Callback to invoke when a node is selected.
     * @param {TreeTableNode} object - Node instance.
     * @group Emits
     */
    onNodeSelect = output();
    /**
     * Callback to invoke when a node is unselected.
     * @param {TreeTableNodeUnSelectEvent} event - Custom node unselect event.
     * @group Emits
     */
    onNodeUnselect = output();
    /**
     * Callback to invoke when a node is selected with right click.
     * @param {TreeTableContextMenuSelectEvent} event - Custom context menu select event.
     * @group Emits
     */
    onContextMenuSelect = output();
    /**
     * Callback to invoke when state of header checkbox changes.
     * @param {TreeTableHeaderCheckboxToggleEvent} event - Custom checkbox toggle event.
     * @group Emits
     */
    onHeaderCheckboxToggle = output();
    /**
     * Callback to invoke when a cell switches to edit mode.
     * @param {TreeTableEditEvent} event - Custom edit event.
     * @group Emits
     */
    onEditInit = output();
    /**
     * Callback to invoke when cell edit is completed.
     * @param {TreeTableEditEvent} event - Custom edit event.
     * @group Emits
     */
    onEditComplete = output();
    /**
     * Callback to invoke when cell edit is cancelled with escape key.
     * @param {TreeTableEditEvent} event - Custom edit event.
     * @group Emits
     */
    onEditCancel = output();
    /**
     * Callback to invoke when selectionKeys are changed.
     * @param {Object} object - updated value of the selectionKeys.
     * @group Emits
     */
    selectionKeysChange = output();
    /**
     * Callback to invoke when collapsedKeys are changed.
     * @param {Object} object - updated value of the collapsedKeys.
     * @group Emits
     */
    collapsedKeysChange = output();
    resizeHelperViewChild = viewChild('resizeHelper', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "resizeHelperViewChild" }] : /* istanbul ignore next */ []));
    reorderIndicatorUpViewChild = viewChild('reorderIndicatorUp', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "reorderIndicatorUpViewChild" }] : /* istanbul ignore next */ []));
    reorderIndicatorDownViewChild = viewChild('reorderIndicatorDown', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "reorderIndicatorDownViewChild" }] : /* istanbul ignore next */ []));
    tableViewChild = viewChild('table', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "tableViewChild" }] : /* istanbul ignore next */ []));
    scrollableViewChild = viewChild('scrollableView', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "scrollableViewChild" }] : /* istanbul ignore next */ []));
    scrollableFrozenViewChild = viewChild('scrollableFrozenView', /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "scrollableFrozenViewChild" }] : /* istanbul ignore next */ []));
    /**
     * @group Templates
     */
    colGroupTemplate = contentChild('colgroup', { ...(ngDevMode ? { debugName: "colGroupTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    captionTemplate = contentChild('caption', { ...(ngDevMode ? { debugName: "captionTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    headerTemplate = contentChild('header', { ...(ngDevMode ? { debugName: "headerTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    bodyTemplate = contentChild('body', { ...(ngDevMode ? { debugName: "bodyTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    footerTemplate = contentChild('footer', { ...(ngDevMode ? { debugName: "footerTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    summaryTemplate = contentChild('summary', { ...(ngDevMode ? { debugName: "summaryTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    emptyMessageTemplate = contentChild('emptymessage', { ...(ngDevMode ? { debugName: "emptyMessageTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    paginatorLeftTemplate = contentChild('paginatorleft', { ...(ngDevMode ? { debugName: "paginatorLeftTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    paginatorRightTemplate = contentChild('paginatorright', { ...(ngDevMode ? { debugName: "paginatorRightTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    paginatorDropdownItemTemplate = contentChild('paginatordropdownitem', { ...(ngDevMode ? { debugName: "paginatorDropdownItemTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    frozenHeaderTemplate = contentChild('frozenheader', { ...(ngDevMode ? { debugName: "frozenHeaderTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    frozenBodyTemplate = contentChild('frozenbody', { ...(ngDevMode ? { debugName: "frozenBodyTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    frozenFooterTemplate = contentChild('frozenfooter', { ...(ngDevMode ? { debugName: "frozenFooterTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    frozenColGroupTemplate = contentChild('frozencolgroup', { ...(ngDevMode ? { debugName: "frozenColGroupTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    loadingIconTemplate = contentChild('loadingicon', { ...(ngDevMode ? { debugName: "loadingIconTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    reorderIndicatorUpIconTemplate = contentChild('reorderindicatorupicon', { ...(ngDevMode ? { debugName: "reorderIndicatorUpIconTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    reorderIndicatorDownIconTemplate = contentChild('reorderindicatordownicon', { ...(ngDevMode ? { debugName: "reorderIndicatorDownIconTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    sortIconTemplate = contentChild('sorticon', { ...(ngDevMode ? { debugName: "sortIconTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    checkboxIconTemplate = contentChild('checkboxicon', { ...(ngDevMode ? { debugName: "checkboxIconTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    headerCheckboxIconTemplate = contentChild('headercheckboxicon', { ...(ngDevMode ? { debugName: "headerCheckboxIconTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    togglerIconTemplate = contentChild('togglericon', { ...(ngDevMode ? { debugName: "togglerIconTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    paginatorFirstPageLinkIconTemplate = contentChild('paginatorfirstpagelinkicon', { ...(ngDevMode ? { debugName: "paginatorFirstPageLinkIconTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    paginatorLastPageLinkIconTemplate = contentChild('paginatorlastpagelinkicon', { ...(ngDevMode ? { debugName: "paginatorLastPageLinkIconTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    paginatorPreviousPageLinkIconTemplate = contentChild('paginatorpreviouspagelinkicon', { ...(ngDevMode ? { debugName: "paginatorPreviousPageLinkIconTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    paginatorNextPageLinkIconTemplate = contentChild('paginatornextpagelinkicon', { ...(ngDevMode ? { debugName: "paginatorNextPageLinkIconTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    /**
     * @group Templates
     */
    loaderTemplate = contentChild('loader', { ...(ngDevMode ? { debugName: "loaderTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    showLoadingMask = computed(() => this.loading() && this.showLoader(), /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "showLoadingMask" }] : /* istanbul ignore next */ []));
    showTopPaginator = computed(() => this.paginator() && (this.paginatorPosition() === 'top' || this.paginatorPosition() === 'both'), /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "showTopPaginator" }] : /* istanbul ignore next */ []));
    showBottomPaginator = computed(() => this.paginator() && (this.paginatorPosition() === 'bottom' || this.paginatorPosition() === 'both'), /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "showBottomPaginator" }] : /* istanbul ignore next */ []));
    dataP = computed(() => this.cn({
        scrollable: this.scrollable(),
        'flex-scrollable': this.scrollable() && this.scrollHeight() === 'flex',
        loading: this.loading(),
        empty: this.isEmpty()
    }), /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "dataP" }] : /* istanbul ignore next */ []));
    lastResizerHelperX;
    reorderIconWidth;
    reorderIconHeight;
    draggedColumn;
    dropPosition;
    preventSelectionSetterPropagation;
    _selection;
    value = [];
    totalRecords = 0;
    _sortField;
    _sortOrder = 1;
    _multiSortMeta;
    _selectionKeys;
    _collapsedKeys;
    first = 0;
    filters = {};
    selectedKeys = {};
    rowTouched;
    _editingCell = signal(null, /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "_editingCell" }] : /* istanbul ignore next */ []));
    get editingCell() {
        return this._editingCell();
    }
    set editingCell(value) {
        this._editingCell.set(value);
    }
    editingCellData;
    editingCellField;
    editingCellClick;
    documentEditListener;
    initialized;
    toggleRowIndex;
    serializedValue;
    filteredNodes;
    filterTimeout;
    filterService = inject(FilterService);
    tableService = inject(TreeTableService);
    constructor() {
        super();
        // value effect
        effect(() => {
            const val = this.valueInput();
            untracked(() => {
                if (val !== undefined) {
                    this.value = val;
                    if (!this.lazy()) {
                        this.totalRecords = this.value ? this.value.length : 0;
                        if (this.sortMode() == 'single' && this._sortField)
                            this.sortSingle();
                        else if (this.sortMode() == 'multiple' && this._multiSortMeta)
                            this.sortMultiple();
                        else if (this.hasFilter())
                            this._filter();
                    }
                    this.updateSerializedValue();
                    this.tableService.onUIUpdate(this.value);
                }
            });
        });
        // sortField effect
        effect(() => {
            const val = this.sortFieldInput();
            untracked(() => {
                if (val !== undefined) {
                    this._sortField = val;
                    if (!this.lazy() || this.initialized) {
                        if (this.sortMode() === 'single') {
                            this.sortSingle();
                        }
                    }
                }
            });
        });
        // sortOrder effect
        effect(() => {
            const val = this.sortOrderInput();
            untracked(() => {
                this._sortOrder = val;
                if (!this.lazy() || this.initialized) {
                    if (this.sortMode() === 'single') {
                        this.sortSingle();
                    }
                }
            });
        });
        // multiSortMeta effect
        effect(() => {
            const val = this.multiSortMetaInput();
            untracked(() => {
                if (val !== undefined) {
                    this._multiSortMeta = val;
                    if (this.sortMode() === 'multiple') {
                        this.sortMultiple();
                    }
                }
            });
        });
        // selection effect
        effect(() => {
            const val = this.selectionInput();
            untracked(() => {
                if (val !== undefined) {
                    this._selection = val;
                    if (!this.preventSelectionSetterPropagation) {
                        this.updateselectedKeys();
                        this.tableService.onSelectionChange();
                    }
                    this.preventSelectionSetterPropagation = false;
                }
            });
        });
        // totalRecords effect
        effect(() => {
            const val = this.totalRecordsInput();
            untracked(() => {
                this.totalRecords = val;
                this.tableService.onTotalRecordsChange(this.totalRecords);
            });
        });
        // selectionKeys effect
        effect(() => {
            const val = this.selectionKeys();
            untracked(() => {
                if (val !== undefined) {
                    this._selectionKeys = val;
                    this.selectionKeysChange.emit(this._selectionKeys);
                }
            });
        });
        // collapsedKeys effect: project the map onto each node's `expanded` flag so the existing
        // expansion engine keeps working unchanged (non-breaking when collapsedKeys is not provided).
        effect(() => {
            const val = this.collapsedKeys();
            untracked(() => {
                if (val !== undefined) {
                    this._collapsedKeys = val;
                    this.applyCollapsedKeys();
                    this.updateSerializedValue();
                    this.tableService.onUIUpdate(this.value);
                    this.collapsedKeysChange.emit(this._collapsedKeys);
                }
            });
        });
        // first effect
        effect(() => {
            const val = this.firstInput();
            untracked(() => {
                this.first = val;
            });
        });
        // filters effect
        effect(() => {
            const val = this.filtersInput();
            untracked(() => {
                this.filters = val;
            });
        });
    }
    onInit() {
        if (this.lazy() && this.lazyLoadOnInit() && !this.virtualScroll()) {
            this.onLazyLoad.emit(this.createLazyLoadMetadata());
        }
        this.initialized = true;
    }
    updateSerializedValue() {
        this.serializedValue = [];
        if (this.paginator())
            this.serializePageNodes();
        else
            this.serializeNodes(null, this.filteredNodes || this.value, 0, true);
    }
    serializeNodes(parent, nodes, level, visible) {
        if (nodes && nodes.length) {
            for (let node of nodes) {
                node.parent = parent;
                const rowNode = {
                    node: node,
                    parent: parent,
                    level: level,
                    visible: visible && (parent ? parent.expanded : true)
                };
                this.serializedValue.push(rowNode);
                if (rowNode.visible && node.expanded) {
                    this.serializeNodes(node, node.children, level + 1, rowNode.visible);
                }
            }
        }
    }
    serializePageNodes() {
        let data = this.filteredNodes || this.value;
        this.serializedValue = [];
        if (data && data.length) {
            const first = this.lazy() ? 0 : this.first;
            for (let i = first; i < first + this.rows(); i++) {
                let node = data[i];
                if (node) {
                    this.serializedValue.push({
                        node: node,
                        parent: null,
                        level: 0,
                        visible: true
                    });
                    this.serializeNodes(node, node.children, 1, true);
                }
            }
        }
    }
    updateselectedKeys() {
        if (this.dataKey() && this._selection) {
            this.selectedKeys = {};
            if (Array.isArray(this._selection)) {
                for (let node of this._selection) {
                    this.selectedKeys[String(resolveFieldData(node.data, this.dataKey()))] = 1;
                }
            }
            else {
                this.selectedKeys[String(resolveFieldData(this._selection.data, this.dataKey()))] = 1;
            }
        }
    }
    onPageChange(event) {
        this.first = event.first;
        this.rows.set(event.rows);
        if (this.lazy())
            this.onLazyLoad.emit(this.createLazyLoadMetadata());
        else
            this.serializePageNodes();
        this.onPage.emit({
            first: this.first,
            rows: event.rows
        });
        this.tableService.onUIUpdate(this.value);
        if (this.scrollable()) {
            this.resetScrollTop();
        }
    }
    sort(event) {
        let originalEvent = event.originalEvent;
        if (this.sortMode() === 'single') {
            this._sortOrder = this._sortField === event.field ? this._sortOrder * -1 : this.defaultSortOrder();
            this._sortField = event.field;
            this.sortSingle();
            if (this.resetPageOnSort() && this.scrollable()) {
                this.resetScrollTop();
            }
        }
        if (this.sortMode() === 'multiple') {
            let metaKey = originalEvent.metaKey || originalEvent.ctrlKey;
            let sortMeta = this.getSortMeta(event.field);
            if (sortMeta) {
                if (!metaKey) {
                    this._multiSortMeta = [{ field: event.field, order: sortMeta.order * -1 }];
                    if (this.resetPageOnSort() && this.scrollable()) {
                        this.resetScrollTop();
                    }
                }
                else {
                    sortMeta.order = sortMeta.order * -1;
                }
            }
            else {
                if (!metaKey || !this._multiSortMeta) {
                    this._multiSortMeta = [];
                    if (this.resetPageOnSort() && this.scrollable()) {
                        this.resetScrollTop();
                    }
                }
                this._multiSortMeta.push({ field: event.field, order: this.defaultSortOrder() });
            }
            this.sortMultiple();
        }
    }
    sortSingle() {
        if (this._sortField && this._sortOrder) {
            if (this.lazy()) {
                this.onLazyLoad.emit(this.createLazyLoadMetadata());
            }
            else if (this.value) {
                this.sortNodes(this.value);
                if (this.hasFilter()) {
                    this._filter();
                }
            }
            let sortMeta = {
                field: this._sortField,
                order: this._sortOrder
            };
            this.onSort.emit(sortMeta);
            this.tableService.onSort(sortMeta);
            this.updateSerializedValue();
        }
    }
    sortNodes(nodes) {
        if (!nodes || nodes.length === 0) {
            return;
        }
        if (this.customSort()) {
            this.sortFunction.emit({
                data: nodes,
                mode: this.sortMode(),
                field: this._sortField,
                order: this._sortOrder
            });
        }
        else {
            nodes.sort((node1, node2) => {
                let value1 = resolveFieldData(node1.data, this._sortField);
                let value2 = resolveFieldData(node2.data, this._sortField);
                let result = 0;
                if (value1 == null && value2 != null)
                    result = -1;
                else if (value1 != null && value2 == null)
                    result = 1;
                else if (value1 == null && value2 == null)
                    result = 0;
                else if (typeof value1 === 'string' && typeof value2 === 'string')
                    result = value1.localeCompare(value2, undefined, { numeric: true });
                else
                    result = value1 < value2 ? -1 : value1 > value2 ? 1 : 0;
                return this._sortOrder * result;
            });
        }
        for (let node of nodes) {
            this.sortNodes(node.children);
        }
    }
    sortMultiple() {
        if (this._multiSortMeta) {
            if (this.lazy()) {
                this.onLazyLoad.emit(this.createLazyLoadMetadata());
            }
            else if (this.value) {
                this.sortMultipleNodes(this.value);
                if (this.hasFilter()) {
                    this._filter();
                }
            }
            this.onSort.emit({
                multisortmeta: this._multiSortMeta
            });
            this.updateSerializedValue();
            this.tableService.onSort(this._multiSortMeta);
        }
    }
    sortMultipleNodes(nodes) {
        if (!nodes || nodes.length === 0) {
            return;
        }
        if (this.customSort()) {
            this.sortFunction.emit({
                data: this.value,
                mode: this.sortMode(),
                multiSortMeta: this._multiSortMeta
            });
        }
        else {
            nodes.sort((node1, node2) => {
                return this.multisortField(node1, node2, this._multiSortMeta, 0);
            });
        }
        for (let node of nodes) {
            this.sortMultipleNodes(node.children);
        }
    }
    multisortField(node1, node2, multiSortMeta, index) {
        if (isEmpty(this._multiSortMeta) || isEmpty(multiSortMeta[index])) {
            return 0;
        }
        let value1 = resolveFieldData(node1.data, multiSortMeta[index].field);
        let value2 = resolveFieldData(node2.data, multiSortMeta[index].field);
        let result = 0;
        if (value1 == null && value2 != null)
            result = -1;
        else if (value1 != null && value2 == null)
            result = 1;
        else if (value1 == null && value2 == null)
            result = 0;
        if (typeof value1 == 'string' || value1 instanceof String) {
            if (value1.localeCompare && value1 != value2) {
                return multiSortMeta[index].order * value1.localeCompare(value2, undefined, { numeric: true });
            }
        }
        else {
            result = value1 < value2 ? -1 : 1;
        }
        if (value1 == value2) {
            return multiSortMeta.length - 1 > index ? this.multisortField(node1, node2, multiSortMeta, index + 1) : 0;
        }
        return multiSortMeta[index].order * result;
    }
    getSortMeta(field) {
        if (this._multiSortMeta && this._multiSortMeta.length) {
            for (let i = 0; i < this._multiSortMeta.length; i++) {
                if (this._multiSortMeta[i].field === field) {
                    return this._multiSortMeta[i];
                }
            }
        }
        return null;
    }
    isSorted(field) {
        if (this.sortMode() === 'single') {
            return this._sortField && this._sortField === field;
        }
        else if (this.sortMode() === 'multiple') {
            let sorted = false;
            if (this._multiSortMeta) {
                for (let i = 0; i < this._multiSortMeta.length; i++) {
                    if (this._multiSortMeta[i].field == field) {
                        sorted = true;
                        break;
                    }
                }
            }
            return sorted;
        }
    }
    createLazyLoadMetadata() {
        return {
            first: this.first,
            rows: this.rows(),
            sortField: this._sortField,
            sortOrder: this._sortOrder,
            filters: this.filters,
            globalFilter: this.filters && this.filters['global'] ? this.filters['global'].value : null,
            multiSortMeta: this._multiSortMeta,
            forceUpdate: () => this.cd.detectChanges()
        };
    }
    onLazyItemLoad(event) {
        this.onLazyLoad.emit({
            ...this.createLazyLoadMetadata(),
            ...event,
            rows: event.last - event.first
        });
    }
    /**
     * Resets scroll to top.
     * @group Method
     */
    resetScrollTop() {
        if (this.virtualScroll())
            this.scrollToVirtualIndex(0);
        else
            this.scrollTo({ top: 0 });
    }
    /**
     * Scrolls to given index when using virtual scroll.
     * @param {number} index - index of the element.
     * @group Method
     */
    scrollToVirtualIndex(index) {
        if (this.scrollableViewChild()) {
            this.scrollableViewChild().scrollToVirtualIndex(index);
        }
        if (this.scrollableFrozenViewChild()) {
            this.scrollableViewChild().scrollToVirtualIndex(index);
        }
    }
    /**
     * Scrolls to given index.
     * @param {ScrollToOptions} options - Scroll options.
     * @group Method
     */
    scrollTo(options) {
        if (this.scrollableViewChild()) {
            this.scrollableViewChild().scrollTo(options);
        }
        if (this.scrollableFrozenViewChild()) {
            this.scrollableViewChild().scrollTo(options);
        }
    }
    isEmpty() {
        let data = this.filteredNodes || this.value;
        return data == null || data.length == 0;
    }
    getBlockableElement() {
        return this.el.nativeElement.children[0];
    }
    onColumnResizeBegin(event) {
        let containerLeft = getOffset(this.el?.nativeElement).left;
        this.lastResizerHelperX = event.pageX - containerLeft + this.el?.nativeElement.scrollLeft;
        event.preventDefault();
    }
    onColumnResize(event) {
        let containerLeft = getOffset(this.el?.nativeElement).left;
        this.el?.nativeElement.setAttribute('data-p-unselectable-text', 'true');
        !this.$unstyled() && addStyle(this.el.nativeElement, { 'user-select': 'none' });
        this.resizeHelperViewChild().nativeElement.style.height = this.el?.nativeElement.offsetHeight + 'px';
        this.resizeHelperViewChild().nativeElement.style.top = 0 + 'px';
        this.resizeHelperViewChild().nativeElement.style.left = event.pageX - containerLeft + this.el?.nativeElement.scrollLeft + 'px';
        this.resizeHelperViewChild().nativeElement.style.display = 'block';
    }
    onColumnResizeEnd(event, column) {
        let delta = this.resizeHelperViewChild().nativeElement.offsetLeft - this.lastResizerHelperX;
        let columnWidth = column.offsetWidth;
        let newColumnWidth = columnWidth + delta;
        let minWidth = column.style.minWidth || 15;
        if (columnWidth + delta > parseInt(minWidth)) {
            if (this.columnResizeMode() === 'fit') {
                let nextColumn = column.nextElementSibling;
                while (!nextColumn.offsetParent) {
                    nextColumn = nextColumn.nextElementSibling;
                }
                if (nextColumn) {
                    let nextColumnWidth = nextColumn.offsetWidth - delta;
                    let nextColumnMinWidth = nextColumn.style.minWidth || 15;
                    if (newColumnWidth > 15 && nextColumnWidth > parseInt(nextColumnMinWidth)) {
                        if (this.scrollable()) {
                            let scrollableView = this.findParentScrollableView(column);
                            let scrollableBodyTable = findSingle(scrollableView, '[data-pc-section="scrollablebody"] table') || findSingle(scrollableView, '[data-pc-name="virtualscroller"] table');
                            let scrollableHeaderTable = findSingle(scrollableView, '[data-pc-section="scrollableheadertable"]');
                            let scrollableFooterTable = findSingle(scrollableView, '[data-pc-section="scrollablefootertable"]');
                            let resizeColumnIndex = getIndex(column);
                            this.resizeColGroup(scrollableHeaderTable, resizeColumnIndex, newColumnWidth, nextColumnWidth);
                            this.resizeColGroup(scrollableBodyTable, resizeColumnIndex, newColumnWidth, nextColumnWidth);
                            this.resizeColGroup(scrollableFooterTable, resizeColumnIndex, newColumnWidth, nextColumnWidth);
                        }
                        else {
                            column.style.width = newColumnWidth + 'px';
                            if (nextColumn) {
                                nextColumn.style.width = nextColumnWidth + 'px';
                            }
                        }
                    }
                }
            }
            else if (this.columnResizeMode() === 'expand') {
                if (this.scrollable()) {
                    let scrollableView = this.findParentScrollableView(column);
                    let scrollableBody = findSingle(scrollableView, '[data-pc-section="scrollablebody"]') || findSingle(scrollableView, '[data-pc-name="virtualscroller"]');
                    let scrollableHeader = findSingle(scrollableView, '[data-pc-section="scrollableheader"]');
                    let scrollableFooter = findSingle(scrollableView, '[data-pc-section="scrollablefooter"]');
                    let scrollableBodyTable = findSingle(scrollableView, '[data-pc-section="scrollablebody"] table') || findSingle(scrollableView, '[data-pc-name="virtualscroller"] table');
                    let scrollableHeaderTable = findSingle(scrollableView, '[data-pc-section="scrollableheadertable"]');
                    let scrollableFooterTable = findSingle(scrollableView, '[data-pc-section="scrollablefootertable"]');
                    scrollableBodyTable.style.width = scrollableBodyTable.offsetWidth + delta + 'px';
                    scrollableHeaderTable.style.width = scrollableHeaderTable.offsetWidth + delta + 'px';
                    if (scrollableFooterTable) {
                        scrollableFooterTable.style.width = scrollableFooterTable.offsetWidth + delta + 'px';
                    }
                    let resizeColumnIndex = getIndex(column);
                    const scrollableBodyTableWidth = column ? scrollableBodyTable.offsetWidth + delta : newColumnWidth;
                    const scrollableHeaderTableWidth = column ? scrollableHeaderTable.offsetWidth + delta : newColumnWidth;
                    const isContainerInViewport = this.el?.nativeElement.offsetWidth >= scrollableBodyTableWidth;
                    let setWidth = (container, table, width, isContainerInViewport) => {
                        if (container && table) {
                            container.style.width = isContainerInViewport ? width + calculateScrollbarWidth(scrollableBody) + 'px' : 'auto';
                            table.style.width = width + 'px';
                        }
                    };
                    setWidth(scrollableBody, scrollableBodyTable, scrollableBodyTableWidth, isContainerInViewport);
                    setWidth(scrollableHeader, scrollableHeaderTable, scrollableHeaderTableWidth, isContainerInViewport);
                    setWidth(scrollableFooter, scrollableFooterTable, scrollableHeaderTableWidth, isContainerInViewport);
                    this.resizeColGroup(scrollableHeaderTable, resizeColumnIndex, newColumnWidth, null);
                    this.resizeColGroup(scrollableBodyTable, resizeColumnIndex, newColumnWidth, null);
                    this.resizeColGroup(scrollableFooterTable, resizeColumnIndex, newColumnWidth, null);
                }
                else {
                    this.tableViewChild().nativeElement.style.width = this.tableViewChild()?.nativeElement.offsetWidth + delta + 'px';
                    column.style.width = newColumnWidth + 'px';
                    let containerWidth = this.tableViewChild()?.nativeElement.style.width;
                    this.el.nativeElement.style.width = containerWidth + 'px';
                }
            }
            this.onColResize.emit({
                element: column,
                delta: delta
            });
        }
        this.resizeHelperViewChild().nativeElement.style.display = 'none';
        this.el.nativeElement.removeAttribute('data-p-unselectable-text');
        !this.$unstyled() && (this.el.nativeElement.style['user-select'] = '');
    }
    findParentScrollableView(column) {
        if (column) {
            let parent = column.parentElement;
            while (parent && !findSingle(parent, '[data-pc-section="scrollableview"]')) {
                parent = parent.parentElement;
            }
            return parent;
        }
        else {
            return null;
        }
    }
    resizeColGroup(table, resizeColumnIndex, newColumnWidth, nextColumnWidth) {
        if (table) {
            let colGroup = table.children[0].nodeName === 'COLGROUP' ? table.children[0] : null;
            if (colGroup) {
                let col = colGroup.children[resizeColumnIndex];
                let nextCol = col.nextElementSibling;
                col.style.width = newColumnWidth + 'px';
                if (nextCol && nextColumnWidth) {
                    nextCol.style.width = nextColumnWidth + 'px';
                }
            }
            else {
                throw 'Scrollable tables require a colgroup to support resizable columns';
            }
        }
    }
    onColumnDragStart(event, columnElement) {
        this.reorderIconWidth = getHiddenElementOuterWidth(this.reorderIndicatorUpViewChild()?.nativeElement);
        this.reorderIconHeight = getHiddenElementOuterHeight(this.reorderIndicatorDownViewChild()?.nativeElement);
        this.draggedColumn = columnElement;
        event.dataTransfer.setData('text', 'b'); // For firefox
    }
    onColumnDragEnter(event, dropHeader) {
        if (this.reorderableColumns() && this.draggedColumn && dropHeader) {
            event.preventDefault();
            let containerOffset = getOffset(this.el?.nativeElement);
            let dropHeaderOffset = getOffset(dropHeader);
            if (this.draggedColumn != dropHeader) {
                let targetLeft = dropHeaderOffset.left - containerOffset.left;
                let targetTop = containerOffset.top - dropHeaderOffset.top;
                let columnCenter = dropHeaderOffset.left + dropHeader.offsetWidth / 2;
                this.reorderIndicatorUpViewChild().nativeElement.style.top = dropHeaderOffset.top - containerOffset.top - (this.reorderIconHeight - 1) + 'px';
                this.reorderIndicatorDownViewChild().nativeElement.style.top = dropHeaderOffset.top - containerOffset.top + dropHeader.offsetHeight + 'px';
                if (event.pageX > columnCenter) {
                    this.reorderIndicatorUpViewChild().nativeElement.style.left = targetLeft + dropHeader.offsetWidth - Math.ceil(this.reorderIconWidth / 2) + 'px';
                    this.reorderIndicatorDownViewChild().nativeElement.style.left = targetLeft + dropHeader.offsetWidth - Math.ceil(this.reorderIconWidth / 2) + 'px';
                    this.dropPosition = 1;
                }
                else {
                    this.reorderIndicatorUpViewChild().nativeElement.style.left = targetLeft - Math.ceil(this.reorderIconWidth / 2) + 'px';
                    this.reorderIndicatorDownViewChild().nativeElement.style.left = targetLeft - Math.ceil(this.reorderIconWidth / 2) + 'px';
                    this.dropPosition = -1;
                }
                this.reorderIndicatorUpViewChild().nativeElement.style.display = 'block';
                this.reorderIndicatorDownViewChild().nativeElement.style.display = 'block';
            }
            else {
                event.dataTransfer.dropEffect = 'none';
            }
        }
    }
    onColumnDragLeave(event) {
        if (this.reorderableColumns() && this.draggedColumn) {
            event.preventDefault();
            this.reorderIndicatorUpViewChild().nativeElement.style.display = 'none';
            this.reorderIndicatorDownViewChild().nativeElement.style.display = 'none';
        }
    }
    onColumnDrop(event, dropColumn) {
        event.preventDefault();
        if (this.draggedColumn) {
            let dragIndex = DomHandler.indexWithinGroup(this.draggedColumn, 'ttreorderablecolumn');
            let dropIndex = DomHandler.indexWithinGroup(dropColumn, 'ttreorderablecolumn');
            let allowDrop = dragIndex != dropIndex;
            if (allowDrop && ((dropIndex - dragIndex == 1 && this.dropPosition === -1) || (dragIndex - dropIndex == 1 && this.dropPosition === 1))) {
                allowDrop = false;
            }
            if (allowDrop && dropIndex < dragIndex && this.dropPosition === 1) {
                dropIndex = dropIndex + 1;
            }
            if (allowDrop && dropIndex > dragIndex && this.dropPosition === -1) {
                dropIndex = dropIndex - 1;
            }
            if (allowDrop) {
                reorderArray(this.columns(), dragIndex, dropIndex);
                this.onColReorder.emit({
                    dragIndex: dragIndex,
                    dropIndex: dropIndex,
                    columns: this.columns()
                });
            }
            this.reorderIndicatorUpViewChild().nativeElement.style.display = 'none';
            this.reorderIndicatorDownViewChild().nativeElement.style.display = 'none';
            this.draggedColumn.draggable = false;
            this.draggedColumn = null;
            this.dropPosition = null;
        }
    }
    handleRowClick(event) {
        let targetNode = event.originalEvent.target.nodeName;
        if (targetNode == 'INPUT' || targetNode == 'BUTTON' || targetNode == 'A' || isClickable(event.originalEvent.target)) {
            return;
        }
        if (this.selectionMode()) {
            this.preventSelectionSetterPropagation = true;
            let rowNode = event.rowNode;
            let selected = this.isSelected(rowNode.node);
            let metaSelection = this.rowTouched ? false : this.metaKeySelection();
            let dataKeyValue = this.dataKey() ? String(resolveFieldData(rowNode.node.data, this.dataKey())) : null;
            if (metaSelection) {
                let keyboardEvent = event.originalEvent;
                let metaKey = keyboardEvent.metaKey || keyboardEvent.ctrlKey;
                if (selected && metaKey) {
                    if (this.isSingleSelectionMode()) {
                        this._selection = null;
                        this.selectedKeys = {};
                        this.selectionChange.emit(null);
                    }
                    else {
                        let selectionIndex = this.findIndexInSelection(rowNode.node);
                        this._selection = this._selection.filter((val, i) => i != selectionIndex);
                        this.selectionChange.emit(this._selection);
                        if (dataKeyValue) {
                            delete this.selectedKeys[dataKeyValue];
                        }
                    }
                    this.onNodeUnselect.emit({
                        originalEvent: event.originalEvent,
                        node: rowNode.node,
                        type: 'row'
                    });
                }
                else {
                    if (this.isSingleSelectionMode()) {
                        this._selection = rowNode.node;
                        this.selectionChange.emit(rowNode.node);
                        if (dataKeyValue) {
                            this.selectedKeys = {};
                            this.selectedKeys[dataKeyValue] = 1;
                        }
                    }
                    else if (this.isMultipleSelectionMode()) {
                        if (metaKey) {
                            this._selection = this._selection || [];
                        }
                        else {
                            this._selection = [];
                            this.selectedKeys = {};
                        }
                        this._selection = [...this._selection, rowNode.node];
                        this.selectionChange.emit(this._selection);
                        if (dataKeyValue) {
                            this.selectedKeys[dataKeyValue] = 1;
                        }
                    }
                    this.onNodeSelect.emit({
                        originalEvent: event.originalEvent,
                        node: rowNode.node,
                        type: 'row',
                        index: event.rowIndex
                    });
                }
            }
            else {
                if (this.selectionMode() === 'single') {
                    if (selected) {
                        this._selection = null;
                        this.selectedKeys = {};
                        this.selectionChange.emit(this._selection);
                        this.onNodeUnselect.emit({
                            originalEvent: event.originalEvent,
                            node: rowNode.node,
                            type: 'row'
                        });
                    }
                    else {
                        this._selection = rowNode.node;
                        this.selectionChange.emit(this._selection);
                        this.onNodeSelect.emit({
                            originalEvent: event.originalEvent,
                            node: rowNode.node,
                            type: 'row',
                            index: event.rowIndex
                        });
                        if (dataKeyValue) {
                            this.selectedKeys = {};
                            this.selectedKeys[dataKeyValue] = 1;
                        }
                    }
                }
                else if (this.selectionMode() === 'multiple') {
                    if (selected) {
                        let selectionIndex = this.findIndexInSelection(rowNode.node);
                        this._selection = this._selection.filter((val, i) => i != selectionIndex);
                        this.selectionChange.emit(this._selection);
                        this.onNodeUnselect.emit({
                            originalEvent: event.originalEvent,
                            node: rowNode.node,
                            type: 'row'
                        });
                        if (dataKeyValue) {
                            delete this.selectedKeys[dataKeyValue];
                        }
                    }
                    else {
                        this._selection = this._selection ? [...this._selection, rowNode.node] : [rowNode.node];
                        this.selectionChange.emit(this._selection);
                        this.onNodeSelect.emit({
                            originalEvent: event.originalEvent,
                            node: rowNode.node,
                            type: 'row',
                            index: event.rowIndex
                        });
                        if (dataKeyValue) {
                            this.selectedKeys[dataKeyValue] = 1;
                        }
                    }
                }
            }
            this.tableService.onSelectionChange();
        }
        this.rowTouched = false;
    }
    handleRowTouchEnd(event) {
        this.rowTouched = true;
    }
    handleRowRightClick(event) {
        const ctxMenu = this.contextMenu();
        if (ctxMenu) {
            const node = event.rowNode.node;
            const showContextMenu = () => {
                ctxMenu.show(event.originalEvent);
                ctxMenu.hideCallback = () => {
                    this.contextMenuSelectionChange.emit(null);
                    this.tableService.onContextMenu(null);
                };
            };
            this.contextMenuSelectionChange.emit(node);
            this.tableService.onContextMenu(node);
            showContextMenu();
            this.onContextMenuSelect.emit({ originalEvent: event.originalEvent, node: node });
        }
    }
    toggleNodeWithCheckbox(event) {
        // legacy selection support, will be removed in v18
        this._selection = this._selection || [];
        this.preventSelectionSetterPropagation = true;
        let node = event.rowNode.node;
        let selected = this.isSelected(node);
        if (selected) {
            this.propagateSelectionDown(node, false);
            if (event.rowNode.parent) {
                this.propagateSelectionUp(node.parent, false);
            }
            this.selectionChange.emit(this._selection);
            this.onNodeUnselect.emit({ originalEvent: event, node: node });
        }
        else {
            this.propagateSelectionDown(node, true);
            if (event.rowNode.parent) {
                this.propagateSelectionUp(node.parent, true);
            }
            this.selectionChange.emit(this._selection);
            this.onNodeSelect.emit({ originalEvent: event, node: node });
        }
        this.tableService.onSelectionChange();
    }
    toggleNodesWithCheckbox(event, check) {
        // legacy selection support, will be removed in v18
        let data = this.filteredNodes || this.value;
        this._selection = check && data ? data.slice() : [];
        this.toggleAll(check);
        if (!check) {
            this._selection = [];
            this.selectedKeys = {};
        }
        this.preventSelectionSetterPropagation = true;
        this.selectionChange.emit(this._selection);
        this.tableService.onSelectionChange();
        this.onHeaderCheckboxToggle.emit({ originalEvent: event, checked: check });
    }
    toggleAll(checked) {
        let data = this.filteredNodes || this.value;
        if (!this._selectionKeys) {
            if (data && data.length) {
                for (let node of data) {
                    this.propagateSelectionDown(node, checked);
                }
            }
        }
        else {
            // legacy selection support, will be removed in v18
            if (data && data.length) {
                for (let node of data) {
                    this.propagateDown(node, checked);
                }
                this.selectionKeysChange.emit(this._selectionKeys);
            }
        }
    }
    propagateSelectionUp(node, select) {
        // legacy selection support, will be removed in v18
        if (node.children && node.children.length) {
            let selectedChildCount = 0;
            let childPartialSelected = false;
            let dataKeyValue = this.dataKey() ? String(resolveFieldData(node.data, this.dataKey())) : null;
            for (let child of node.children) {
                if (this.isSelected(child))
                    selectedChildCount++;
                else if (child.partialSelected)
                    childPartialSelected = true;
            }
            if (select && selectedChildCount == node.children.length) {
                this._selection = [...(this._selection || []), node];
                node.partialSelected = false;
                if (dataKeyValue) {
                    this.selectedKeys[dataKeyValue] = 1;
                }
            }
            else {
                if (!select) {
                    let index = this.findIndexInSelection(node);
                    if (index >= 0) {
                        this._selection = this._selection.filter((val, i) => i != index);
                        if (dataKeyValue) {
                            delete this.selectedKeys[dataKeyValue];
                        }
                    }
                }
                if (childPartialSelected || (selectedChildCount > 0 && selectedChildCount != node.children.length))
                    node.partialSelected = true;
                else
                    node.partialSelected = false;
            }
        }
        let parent = node.parent;
        node.checked = select;
        if (parent) {
            this.propagateSelectionUp(parent, select);
        }
    }
    propagateSelectionDown(node, select) {
        // legacy selection support, will be removed in v18
        let index = this.findIndexInSelection(node);
        let dataKeyValue = this.dataKey() ? String(resolveFieldData(node.data, this.dataKey())) : null;
        if (select && index == -1) {
            this._selection = [...(this._selection || []), node];
            if (dataKeyValue) {
                this.selectedKeys[dataKeyValue] = 1;
            }
        }
        else if (!select && index > -1) {
            this._selection = this._selection.filter((val, i) => i != index);
            if (dataKeyValue) {
                delete this.selectedKeys[dataKeyValue];
            }
        }
        node.partialSelected = false;
        node.checked = select;
        if (node.children && node.children.length) {
            for (let child of node.children) {
                this.propagateSelectionDown(child, select);
            }
        }
    }
    isSelected(node) {
        // legacy selection support, will be removed in v18
        if (node && this._selection) {
            if (this.dataKey()) {
                if (node.hasOwnProperty('checked')) {
                    return node['checked'];
                }
                else {
                    return this.selectedKeys[resolveFieldData(node.data, this.dataKey())] !== undefined;
                }
            }
            else {
                if (Array.isArray(this._selection))
                    return this.findIndexInSelection(node) > -1;
                else
                    return this.equals(node, this._selection);
            }
        }
        return false;
    }
    isNodeSelected(node) {
        return this.selectionMode() && this._selectionKeys ? this._selectionKeys[this.nodeKey(node)]?.checked === true : false;
    }
    isNodePartialSelected(node) {
        return this.selectionMode() && this._selectionKeys ? this._selectionKeys[this.nodeKey(node)]?.partialChecked === true : false;
    }
    nodeKey(node) {
        return resolveFieldData(node, this.dataKey()) || resolveFieldData(node?.data, this.dataKey());
    }
    /**
     * Projects the current `collapsedKeys` map onto each node's `expanded` flag (a row is expanded
     * unless its key maps to `true`).
     */
    applyCollapsedKeys() {
        if (this._collapsedKeys == null) {
            return;
        }
        const walk = (nodes) => {
            if (!nodes)
                return;
            for (const node of nodes) {
                const key = this.nodeKey(node);
                if (key != null) {
                    node.expanded = !this._collapsedKeys[key];
                }
                if (node.children) {
                    walk(node.children);
                }
            }
        };
        walk(this.value);
    }
    /**
     * Reflects a row's expansion change back into the two-way `collapsedKeys` map when it is in use.
     */
    setNodeCollapsed(node, collapsed) {
        if (this._collapsedKeys == null) {
            return;
        }
        const key = this.nodeKey(node);
        if (key == null) {
            return;
        }
        const next = { ...this._collapsedKeys };
        if (collapsed) {
            next[key] = true;
        }
        else {
            delete next[key];
        }
        this._collapsedKeys = next;
        this.collapsedKeysChange.emit(this._collapsedKeys);
    }
    toggleCheckbox(event) {
        let { rowNode, check, originalEvent } = event;
        let node = rowNode.node;
        if (this._selectionKeys) {
            this.propagateDown(node, check);
            if (node.parent) {
                this.propagateUp(node.parent, check);
            }
            this.selectionKeysChange.emit(this._selectionKeys);
        }
        else {
            this.toggleNodeWithCheckbox({ originalEvent, rowNode });
        }
        this.tableService.onSelectionChange();
    }
    propagateDown(node, check) {
        if (check) {
            this._selectionKeys[this.nodeKey(node)] = { checked: true, partialChecked: false };
        }
        else {
            delete this._selectionKeys[this.nodeKey(node)];
        }
        if (node.children && node.children.length) {
            for (let child of node.children) {
                this.propagateDown(child, check);
            }
        }
    }
    propagateUp(node, check) {
        let checkedChildCount = 0;
        let childPartialSelected = false;
        for (let child of node.children) {
            if (this._selectionKeys[this.nodeKey(child)] && this._selectionKeys[this.nodeKey(child)].checked)
                checkedChildCount++;
            else if (this._selectionKeys[this.nodeKey(child)] && this._selectionKeys[this.nodeKey(child)].partialChecked)
                childPartialSelected = true;
        }
        if (check && checkedChildCount === node.children.length) {
            this._selectionKeys[this.nodeKey(node)] = { checked: true, partialChecked: false };
        }
        else {
            if (!check) {
                delete this._selectionKeys[this.nodeKey(node)];
            }
            if (childPartialSelected || (checkedChildCount > 0 && checkedChildCount !== node.children.length))
                this._selectionKeys[this.nodeKey(node)] = { checked: false, partialChecked: true };
            else
                this._selectionKeys[this.nodeKey(node)] = { checked: false, partialChecked: false };
        }
        let parent = node.parent;
        if (parent) {
            this.propagateUp(parent, check);
        }
    }
    findIndexInSelection(node) {
        let index = -1;
        if (this._selection && this._selection.length) {
            for (let i = 0; i < this._selection.length; i++) {
                if (this.equals(node, this._selection[i])) {
                    index = i;
                    break;
                }
            }
        }
        return index;
    }
    isSingleSelectionMode() {
        return this.selectionMode() === 'single';
    }
    isMultipleSelectionMode() {
        return this.selectionMode() === 'multiple';
    }
    equals(node1, node2) {
        return this.compareSelectionBy() === 'equals' ? equals(node1, node2) : equals(node1.data, node2.data, this.dataKey());
    }
    filter(value, field, matchMode) {
        if (this.filterTimeout) {
            clearTimeout(this.filterTimeout);
        }
        if (!this.isFilterBlank(value)) {
            this.filters[field] = { value: value, matchMode: matchMode };
        }
        else if (this.filters[field]) {
            delete this.filters[field];
        }
        this.filterTimeout = setTimeout(() => {
            this._filter();
            this.filterTimeout = null;
        }, this.filterDelay());
    }
    filterGlobal(value, matchMode) {
        this.filter(value, 'global', matchMode);
    }
    isFilterBlank(filter) {
        if (filter !== null && filter !== undefined) {
            if ((typeof filter === 'string' && filter.trim().length == 0) || (Array.isArray(filter) && filter.length == 0))
                return true;
            else
                return false;
        }
        return true;
    }
    _filter() {
        if (this.lazy()) {
            this.onLazyLoad.emit(this.createLazyLoadMetadata());
        }
        else {
            if (!this.value) {
                return;
            }
            if (!this.hasFilter()) {
                this.filteredNodes = null;
                if (this.paginator()) {
                    this.totalRecords = this.value ? this.value.length : 0;
                }
            }
            else {
                let globalFilterFieldsArray;
                if (this.filters['global']) {
                    if (!this.columns() && !this.globalFilterFields())
                        throw new Error('Global filtering requires dynamic columns or globalFilterFields to be defined.');
                    else
                        globalFilterFieldsArray = this.globalFilterFields() || this.columns();
                }
                this.filteredNodes = [];
                const isStrictMode = this.filterMode() === 'strict';
                let isValueChanged = false;
                for (let node of this.value) {
                    let copyNode = { ...node };
                    let localMatch = true;
                    let globalMatch = false;
                    let paramsWithoutNode;
                    for (let prop in this.filters) {
                        if (this.filters.hasOwnProperty(prop) && prop !== 'global') {
                            let filterMeta = this.filters[prop];
                            let filterField = prop;
                            let filterValue = filterMeta.value;
                            let filterMatchMode = filterMeta.matchMode || 'startsWith';
                            let filterConstraint = this.filterService.filters[filterMatchMode];
                            paramsWithoutNode = { filterField, filterValue, filterConstraint, isStrictMode };
                            if ((isStrictMode && !(this.findFilteredNodes(copyNode, paramsWithoutNode) || this.isFilterMatched(copyNode, paramsWithoutNode))) ||
                                (!isStrictMode && !(this.isFilterMatched(copyNode, paramsWithoutNode) || this.findFilteredNodes(copyNode, paramsWithoutNode)))) {
                                localMatch = false;
                            }
                            if (!localMatch) {
                                break;
                            }
                        }
                    }
                    if (this.filters['global'] && !globalMatch && globalFilterFieldsArray) {
                        let copyNodeForGlobal = { ...copyNode };
                        let filterField = undefined;
                        let filterValue = this.filters['global'].value;
                        let filterConstraint = this.filterService.filters[this.filters['global'].matchMode];
                        paramsWithoutNode = {
                            filterField,
                            filterValue,
                            filterConstraint,
                            isStrictMode,
                            globalFilterFieldsArray
                        };
                        if ((isStrictMode && (this.findFilteredNodes(copyNodeForGlobal, paramsWithoutNode) || this.isFilterMatched(copyNodeForGlobal, paramsWithoutNode))) ||
                            (!isStrictMode && (this.isFilterMatched(copyNodeForGlobal, paramsWithoutNode) || this.findFilteredNodes(copyNodeForGlobal, paramsWithoutNode)))) {
                            globalMatch = true;
                            copyNode = copyNodeForGlobal;
                        }
                    }
                    let matches = localMatch;
                    if (this.filters['global']) {
                        matches = localMatch && globalMatch;
                    }
                    if (matches) {
                        this.filteredNodes.push(copyNode);
                    }
                    isValueChanged = isValueChanged || !localMatch || globalMatch || (localMatch && this.filteredNodes.length > 0) || (!globalMatch && this.filteredNodes.length === 0);
                }
                if (!isValueChanged) {
                    this.filteredNodes = null;
                }
                if (this.paginator()) {
                    this.totalRecords = this.filteredNodes ? this.filteredNodes.length : this.value ? this.value.length : 0;
                }
            }
            this.cd.markForCheck();
        }
        this.first = 0;
        const filteredValue = this.filteredNodes || this.value;
        this.onFilter.emit({
            filters: this.filters,
            filteredValue: filteredValue
        });
        this.tableService.onUIUpdate(filteredValue);
        this.updateSerializedValue();
        if (this.scrollable()) {
            this.resetScrollTop();
        }
    }
    findFilteredNodes(node, paramsWithoutNode) {
        if (node) {
            let matched = false;
            if (node.children) {
                let childNodes = [...node.children];
                node.children = [];
                for (let childNode of childNodes) {
                    let copyChildNode = { ...childNode };
                    if (this.isFilterMatched(copyChildNode, paramsWithoutNode)) {
                        matched = true;
                        node.children.push(copyChildNode);
                    }
                }
            }
            if (matched) {
                return true;
            }
        }
    }
    isFilterMatched(node, filterOptions) {
        let { filterField, filterValue, filterConstraint, isStrictMode, globalFilterFieldsArray } = filterOptions;
        let matched = false;
        const isMatched = (field) => filterConstraint(resolveFieldData(node.data, field), filterValue, this.filterLocale());
        matched = globalFilterFieldsArray?.length ? globalFilterFieldsArray.some((globalFilterField) => isMatched(globalFilterField.field || globalFilterField)) : isMatched(filterField);
        if (!matched || (isStrictMode && !this.isNodeLeaf(node))) {
            matched =
                this.findFilteredNodes(node, {
                    filterField,
                    filterValue,
                    filterConstraint,
                    isStrictMode,
                    globalFilterFieldsArray
                }) || matched;
        }
        return matched;
    }
    isNodeLeaf(node) {
        return node.leaf === false ? false : !(node.children && node.children.length);
    }
    hasFilter() {
        let empty = true;
        for (let prop in this.filters) {
            if (this.filters.hasOwnProperty(prop)) {
                empty = false;
                break;
            }
        }
        return !empty;
    }
    /**
     * Clears the sort and paginator state.
     * @group Method
     */
    reset() {
        this._sortField = null;
        this._sortOrder = 1;
        this._multiSortMeta = null;
        this.tableService.onSort(null);
        this.filteredNodes = null;
        this.filters = {};
        this.first = 0;
        if (this.lazy()) {
            this.onLazyLoad.emit(this.createLazyLoadMetadata());
        }
        else {
            this.totalRecords = this.value ? this.value.length : 0;
        }
    }
    updateEditingCell(cell, data, field) {
        this.editingCell = cell;
        this.editingCellData = data;
        this.editingCellField = field;
        this.bindDocumentEditListener();
    }
    isEditingCellValid() {
        return this.editingCell && find(this.editingCell, '.ng-invalid.ng-dirty').length === 0;
    }
    bindDocumentEditListener() {
        if (!this.documentEditListener) {
            this.documentEditListener = this.renderer.listen(this.document, 'click', (event) => {
                if (this.editingCell && !this.editingCellClick && this.isEditingCellValid()) {
                    !this.$unstyled() && removeClass(this.editingCell, 'p-cell-editing');
                    this.editingCell = null;
                    this.onEditComplete.emit({ field: this.editingCellField, data: this.editingCellData });
                    this.editingCellField = null;
                    this.editingCellData = null;
                    this.unbindDocumentEditListener();
                }
                this.editingCellClick = false;
            });
        }
    }
    unbindDocumentEditListener() {
        if (this.documentEditListener) {
            this.documentEditListener();
            this.documentEditListener = null;
        }
    }
    onDestroy() {
        this.unbindDocumentEditListener();
        this.editingCell = null;
        this.editingCellField = null;
        this.editingCellData = null;
        this.initialized = null;
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTable, deps: [], target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.0.0", version: "22.0.6", type: TreeTable, isStandalone: true, selector: "p-treetable, p-tree-table", inputs: { columns: { classPropertyName: "columns", publicName: "columns", isSignal: true, isRequired: false, transformFunction: null }, tableStyle: { classPropertyName: "tableStyle", publicName: "tableStyle", isSignal: true, isRequired: false, transformFunction: null }, tableStyleClass: { classPropertyName: "tableStyleClass", publicName: "tableStyleClass", isSignal: true, isRequired: false, transformFunction: null }, autoLayout: { classPropertyName: "autoLayout", publicName: "autoLayout", isSignal: true, isRequired: false, transformFunction: null }, lazy: { classPropertyName: "lazy", publicName: "lazy", isSignal: true, isRequired: false, transformFunction: null }, lazyLoadOnInit: { classPropertyName: "lazyLoadOnInit", publicName: "lazyLoadOnInit", isSignal: true, isRequired: false, transformFunction: null }, paginator: { classPropertyName: "paginator", publicName: "paginator", isSignal: true, isRequired: false, transformFunction: null }, rows: { classPropertyName: "rows", publicName: "rows", isSignal: true, isRequired: false, transformFunction: null }, firstInput: { classPropertyName: "firstInput", publicName: "first", isSignal: true, isRequired: false, transformFunction: null }, pageLinks: { classPropertyName: "pageLinks", publicName: "pageLinks", isSignal: true, isRequired: false, transformFunction: null }, rowsPerPageOptions: { classPropertyName: "rowsPerPageOptions", publicName: "rowsPerPageOptions", isSignal: true, isRequired: false, transformFunction: null }, alwaysShowPaginator: { classPropertyName: "alwaysShowPaginator", publicName: "alwaysShowPaginator", isSignal: true, isRequired: false, transformFunction: null }, paginatorPosition: { classPropertyName: "paginatorPosition", publicName: "paginatorPosition", isSignal: true, isRequired: false, transformFunction: null }, paginatorStyleClass: { classPropertyName: "paginatorStyleClass", publicName: "paginatorStyleClass", isSignal: true, isRequired: false, transformFunction: null }, paginatorDropdownAppendTo: { classPropertyName: "paginatorDropdownAppendTo", publicName: "paginatorDropdownAppendTo", isSignal: true, isRequired: false, transformFunction: null }, currentPageReportTemplate: { classPropertyName: "currentPageReportTemplate", publicName: "currentPageReportTemplate", isSignal: true, isRequired: false, transformFunction: null }, showCurrentPageReport: { classPropertyName: "showCurrentPageReport", publicName: "showCurrentPageReport", isSignal: true, isRequired: false, transformFunction: null }, showJumpToPageDropdown: { classPropertyName: "showJumpToPageDropdown", publicName: "showJumpToPageDropdown", isSignal: true, isRequired: false, transformFunction: null }, showFirstLastIcon: { classPropertyName: "showFirstLastIcon", publicName: "showFirstLastIcon", isSignal: true, isRequired: false, transformFunction: null }, showPageLinks: { classPropertyName: "showPageLinks", publicName: "showPageLinks", isSignal: true, isRequired: false, transformFunction: null }, defaultSortOrder: { classPropertyName: "defaultSortOrder", publicName: "defaultSortOrder", isSignal: true, isRequired: false, transformFunction: null }, sortMode: { classPropertyName: "sortMode", publicName: "sortMode", isSignal: true, isRequired: false, transformFunction: null }, resetPageOnSort: { classPropertyName: "resetPageOnSort", publicName: "resetPageOnSort", isSignal: true, isRequired: false, transformFunction: null }, customSort: { classPropertyName: "customSort", publicName: "customSort", isSignal: true, isRequired: false, transformFunction: null }, selectionMode: { classPropertyName: "selectionMode", publicName: "selectionMode", isSignal: true, isRequired: false, transformFunction: null }, contextMenuSelection: { classPropertyName: "contextMenuSelection", publicName: "contextMenuSelection", isSignal: true, isRequired: false, transformFunction: null }, dataKey: { classPropertyName: "dataKey", publicName: "dataKey", isSignal: true, isRequired: false, transformFunction: null }, metaKeySelection: { classPropertyName: "metaKeySelection", publicName: "metaKeySelection", isSignal: true, isRequired: false, transformFunction: null }, compareSelectionBy: { classPropertyName: "compareSelectionBy", publicName: "compareSelectionBy", isSignal: true, isRequired: false, transformFunction: null }, rowHover: { classPropertyName: "rowHover", publicName: "rowHover", isSignal: true, isRequired: false, transformFunction: null }, loading: { classPropertyName: "loading", publicName: "loading", isSignal: true, isRequired: false, transformFunction: null }, loadingIcon: { classPropertyName: "loadingIcon", publicName: "loadingIcon", isSignal: true, isRequired: false, transformFunction: null }, showLoader: { classPropertyName: "showLoader", publicName: "showLoader", isSignal: true, isRequired: false, transformFunction: null }, scrollable: { classPropertyName: "scrollable", publicName: "scrollable", isSignal: true, isRequired: false, transformFunction: null }, scrollHeight: { classPropertyName: "scrollHeight", publicName: "scrollHeight", isSignal: true, isRequired: false, transformFunction: null }, virtualScroll: { classPropertyName: "virtualScroll", publicName: "virtualScroll", isSignal: true, isRequired: false, transformFunction: null }, virtualScrollItemSize: { classPropertyName: "virtualScrollItemSize", publicName: "virtualScrollItemSize", isSignal: true, isRequired: false, transformFunction: null }, virtualScrollOptions: { classPropertyName: "virtualScrollOptions", publicName: "virtualScrollOptions", isSignal: true, isRequired: false, transformFunction: null }, virtualScrollDelay: { classPropertyName: "virtualScrollDelay", publicName: "virtualScrollDelay", isSignal: true, isRequired: false, transformFunction: null }, frozenWidth: { classPropertyName: "frozenWidth", publicName: "frozenWidth", isSignal: true, isRequired: false, transformFunction: null }, frozenColumns: { classPropertyName: "frozenColumns", publicName: "frozenColumns", isSignal: true, isRequired: false, transformFunction: null }, resizableColumns: { classPropertyName: "resizableColumns", publicName: "resizableColumns", isSignal: true, isRequired: false, transformFunction: null }, columnResizeMode: { classPropertyName: "columnResizeMode", publicName: "columnResizeMode", isSignal: true, isRequired: false, transformFunction: null }, reorderableColumns: { classPropertyName: "reorderableColumns", publicName: "reorderableColumns", isSignal: true, isRequired: false, transformFunction: null }, contextMenu: { classPropertyName: "contextMenu", publicName: "contextMenu", isSignal: true, isRequired: false, transformFunction: null }, rowTrackBy: { classPropertyName: "rowTrackBy", publicName: "rowTrackBy", isSignal: true, isRequired: false, transformFunction: null }, filtersInput: { classPropertyName: "filtersInput", publicName: "filters", isSignal: true, isRequired: false, transformFunction: null }, globalFilterFields: { classPropertyName: "globalFilterFields", publicName: "globalFilterFields", isSignal: true, isRequired: false, transformFunction: null }, filterDelay: { classPropertyName: "filterDelay", publicName: "filterDelay", isSignal: true, isRequired: false, transformFunction: null }, filterMode: { classPropertyName: "filterMode", publicName: "filterMode", isSignal: true, isRequired: false, transformFunction: null }, filterLocale: { classPropertyName: "filterLocale", publicName: "filterLocale", isSignal: true, isRequired: false, transformFunction: null }, paginatorLocale: { classPropertyName: "paginatorLocale", publicName: "paginatorLocale", isSignal: true, isRequired: false, transformFunction: null }, totalRecordsInput: { classPropertyName: "totalRecordsInput", publicName: "totalRecords", isSignal: true, isRequired: false, transformFunction: null }, sortFieldInput: { classPropertyName: "sortFieldInput", publicName: "sortField", isSignal: true, isRequired: false, transformFunction: null }, sortOrderInput: { classPropertyName: "sortOrderInput", publicName: "sortOrder", isSignal: true, isRequired: false, transformFunction: null }, multiSortMetaInput: { classPropertyName: "multiSortMetaInput", publicName: "multiSortMeta", isSignal: true, isRequired: false, transformFunction: null }, selectionInput: { classPropertyName: "selectionInput", publicName: "selection", isSignal: true, isRequired: false, transformFunction: null }, valueInput: { classPropertyName: "valueInput", publicName: "value", isSignal: true, isRequired: false, transformFunction: null }, selectionKeys: { classPropertyName: "selectionKeys", publicName: "selectionKeys", isSignal: true, isRequired: false, transformFunction: null }, collapsedKeys: { classPropertyName: "collapsedKeys", publicName: "collapsedKeys", isSignal: true, isRequired: false, transformFunction: null }, showGridlines: { classPropertyName: "showGridlines", publicName: "showGridlines", isSignal: true, isRequired: false, transformFunction: null } }, outputs: { rows: "rowsChange", selectionChange: "selectionChange", contextMenuSelectionChange: "contextMenuSelectionChange", onFilter: "onFilter", onNodeExpand: "onNodeExpand", onNodeCollapse: "onNodeCollapse", onPage: "onPage", onSort: "onSort", onLazyLoad: "onLazyLoad", sortFunction: "sortFunction", onColResize: "onColResize", onColReorder: "onColReorder", onNodeSelect: "onNodeSelect", onNodeUnselect: "onNodeUnselect", onContextMenuSelect: "onContextMenuSelect", onHeaderCheckboxToggle: "onHeaderCheckboxToggle", onEditInit: "onEditInit", onEditComplete: "onEditComplete", onEditCancel: "onEditCancel", selectionKeysChange: "selectionKeysChange", collapsedKeysChange: "collapsedKeysChange" }, host: { properties: { "class": "cx('root')", "attr.data-p": "dataP()", "attr.data-scrollselectors": "'.p-treetable-scrollable-body'" } }, providers: [TreeTableService, TreeTableStyle, { provide: TREETABLE_INSTANCE, useExisting: TreeTable }, { provide: PARENT_INSTANCE, useExisting: TreeTable }], queries: [{ propertyName: "colGroupTemplate", first: true, predicate: ["colgroup"], isSignal: true }, { propertyName: "captionTemplate", first: true, predicate: ["caption"], isSignal: true }, { propertyName: "headerTemplate", first: true, predicate: ["header"], isSignal: true }, { propertyName: "bodyTemplate", first: true, predicate: ["body"], isSignal: true }, { propertyName: "footerTemplate", first: true, predicate: ["footer"], isSignal: true }, { propertyName: "summaryTemplate", first: true, predicate: ["summary"], isSignal: true }, { propertyName: "emptyMessageTemplate", first: true, predicate: ["emptymessage"], isSignal: true }, { propertyName: "paginatorLeftTemplate", first: true, predicate: ["paginatorleft"], isSignal: true }, { propertyName: "paginatorRightTemplate", first: true, predicate: ["paginatorright"], isSignal: true }, { propertyName: "paginatorDropdownItemTemplate", first: true, predicate: ["paginatordropdownitem"], isSignal: true }, { propertyName: "frozenHeaderTemplate", first: true, predicate: ["frozenheader"], isSignal: true }, { propertyName: "frozenBodyTemplate", first: true, predicate: ["frozenbody"], isSignal: true }, { propertyName: "frozenFooterTemplate", first: true, predicate: ["frozenfooter"], isSignal: true }, { propertyName: "frozenColGroupTemplate", first: true, predicate: ["frozencolgroup"], isSignal: true }, { propertyName: "loadingIconTemplate", first: true, predicate: ["loadingicon"], isSignal: true }, { propertyName: "reorderIndicatorUpIconTemplate", first: true, predicate: ["reorderindicatorupicon"], isSignal: true }, { propertyName: "reorderIndicatorDownIconTemplate", first: true, predicate: ["reorderindicatordownicon"], isSignal: true }, { propertyName: "sortIconTemplate", first: true, predicate: ["sorticon"], isSignal: true }, { propertyName: "checkboxIconTemplate", first: true, predicate: ["checkboxicon"], isSignal: true }, { propertyName: "headerCheckboxIconTemplate", first: true, predicate: ["headercheckboxicon"], isSignal: true }, { propertyName: "togglerIconTemplate", first: true, predicate: ["togglericon"], isSignal: true }, { propertyName: "paginatorFirstPageLinkIconTemplate", first: true, predicate: ["paginatorfirstpagelinkicon"], isSignal: true }, { propertyName: "paginatorLastPageLinkIconTemplate", first: true, predicate: ["paginatorlastpagelinkicon"], isSignal: true }, { propertyName: "paginatorPreviousPageLinkIconTemplate", first: true, predicate: ["paginatorpreviouspagelinkicon"], isSignal: true }, { propertyName: "paginatorNextPageLinkIconTemplate", first: true, predicate: ["paginatornextpagelinkicon"], isSignal: true }, { propertyName: "loaderTemplate", first: true, predicate: ["loader"], isSignal: true }], viewQueries: [{ propertyName: "resizeHelperViewChild", first: true, predicate: ["resizeHelper"], descendants: true, isSignal: true }, { propertyName: "reorderIndicatorUpViewChild", first: true, predicate: ["reorderIndicatorUp"], descendants: true, isSignal: true }, { propertyName: "reorderIndicatorDownViewChild", first: true, predicate: ["reorderIndicatorDown"], descendants: true, isSignal: true }, { propertyName: "tableViewChild", first: true, predicate: ["table"], descendants: true, isSignal: true }, { propertyName: "scrollableViewChild", first: true, predicate: ["scrollableView"], descendants: true, isSignal: true }, { propertyName: "scrollableFrozenViewChild", first: true, predicate: ["scrollableFrozenView"], descendants: true, isSignal: true }], usesInheritance: true, hostDirectives: [{ directive: i1.Bind }], ngImport: i0, template: `
        @if (showLoadingMask()) {
            <div [pBind]="ptm('mask')" [class]="cx('mask')" animate.enter="p-overlay-mask-enter-active" animate.leave="p-overlay-mask-leave-active">
                @if (loadingIcon()) {
                    <i [class]="cn(cx('loadingIcon'), 'pi-spin' + loadingIcon())"></i>
                }
                @if (!loadingIcon()) {
                    @if (!loadingIconTemplate()) {
                        <svg data-p-icon="spinner" [class]="cx('loadingIcon')" [spin]="true" />
                    }
                    @if (loadingIconTemplate()) {
                        <span [class]="cx('loadingIcon')">
                            <ng-template *ngTemplateOutlet="loadingIconTemplate()"></ng-template>
                        </span>
                    }
                }
            </div>
        }
        @if (captionTemplate()) {
            <div [pBind]="ptm('header')" [class]="cx('header')">
                <ng-container *ngTemplateOutlet="captionTemplate()"></ng-container>
            </div>
        }
        @if (showTopPaginator()) {
            <p-paginator
                [pt]="ptm('pcPaginator')"
                [rows]="rows()"
                [first]="first"
                [totalRecords]="totalRecords"
                [pageLinkSize]="pageLinks()"
                [class]="cx('pcPaginator')"
                [alwaysShow]="alwaysShowPaginator()"
                (onPageChange)="onPageChange($event)"
                [rowsPerPageOptions]="rowsPerPageOptions()"
                [templateLeft]="paginatorLeftTemplate()"
                [templateRight]="paginatorRightTemplate()"
                [appendTo]="paginatorDropdownAppendTo()"
                [currentPageReportTemplate]="currentPageReportTemplate()"
                [showFirstLastIcon]="showFirstLastIcon()"
                [dropdownItemTemplate]="paginatorDropdownItemTemplate()"
                [showCurrentPageReport]="showCurrentPageReport()"
                [showJumpToPageDropdown]="showJumpToPageDropdown()"
                [showPageLinks]="showPageLinks()"
                [locale]="paginatorLocale()"
                [unstyled]="unstyled()"
            >
                @if (paginatorFirstPageLinkIconTemplate()) {
                    <ng-template #firstpagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorFirstPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }

                @if (paginatorPreviousPageLinkIconTemplate()) {
                    <ng-template #previouspagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorPreviousPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }

                @if (paginatorLastPageLinkIconTemplate()) {
                    <ng-template #lastpagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorLastPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }

                @if (paginatorNextPageLinkIconTemplate()) {
                    <ng-template #nextpagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorNextPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }
            </p-paginator>
        }

        @if (!scrollable()) {
            <div [pBind]="ptm('wrapper')" [class]="cx('wrapper')" [style]="sx('wrapper')">
                <table role="treegrid" [pBind]="ptm('table')" #table [class]="tableStyleClass()" [style]="tableStyle()">
                    <ng-container *ngTemplateOutlet="colGroupTemplate(); context: { $implicit: columns() }"></ng-container>
                    <thead role="rowgroup" [class]="cx('thead')" [pBind]="ptm('thead')">
                        <ng-container *ngTemplateOutlet="headerTemplate(); context: { $implicit: columns() }"></ng-container>
                    </thead>
                    <tbody [class]="cx('tbody')" [pBind]="ptm('tbody')" role="rowgroup" [unstyled]="unstyled()" [pTreeTableBody]="columns()" [pTreeTableBodyTemplate]="bodyTemplate()"></tbody>
                    <tfoot [class]="cx('tfoot')" [pBind]="ptm('tfoot')" role="rowgroup">
                        <ng-container *ngTemplateOutlet="footerTemplate(); context: { $implicit: columns() }"></ng-container>
                    </tfoot>
                </table>
            </div>
        }

        @if (scrollable()) {
            <div [pBind]="ptm('scrollableWrapper')" [class]="cx('scrollableWrapper')">
                @if (frozenColumns() || frozenBodyTemplate()) {
                    <div
                        [class]="cn(cx('scrollableView'), cx('frozenView'))"
                        #scrollableFrozenView
                        [ttScrollableView]="frozenColumns()"
                        [unstyled]="unstyled()"
                        [frozen]="true"
                        [style]="{ width: frozenWidth() }"
                        [scrollHeight]="scrollHeight()"
                        [pBind]="ptm('scrollableView')"
                    ></div>
                }
                <div
                    [class]="cx('scrollableView')"
                    [pBind]="ptm('scrollableView')"
                    #scrollableView
                    [ttScrollableView]="columns()"
                    [unstyled]="unstyled()"
                    [frozen]="false"
                    [scrollHeight]="scrollHeight()"
                    [style]="{ left: frozenWidth(), width: 'calc(100% - ' + frozenWidth() + ')' }"
                ></div>
            </div>
        }

        @if (showBottomPaginator()) {
            <p-paginator
                [pt]="ptm('pcPaginator')"
                [rows]="rows()"
                [first]="first"
                [totalRecords]="totalRecords"
                [pageLinkSize]="pageLinks()"
                [class]="cx('pcPaginator')"
                [alwaysShow]="alwaysShowPaginator()"
                (onPageChange)="onPageChange($event)"
                [rowsPerPageOptions]="rowsPerPageOptions()"
                [templateLeft]="paginatorLeftTemplate()"
                [templateRight]="paginatorRightTemplate()"
                [appendTo]="paginatorDropdownAppendTo()"
                [currentPageReportTemplate]="currentPageReportTemplate()"
                [showFirstLastIcon]="showFirstLastIcon()"
                [dropdownItemTemplate]="paginatorDropdownItemTemplate()"
                [showCurrentPageReport]="showCurrentPageReport()"
                [showJumpToPageDropdown]="showJumpToPageDropdown()"
                [showPageLinks]="showPageLinks()"
                [locale]="paginatorLocale()"
                [unstyled]="unstyled()"
            >
                @if (paginatorFirstPageLinkIconTemplate()) {
                    <ng-template #firstpagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorFirstPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }

                @if (paginatorPreviousPageLinkIconTemplate()) {
                    <ng-template #previouspagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorPreviousPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }

                @if (paginatorLastPageLinkIconTemplate()) {
                    <ng-template #lastpagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorLastPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }

                @if (paginatorNextPageLinkIconTemplate()) {
                    <ng-template #nextpagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorNextPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }
            </p-paginator>
        }
        @if (summaryTemplate()) {
            <div [pBind]="ptm('footer')" [class]="cx('footer')">
                <ng-container *ngTemplateOutlet="summaryTemplate()"></ng-container>
            </div>
        }

        @if (resizableColumns()) {
            <div [pBind]="ptm('columnResizerHelper')" #resizeHelper [class]="cx('columnResizerHelper')" [style.display]="'none'"></div>
        }
        @if (reorderableColumns()) {
            <span [pBind]="ptm('reorderIndicatorUp')" #reorderIndicatorUp [class]="cx('reorderIndicatorUp')" [style.display]="'none'">
                @if (!reorderIndicatorUpIconTemplate()) {
                    <svg data-p-icon="arrow-down" />
                }
                <ng-template *ngTemplateOutlet="reorderIndicatorUpIconTemplate()"></ng-template>
            </span>
            <span [pBind]="ptm('reorderIndicatorDown')" #reorderIndicatorDown [class]="cx('reorderIndicatorDown')" [style.display]="'none'">
                @if (!reorderIndicatorDownIconTemplate()) {
                    <svg data-p-icon="arrow-up" />
                }
                <ng-template *ngTemplateOutlet="reorderIndicatorDownIconTemplate()"></ng-template>
            </span>
        }
    `, isInline: true, dependencies: [{ kind: "directive", type: NgTemplateOutlet, selector: "[ngTemplateOutlet]", inputs: ["ngTemplateOutletContext", "ngTemplateOutlet", "ngTemplateOutletInjector"] }, { kind: "directive", type: Bind, selector: "[pBind]", inputs: ["pBind"] }, { kind: "ngmodule", type: PaginatorModule }, { kind: "component", type: i2.Paginator, selector: "p-paginator", inputs: ["pageLinkSize", "alwaysShow", "templateLeft", "templateRight", "dropdownScrollHeight", "currentPageReportTemplate", "showCurrentPageReport", "showFirstLastIcon", "totalRecords", "rows", "first", "rowsPerPageOptions", "showJumpToPageDropdown", "showJumpToPageInput", "jumpToPageItemTemplate", "showPageLinks", "locale", "dropdownItemTemplate", "appendTo"], outputs: ["rowsChange", "firstChange", "onPageChange"] }, { kind: "component", type: Spinner, selector: "svg[data-p-icon=\"spinner\"]" }, { kind: "component", type: ArrowDown, selector: "svg[data-p-icon=\"arrow-down\"]" }, { kind: "component", type: ArrowUp, selector: "svg[data-p-icon=\"arrow-up\"]" }, { kind: "component", type: TTScrollableView, selector: "[ttScrollableView]", inputs: ["ttScrollableView", "frozen", "scrollHeight"] }, { kind: "component", type: TTBody, selector: "[pTreeTableBody]", inputs: ["pTreeTableBody", "pTreeTableBodyTemplate", "frozen", "serializedNodes", "scrollerOptions"] }], encapsulation: i0.ViewEncapsulation.None });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTable, decorators: [{
            type: Component,
            args: [{
                    selector: 'p-treetable, p-tree-table',
                    standalone: true,
                    imports: [NgTemplateOutlet, Bind, PaginatorModule, Spinner, ArrowDown, ArrowUp, TTScrollableView, TTBody],
                    template: `
        @if (showLoadingMask()) {
            <div [pBind]="ptm('mask')" [class]="cx('mask')" animate.enter="p-overlay-mask-enter-active" animate.leave="p-overlay-mask-leave-active">
                @if (loadingIcon()) {
                    <i [class]="cn(cx('loadingIcon'), 'pi-spin' + loadingIcon())"></i>
                }
                @if (!loadingIcon()) {
                    @if (!loadingIconTemplate()) {
                        <svg data-p-icon="spinner" [class]="cx('loadingIcon')" [spin]="true" />
                    }
                    @if (loadingIconTemplate()) {
                        <span [class]="cx('loadingIcon')">
                            <ng-template *ngTemplateOutlet="loadingIconTemplate()"></ng-template>
                        </span>
                    }
                }
            </div>
        }
        @if (captionTemplate()) {
            <div [pBind]="ptm('header')" [class]="cx('header')">
                <ng-container *ngTemplateOutlet="captionTemplate()"></ng-container>
            </div>
        }
        @if (showTopPaginator()) {
            <p-paginator
                [pt]="ptm('pcPaginator')"
                [rows]="rows()"
                [first]="first"
                [totalRecords]="totalRecords"
                [pageLinkSize]="pageLinks()"
                [class]="cx('pcPaginator')"
                [alwaysShow]="alwaysShowPaginator()"
                (onPageChange)="onPageChange($event)"
                [rowsPerPageOptions]="rowsPerPageOptions()"
                [templateLeft]="paginatorLeftTemplate()"
                [templateRight]="paginatorRightTemplate()"
                [appendTo]="paginatorDropdownAppendTo()"
                [currentPageReportTemplate]="currentPageReportTemplate()"
                [showFirstLastIcon]="showFirstLastIcon()"
                [dropdownItemTemplate]="paginatorDropdownItemTemplate()"
                [showCurrentPageReport]="showCurrentPageReport()"
                [showJumpToPageDropdown]="showJumpToPageDropdown()"
                [showPageLinks]="showPageLinks()"
                [locale]="paginatorLocale()"
                [unstyled]="unstyled()"
            >
                @if (paginatorFirstPageLinkIconTemplate()) {
                    <ng-template #firstpagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorFirstPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }

                @if (paginatorPreviousPageLinkIconTemplate()) {
                    <ng-template #previouspagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorPreviousPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }

                @if (paginatorLastPageLinkIconTemplate()) {
                    <ng-template #lastpagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorLastPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }

                @if (paginatorNextPageLinkIconTemplate()) {
                    <ng-template #nextpagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorNextPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }
            </p-paginator>
        }

        @if (!scrollable()) {
            <div [pBind]="ptm('wrapper')" [class]="cx('wrapper')" [style]="sx('wrapper')">
                <table role="treegrid" [pBind]="ptm('table')" #table [class]="tableStyleClass()" [style]="tableStyle()">
                    <ng-container *ngTemplateOutlet="colGroupTemplate(); context: { $implicit: columns() }"></ng-container>
                    <thead role="rowgroup" [class]="cx('thead')" [pBind]="ptm('thead')">
                        <ng-container *ngTemplateOutlet="headerTemplate(); context: { $implicit: columns() }"></ng-container>
                    </thead>
                    <tbody [class]="cx('tbody')" [pBind]="ptm('tbody')" role="rowgroup" [unstyled]="unstyled()" [pTreeTableBody]="columns()" [pTreeTableBodyTemplate]="bodyTemplate()"></tbody>
                    <tfoot [class]="cx('tfoot')" [pBind]="ptm('tfoot')" role="rowgroup">
                        <ng-container *ngTemplateOutlet="footerTemplate(); context: { $implicit: columns() }"></ng-container>
                    </tfoot>
                </table>
            </div>
        }

        @if (scrollable()) {
            <div [pBind]="ptm('scrollableWrapper')" [class]="cx('scrollableWrapper')">
                @if (frozenColumns() || frozenBodyTemplate()) {
                    <div
                        [class]="cn(cx('scrollableView'), cx('frozenView'))"
                        #scrollableFrozenView
                        [ttScrollableView]="frozenColumns()"
                        [unstyled]="unstyled()"
                        [frozen]="true"
                        [style]="{ width: frozenWidth() }"
                        [scrollHeight]="scrollHeight()"
                        [pBind]="ptm('scrollableView')"
                    ></div>
                }
                <div
                    [class]="cx('scrollableView')"
                    [pBind]="ptm('scrollableView')"
                    #scrollableView
                    [ttScrollableView]="columns()"
                    [unstyled]="unstyled()"
                    [frozen]="false"
                    [scrollHeight]="scrollHeight()"
                    [style]="{ left: frozenWidth(), width: 'calc(100% - ' + frozenWidth() + ')' }"
                ></div>
            </div>
        }

        @if (showBottomPaginator()) {
            <p-paginator
                [pt]="ptm('pcPaginator')"
                [rows]="rows()"
                [first]="first"
                [totalRecords]="totalRecords"
                [pageLinkSize]="pageLinks()"
                [class]="cx('pcPaginator')"
                [alwaysShow]="alwaysShowPaginator()"
                (onPageChange)="onPageChange($event)"
                [rowsPerPageOptions]="rowsPerPageOptions()"
                [templateLeft]="paginatorLeftTemplate()"
                [templateRight]="paginatorRightTemplate()"
                [appendTo]="paginatorDropdownAppendTo()"
                [currentPageReportTemplate]="currentPageReportTemplate()"
                [showFirstLastIcon]="showFirstLastIcon()"
                [dropdownItemTemplate]="paginatorDropdownItemTemplate()"
                [showCurrentPageReport]="showCurrentPageReport()"
                [showJumpToPageDropdown]="showJumpToPageDropdown()"
                [showPageLinks]="showPageLinks()"
                [locale]="paginatorLocale()"
                [unstyled]="unstyled()"
            >
                @if (paginatorFirstPageLinkIconTemplate()) {
                    <ng-template #firstpagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorFirstPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }

                @if (paginatorPreviousPageLinkIconTemplate()) {
                    <ng-template #previouspagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorPreviousPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }

                @if (paginatorLastPageLinkIconTemplate()) {
                    <ng-template #lastpagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorLastPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }

                @if (paginatorNextPageLinkIconTemplate()) {
                    <ng-template #nextpagelinkicon>
                        <ng-container *ngTemplateOutlet="paginatorNextPageLinkIconTemplate()"></ng-container>
                    </ng-template>
                }
            </p-paginator>
        }
        @if (summaryTemplate()) {
            <div [pBind]="ptm('footer')" [class]="cx('footer')">
                <ng-container *ngTemplateOutlet="summaryTemplate()"></ng-container>
            </div>
        }

        @if (resizableColumns()) {
            <div [pBind]="ptm('columnResizerHelper')" #resizeHelper [class]="cx('columnResizerHelper')" [style.display]="'none'"></div>
        }
        @if (reorderableColumns()) {
            <span [pBind]="ptm('reorderIndicatorUp')" #reorderIndicatorUp [class]="cx('reorderIndicatorUp')" [style.display]="'none'">
                @if (!reorderIndicatorUpIconTemplate()) {
                    <svg data-p-icon="arrow-down" />
                }
                <ng-template *ngTemplateOutlet="reorderIndicatorUpIconTemplate()"></ng-template>
            </span>
            <span [pBind]="ptm('reorderIndicatorDown')" #reorderIndicatorDown [class]="cx('reorderIndicatorDown')" [style.display]="'none'">
                @if (!reorderIndicatorDownIconTemplate()) {
                    <svg data-p-icon="arrow-up" />
                }
                <ng-template *ngTemplateOutlet="reorderIndicatorDownIconTemplate()"></ng-template>
            </span>
        }
    `,
                    providers: [TreeTableService, TreeTableStyle, { provide: TREETABLE_INSTANCE, useExisting: TreeTable }, { provide: PARENT_INSTANCE, useExisting: TreeTable }],
                    encapsulation: ViewEncapsulation.None,
                    host: {
                        '[class]': "cx('root')",
                        '[attr.data-p]': 'dataP()',
                        '[attr.data-scrollselectors]': "'.p-treetable-scrollable-body'"
                    },
                    hostDirectives: [Bind]
                }]
        }], ctorParameters: () => [], propDecorators: { columns: [{ type: i0.Input, args: [{ isSignal: true, alias: "columns", required: false }] }], tableStyle: [{ type: i0.Input, args: [{ isSignal: true, alias: "tableStyle", required: false }] }], tableStyleClass: [{ type: i0.Input, args: [{ isSignal: true, alias: "tableStyleClass", required: false }] }], autoLayout: [{ type: i0.Input, args: [{ isSignal: true, alias: "autoLayout", required: false }] }], lazy: [{ type: i0.Input, args: [{ isSignal: true, alias: "lazy", required: false }] }], lazyLoadOnInit: [{ type: i0.Input, args: [{ isSignal: true, alias: "lazyLoadOnInit", required: false }] }], paginator: [{ type: i0.Input, args: [{ isSignal: true, alias: "paginator", required: false }] }], rows: [{ type: i0.Input, args: [{ isSignal: true, alias: "rows", required: false }] }, { type: i0.Output, args: ["rowsChange"] }], firstInput: [{ type: i0.Input, args: [{ isSignal: true, alias: "first", required: false }] }], pageLinks: [{ type: i0.Input, args: [{ isSignal: true, alias: "pageLinks", required: false }] }], rowsPerPageOptions: [{ type: i0.Input, args: [{ isSignal: true, alias: "rowsPerPageOptions", required: false }] }], alwaysShowPaginator: [{ type: i0.Input, args: [{ isSignal: true, alias: "alwaysShowPaginator", required: false }] }], paginatorPosition: [{ type: i0.Input, args: [{ isSignal: true, alias: "paginatorPosition", required: false }] }], paginatorStyleClass: [{ type: i0.Input, args: [{ isSignal: true, alias: "paginatorStyleClass", required: false }] }], paginatorDropdownAppendTo: [{ type: i0.Input, args: [{ isSignal: true, alias: "paginatorDropdownAppendTo", required: false }] }], currentPageReportTemplate: [{ type: i0.Input, args: [{ isSignal: true, alias: "currentPageReportTemplate", required: false }] }], showCurrentPageReport: [{ type: i0.Input, args: [{ isSignal: true, alias: "showCurrentPageReport", required: false }] }], showJumpToPageDropdown: [{ type: i0.Input, args: [{ isSignal: true, alias: "showJumpToPageDropdown", required: false }] }], showFirstLastIcon: [{ type: i0.Input, args: [{ isSignal: true, alias: "showFirstLastIcon", required: false }] }], showPageLinks: [{ type: i0.Input, args: [{ isSignal: true, alias: "showPageLinks", required: false }] }], defaultSortOrder: [{ type: i0.Input, args: [{ isSignal: true, alias: "defaultSortOrder", required: false }] }], sortMode: [{ type: i0.Input, args: [{ isSignal: true, alias: "sortMode", required: false }] }], resetPageOnSort: [{ type: i0.Input, args: [{ isSignal: true, alias: "resetPageOnSort", required: false }] }], customSort: [{ type: i0.Input, args: [{ isSignal: true, alias: "customSort", required: false }] }], selectionMode: [{ type: i0.Input, args: [{ isSignal: true, alias: "selectionMode", required: false }] }], contextMenuSelection: [{ type: i0.Input, args: [{ isSignal: true, alias: "contextMenuSelection", required: false }] }], dataKey: [{ type: i0.Input, args: [{ isSignal: true, alias: "dataKey", required: false }] }], metaKeySelection: [{ type: i0.Input, args: [{ isSignal: true, alias: "metaKeySelection", required: false }] }], compareSelectionBy: [{ type: i0.Input, args: [{ isSignal: true, alias: "compareSelectionBy", required: false }] }], rowHover: [{ type: i0.Input, args: [{ isSignal: true, alias: "rowHover", required: false }] }], loading: [{ type: i0.Input, args: [{ isSignal: true, alias: "loading", required: false }] }], loadingIcon: [{ type: i0.Input, args: [{ isSignal: true, alias: "loadingIcon", required: false }] }], showLoader: [{ type: i0.Input, args: [{ isSignal: true, alias: "showLoader", required: false }] }], scrollable: [{ type: i0.Input, args: [{ isSignal: true, alias: "scrollable", required: false }] }], scrollHeight: [{ type: i0.Input, args: [{ isSignal: true, alias: "scrollHeight", required: false }] }], virtualScroll: [{ type: i0.Input, args: [{ isSignal: true, alias: "virtualScroll", required: false }] }], virtualScrollItemSize: [{ type: i0.Input, args: [{ isSignal: true, alias: "virtualScrollItemSize", required: false }] }], virtualScrollOptions: [{ type: i0.Input, args: [{ isSignal: true, alias: "virtualScrollOptions", required: false }] }], virtualScrollDelay: [{ type: i0.Input, args: [{ isSignal: true, alias: "virtualScrollDelay", required: false }] }], frozenWidth: [{ type: i0.Input, args: [{ isSignal: true, alias: "frozenWidth", required: false }] }], frozenColumns: [{ type: i0.Input, args: [{ isSignal: true, alias: "frozenColumns", required: false }] }], resizableColumns: [{ type: i0.Input, args: [{ isSignal: true, alias: "resizableColumns", required: false }] }], columnResizeMode: [{ type: i0.Input, args: [{ isSignal: true, alias: "columnResizeMode", required: false }] }], reorderableColumns: [{ type: i0.Input, args: [{ isSignal: true, alias: "reorderableColumns", required: false }] }], contextMenu: [{ type: i0.Input, args: [{ isSignal: true, alias: "contextMenu", required: false }] }], rowTrackBy: [{ type: i0.Input, args: [{ isSignal: true, alias: "rowTrackBy", required: false }] }], filtersInput: [{ type: i0.Input, args: [{ isSignal: true, alias: "filters", required: false }] }], globalFilterFields: [{ type: i0.Input, args: [{ isSignal: true, alias: "globalFilterFields", required: false }] }], filterDelay: [{ type: i0.Input, args: [{ isSignal: true, alias: "filterDelay", required: false }] }], filterMode: [{ type: i0.Input, args: [{ isSignal: true, alias: "filterMode", required: false }] }], filterLocale: [{ type: i0.Input, args: [{ isSignal: true, alias: "filterLocale", required: false }] }], paginatorLocale: [{ type: i0.Input, args: [{ isSignal: true, alias: "paginatorLocale", required: false }] }], totalRecordsInput: [{ type: i0.Input, args: [{ isSignal: true, alias: "totalRecords", required: false }] }], sortFieldInput: [{ type: i0.Input, args: [{ isSignal: true, alias: "sortField", required: false }] }], sortOrderInput: [{ type: i0.Input, args: [{ isSignal: true, alias: "sortOrder", required: false }] }], multiSortMetaInput: [{ type: i0.Input, args: [{ isSignal: true, alias: "multiSortMeta", required: false }] }], selectionInput: [{ type: i0.Input, args: [{ isSignal: true, alias: "selection", required: false }] }], valueInput: [{ type: i0.Input, args: [{ isSignal: true, alias: "value", required: false }] }], selectionKeys: [{ type: i0.Input, args: [{ isSignal: true, alias: "selectionKeys", required: false }] }], collapsedKeys: [{ type: i0.Input, args: [{ isSignal: true, alias: "collapsedKeys", required: false }] }], showGridlines: [{ type: i0.Input, args: [{ isSignal: true, alias: "showGridlines", required: false }] }], selectionChange: [{ type: i0.Output, args: ["selectionChange"] }], contextMenuSelectionChange: [{ type: i0.Output, args: ["contextMenuSelectionChange"] }], onFilter: [{ type: i0.Output, args: ["onFilter"] }], onNodeExpand: [{ type: i0.Output, args: ["onNodeExpand"] }], onNodeCollapse: [{ type: i0.Output, args: ["onNodeCollapse"] }], onPage: [{ type: i0.Output, args: ["onPage"] }], onSort: [{ type: i0.Output, args: ["onSort"] }], onLazyLoad: [{ type: i0.Output, args: ["onLazyLoad"] }], sortFunction: [{ type: i0.Output, args: ["sortFunction"] }], onColResize: [{ type: i0.Output, args: ["onColResize"] }], onColReorder: [{ type: i0.Output, args: ["onColReorder"] }], onNodeSelect: [{ type: i0.Output, args: ["onNodeSelect"] }], onNodeUnselect: [{ type: i0.Output, args: ["onNodeUnselect"] }], onContextMenuSelect: [{ type: i0.Output, args: ["onContextMenuSelect"] }], onHeaderCheckboxToggle: [{ type: i0.Output, args: ["onHeaderCheckboxToggle"] }], onEditInit: [{ type: i0.Output, args: ["onEditInit"] }], onEditComplete: [{ type: i0.Output, args: ["onEditComplete"] }], onEditCancel: [{ type: i0.Output, args: ["onEditCancel"] }], selectionKeysChange: [{ type: i0.Output, args: ["selectionKeysChange"] }], collapsedKeysChange: [{ type: i0.Output, args: ["collapsedKeysChange"] }], resizeHelperViewChild: [{ type: i0.ViewChild, args: ['resizeHelper', { isSignal: true }] }], reorderIndicatorUpViewChild: [{ type: i0.ViewChild, args: ['reorderIndicatorUp', { isSignal: true }] }], reorderIndicatorDownViewChild: [{ type: i0.ViewChild, args: ['reorderIndicatorDown', { isSignal: true }] }], tableViewChild: [{ type: i0.ViewChild, args: ['table', { isSignal: true }] }], scrollableViewChild: [{ type: i0.ViewChild, args: ['scrollableView', { isSignal: true }] }], scrollableFrozenViewChild: [{ type: i0.ViewChild, args: ['scrollableFrozenView', { isSignal: true }] }], colGroupTemplate: [{ type: i0.ContentChild, args: ['colgroup', { ...{ descendants: false }, isSignal: true }] }], captionTemplate: [{ type: i0.ContentChild, args: ['caption', { ...{ descendants: false }, isSignal: true }] }], headerTemplate: [{ type: i0.ContentChild, args: ['header', { ...{ descendants: false }, isSignal: true }] }], bodyTemplate: [{ type: i0.ContentChild, args: ['body', { ...{ descendants: false }, isSignal: true }] }], footerTemplate: [{ type: i0.ContentChild, args: ['footer', { ...{ descendants: false }, isSignal: true }] }], summaryTemplate: [{ type: i0.ContentChild, args: ['summary', { ...{ descendants: false }, isSignal: true }] }], emptyMessageTemplate: [{ type: i0.ContentChild, args: ['emptymessage', { ...{ descendants: false }, isSignal: true }] }], paginatorLeftTemplate: [{ type: i0.ContentChild, args: ['paginatorleft', { ...{ descendants: false }, isSignal: true }] }], paginatorRightTemplate: [{ type: i0.ContentChild, args: ['paginatorright', { ...{ descendants: false }, isSignal: true }] }], paginatorDropdownItemTemplate: [{ type: i0.ContentChild, args: ['paginatordropdownitem', { ...{ descendants: false }, isSignal: true }] }], frozenHeaderTemplate: [{ type: i0.ContentChild, args: ['frozenheader', { ...{ descendants: false }, isSignal: true }] }], frozenBodyTemplate: [{ type: i0.ContentChild, args: ['frozenbody', { ...{ descendants: false }, isSignal: true }] }], frozenFooterTemplate: [{ type: i0.ContentChild, args: ['frozenfooter', { ...{ descendants: false }, isSignal: true }] }], frozenColGroupTemplate: [{ type: i0.ContentChild, args: ['frozencolgroup', { ...{ descendants: false }, isSignal: true }] }], loadingIconTemplate: [{ type: i0.ContentChild, args: ['loadingicon', { ...{ descendants: false }, isSignal: true }] }], reorderIndicatorUpIconTemplate: [{ type: i0.ContentChild, args: ['reorderindicatorupicon', { ...{ descendants: false }, isSignal: true }] }], reorderIndicatorDownIconTemplate: [{ type: i0.ContentChild, args: ['reorderindicatordownicon', { ...{ descendants: false }, isSignal: true }] }], sortIconTemplate: [{ type: i0.ContentChild, args: ['sorticon', { ...{ descendants: false }, isSignal: true }] }], checkboxIconTemplate: [{ type: i0.ContentChild, args: ['checkboxicon', { ...{ descendants: false }, isSignal: true }] }], headerCheckboxIconTemplate: [{ type: i0.ContentChild, args: ['headercheckboxicon', { ...{ descendants: false }, isSignal: true }] }], togglerIconTemplate: [{ type: i0.ContentChild, args: ['togglericon', { ...{ descendants: false }, isSignal: true }] }], paginatorFirstPageLinkIconTemplate: [{ type: i0.ContentChild, args: ['paginatorfirstpagelinkicon', { ...{ descendants: false }, isSignal: true }] }], paginatorLastPageLinkIconTemplate: [{ type: i0.ContentChild, args: ['paginatorlastpagelinkicon', { ...{ descendants: false }, isSignal: true }] }], paginatorPreviousPageLinkIconTemplate: [{ type: i0.ContentChild, args: ['paginatorpreviouspagelinkicon', { ...{ descendants: false }, isSignal: true }] }], paginatorNextPageLinkIconTemplate: [{ type: i0.ContentChild, args: ['paginatornextpagelinkicon', { ...{ descendants: false }, isSignal: true }] }], loaderTemplate: [{ type: i0.ContentChild, args: ['loader', { ...{ descendants: false }, isSignal: true }] }] } });

class TTSortableColumn extends BaseComponent {
    hostName = 'TreeTable ';
    bindDirectiveInstance = inject(Bind, { self: true });
    onAfterViewChecked() {
        this.bindDirectiveInstance.setAttrs(this.ptm('sortableColumn', { context: { sorted: this.sorted() } }));
    }
    field = input(undefined, { ...(ngDevMode ? { debugName: "field" } : /* istanbul ignore next */ {}), alias: 'ttSortableColumn' });
    ttSortableColumnDisabled = input(undefined, { ...(ngDevMode ? { debugName: "ttSortableColumnDisabled" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    sorted = signal(undefined, /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "sorted" }] : /* istanbul ignore next */ []));
    _componentStyle = inject(TreeTableStyle);
    tt = inject(TREETABLE_INSTANCE);
    get hostTabindex() {
        return this.isEnabled() ? '0' : null;
    }
    get ariaSorted() {
        if (this.sorted() && this.tt._sortOrder < 0)
            return 'descending';
        else if (this.sorted() && this.tt._sortOrder > 0)
            return 'ascending';
        else
            return 'none';
    }
    constructor() {
        super();
        this.tt.tableService.sortSource$.pipe(takeUntilDestroyed()).subscribe(() => {
            this.updateSortState();
        });
    }
    onInit() {
        if (this.isEnabled()) {
            this.updateSortState();
        }
    }
    updateSortState() {
        this.sorted.set(this.tt.isSorted(this.field()));
    }
    onClick(event) {
        if (this.isEnabled()) {
            this.updateSortState();
            this.tt.sort({
                originalEvent: event,
                field: this.field()
            });
            clearSelection();
        }
    }
    onEnterKey(event) {
        this.onClick(event);
    }
    isEnabled() {
        return this.ttSortableColumnDisabled() !== true;
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTSortableColumn, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.1.0", version: "22.0.6", type: TTSortableColumn, isStandalone: true, selector: "[ttSortableColumn]", inputs: { field: { classPropertyName: "field", publicName: "ttSortableColumn", isSignal: true, isRequired: false, transformFunction: null }, ttSortableColumnDisabled: { classPropertyName: "ttSortableColumnDisabled", publicName: "ttSortableColumnDisabled", isSignal: true, isRequired: false, transformFunction: null } }, host: { attributes: { "role": "columnheader" }, listeners: { "click": "onClick($event)", "keydown.enter": "onEnterKey($event)" }, properties: { "class": "cx(\"sortableColumn\")", "tabindex": "hostTabindex", "attr.aria-sort": "ariaSorted" } }, providers: [TreeTableStyle], usesInheritance: true, hostDirectives: [{ directive: i1.Bind }], ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTSortableColumn, decorators: [{
            type: Directive,
            args: [{
                    selector: '[ttSortableColumn]',
                    standalone: true,
                    host: {
                        '[class]': 'cx("sortableColumn")',
                        '[tabindex]': 'hostTabindex',
                        role: 'columnheader',
                        '[attr.aria-sort]': 'ariaSorted'
                    },
                    providers: [TreeTableStyle],
                    hostDirectives: [Bind]
                }]
        }], ctorParameters: () => [], propDecorators: { field: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttSortableColumn", required: false }] }], ttSortableColumnDisabled: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttSortableColumnDisabled", required: false }] }], onClick: [{
                type: HostListener,
                args: ['click', ['$event']]
            }], onEnterKey: [{
                type: HostListener,
                args: ['keydown.enter', ['$event']]
            }] } });

class TTSortIcon extends BaseComponent {
    hostName = 'TreeTable';
    field = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "field" }] : /* istanbul ignore next */ []));
    ariaLabelDesc = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "ariaLabelDesc" }] : /* istanbul ignore next */ []));
    ariaLabelAsc = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "ariaLabelAsc" }] : /* istanbul ignore next */ []));
    sortOrder = signal(undefined, /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "sortOrder" }] : /* istanbul ignore next */ []));
    _componentStyle = inject(TreeTableStyle);
    tt = inject(TREETABLE_INSTANCE);
    constructor() {
        super();
        this.tt.tableService.sortSource$.pipe(takeUntilDestroyed()).subscribe(() => {
            this.updateSortState();
        });
    }
    onInit() {
        this.updateSortState();
    }
    onClick(event) {
        event.preventDefault();
    }
    getMultiSortMetaIndex() {
        let multiSortMeta = this.tt._multiSortMeta;
        let index = -1;
        if (multiSortMeta && this.tt.sortMode() === 'multiple' && multiSortMeta.length > 1) {
            for (let i = 0; i < multiSortMeta.length; i++) {
                let meta = multiSortMeta[i];
                if (meta.field === this.field()) {
                    index = i;
                    break;
                }
            }
        }
        return index;
    }
    updateSortState() {
        if (this.tt.sortMode() === 'single') {
            this.sortOrder.set(this.tt.isSorted(this.field()) ? this.tt._sortOrder : 0);
        }
        else if (this.tt.sortMode() === 'multiple') {
            let sortMeta = this.tt.getSortMeta(this.field());
            this.sortOrder.set(sortMeta ? sortMeta.order : 0);
        }
    }
    getBadgeValue() {
        return this.getMultiSortMetaIndex() + 1;
    }
    isMultiSorted() {
        return this.tt.sortMode() === 'multiple' && this.getMultiSortMetaIndex() > -1;
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTSortIcon, deps: [], target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.0.0", version: "22.0.6", type: TTSortIcon, isStandalone: true, selector: "p-treetable-sort-icon, p-tree-table-sort-icon", inputs: { field: { classPropertyName: "field", publicName: "field", isSignal: true, isRequired: false, transformFunction: null }, ariaLabelDesc: { classPropertyName: "ariaLabelDesc", publicName: "ariaLabelDesc", isSignal: true, isRequired: false, transformFunction: null }, ariaLabelAsc: { classPropertyName: "ariaLabelAsc", publicName: "ariaLabelAsc", isSignal: true, isRequired: false, transformFunction: null } }, providers: [TreeTableStyle], usesInheritance: true, ngImport: i0, template: `
        @if (!tt.sortIconTemplate()) {
            @if (sortOrder() === 0) {
                <svg data-p-icon="sort-alt" [class]="cx('sortableColumnIcon')" [pBind]="ptm('sortableColumnIcon')" />
            }
            @if (sortOrder() === 1) {
                <svg data-p-icon="sort-amount-up-alt" [class]="cx('sortableColumnIcon')" [pBind]="ptm('sortableColumnIcon')" />
            }
            @if (sortOrder() === -1) {
                <svg data-p-icon="sort-amount-down" [class]="cx('sortableColumnIcon')" [pBind]="ptm('sortableColumnIcon')" />
            }
        }
        @if (tt.sortIconTemplate()) {
            <span [class]="cx('sortableColumnIcon')" [pBind]="ptm('sortableColumnIcon')">
                <ng-template *ngTemplateOutlet="tt.sortIconTemplate(); context: { $implicit: sortOrder() }"></ng-template>
            </span>
        }
        @if (isMultiSorted()) {
            <p-badge [class]="cx('sortableColumnBadge')" [value]="getBadgeValue()" size="small" [pt]="ptm('pcSortableColumnBadge')" [unstyled]="unstyled()" />
        }
    `, isInline: true, dependencies: [{ kind: "directive", type: NgTemplateOutlet, selector: "[ngTemplateOutlet]", inputs: ["ngTemplateOutletContext", "ngTemplateOutlet", "ngTemplateOutletInjector"] }, { kind: "directive", type: Bind, selector: "[pBind]", inputs: ["pBind"] }, { kind: "ngmodule", type: BadgeModule }, { kind: "component", type: i1$1.Badge, selector: "p-badge", inputs: ["badgeSize", "size", "severity", "value", "badgeDisabled"] }, { kind: "component", type: SortAlt, selector: "svg[data-p-icon=\"sort-alt\"]" }, { kind: "component", type: SortAmountUpAlt, selector: "svg[data-p-icon=\"sort-amount-up-alt\"]" }, { kind: "component", type: SortAmountDown, selector: "svg[data-p-icon=\"sort-amount-down\"]" }], changeDetection: i0.ChangeDetectionStrategy.OnPush, encapsulation: i0.ViewEncapsulation.None });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTSortIcon, decorators: [{
            type: Component,
            args: [{
                    selector: 'p-treetable-sort-icon, p-tree-table-sort-icon',
                    standalone: true,
                    imports: [NgTemplateOutlet, Bind, BadgeModule, SortAlt, SortAmountUpAlt, SortAmountDown],
                    template: `
        @if (!tt.sortIconTemplate()) {
            @if (sortOrder() === 0) {
                <svg data-p-icon="sort-alt" [class]="cx('sortableColumnIcon')" [pBind]="ptm('sortableColumnIcon')" />
            }
            @if (sortOrder() === 1) {
                <svg data-p-icon="sort-amount-up-alt" [class]="cx('sortableColumnIcon')" [pBind]="ptm('sortableColumnIcon')" />
            }
            @if (sortOrder() === -1) {
                <svg data-p-icon="sort-amount-down" [class]="cx('sortableColumnIcon')" [pBind]="ptm('sortableColumnIcon')" />
            }
        }
        @if (tt.sortIconTemplate()) {
            <span [class]="cx('sortableColumnIcon')" [pBind]="ptm('sortableColumnIcon')">
                <ng-template *ngTemplateOutlet="tt.sortIconTemplate(); context: { $implicit: sortOrder() }"></ng-template>
            </span>
        }
        @if (isMultiSorted()) {
            <p-badge [class]="cx('sortableColumnBadge')" [value]="getBadgeValue()" size="small" [pt]="ptm('pcSortableColumnBadge')" [unstyled]="unstyled()" />
        }
    `,
                    encapsulation: ViewEncapsulation.None,
                    changeDetection: ChangeDetectionStrategy.OnPush,
                    providers: [TreeTableStyle]
                }]
        }], ctorParameters: () => [], propDecorators: { field: [{ type: i0.Input, args: [{ isSignal: true, alias: "field", required: false }] }], ariaLabelDesc: [{ type: i0.Input, args: [{ isSignal: true, alias: "ariaLabelDesc", required: false }] }], ariaLabelAsc: [{ type: i0.Input, args: [{ isSignal: true, alias: "ariaLabelAsc", required: false }] }] } });

class TTResizableColumn extends BaseComponent {
    hostName = 'TreeTable';
    ttResizableColumnDisabled = input(undefined, { ...(ngDevMode ? { debugName: "ttResizableColumnDisabled" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    resizer;
    resizerMouseDownListener;
    documentMouseMoveListener;
    documentMouseUpListener;
    tt = inject(TREETABLE_INSTANCE);
    onAfterViewInit() {
        if (isPlatformBrowser(this.platformId)) {
            if (this.isEnabled()) {
                addClass(this.el.nativeElement, 'p-resizable-column');
                this.resizer = this.renderer.createElement('span');
                !this.$unstyled() && this.renderer.addClass(this.resizer, 'p-column-resizer');
                this.resizer.setAttribute('data-pc-section', 'columnresizer');
                this.renderer.appendChild(this.el.nativeElement, this.resizer);
                this.resizerMouseDownListener = this.renderer.listen(this.resizer, 'mousedown', this.onMouseDown.bind(this));
            }
        }
    }
    bindDocumentEvents() {
        this.documentMouseMoveListener = this.renderer.listen(this.document, 'mousemove', this.onDocumentMouseMove.bind(this));
        this.documentMouseUpListener = this.renderer.listen(this.document, 'mouseup', this.onDocumentMouseUp.bind(this));
    }
    unbindDocumentEvents() {
        if (this.documentMouseMoveListener) {
            this.documentMouseMoveListener();
            this.documentMouseMoveListener = null;
        }
        if (this.documentMouseUpListener) {
            this.documentMouseUpListener();
            this.documentMouseUpListener = null;
        }
    }
    onMouseDown(event) {
        this.tt.onColumnResizeBegin(event);
        this.bindDocumentEvents();
    }
    onDocumentMouseMove(event) {
        this.tt.onColumnResize(event);
    }
    onDocumentMouseUp(event) {
        this.tt.onColumnResizeEnd(event, this.el.nativeElement);
        this.unbindDocumentEvents();
    }
    isEnabled() {
        return this.ttResizableColumnDisabled() !== true;
    }
    onDestroy() {
        if (this.resizerMouseDownListener) {
            this.resizerMouseDownListener();
            this.resizerMouseDownListener = null;
        }
        this.unbindDocumentEvents();
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTResizableColumn, deps: null, target: i0.ɵɵFactoryTarget.Directive });
    static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.1.0", version: "22.0.6", type: TTResizableColumn, isStandalone: true, selector: "[ttResizableColumn]", inputs: { ttResizableColumnDisabled: { classPropertyName: "ttResizableColumnDisabled", publicName: "ttResizableColumnDisabled", isSignal: true, isRequired: false, transformFunction: null } }, usesInheritance: true, ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTResizableColumn, decorators: [{
            type: Directive,
            args: [{
                    selector: '[ttResizableColumn]',
                    standalone: true
                }]
        }], propDecorators: { ttResizableColumnDisabled: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttResizableColumnDisabled", required: false }] }] } });

class TTReorderableColumn extends BaseComponent {
    hostName = 'TreeTable';
    ttReorderableColumnDisabled = input(undefined, { ...(ngDevMode ? { debugName: "ttReorderableColumnDisabled" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    dragStartListener;
    dragOverListener;
    dragEnterListener;
    dragLeaveListener;
    mouseDownListener;
    tt = inject(TREETABLE_INSTANCE);
    onAfterViewInit() {
        if (this.isEnabled()) {
            this.bindEvents();
        }
    }
    bindEvents() {
        if (isPlatformBrowser(this.platformId)) {
            this.mouseDownListener = this.renderer.listen(this.el.nativeElement, 'mousedown', this.onMouseDown.bind(this));
            this.dragStartListener = this.renderer.listen(this.el.nativeElement, 'dragstart', this.onDragStart.bind(this));
            this.dragOverListener = this.renderer.listen(this.el.nativeElement, 'dragover', this.onDragEnter.bind(this));
            this.dragEnterListener = this.renderer.listen(this.el.nativeElement, 'dragenter', this.onDragEnter.bind(this));
            this.dragLeaveListener = this.renderer.listen(this.el.nativeElement, 'dragleave', this.onDragLeave.bind(this));
        }
    }
    unbindEvents() {
        if (isPlatformBrowser(this.platformId)) {
            if (this.mouseDownListener) {
                this.mouseDownListener();
                this.mouseDownListener = null;
            }
            if (this.dragOverListener) {
                this.dragOverListener();
                this.dragOverListener = null;
            }
            if (this.dragEnterListener) {
                this.dragEnterListener();
                this.dragEnterListener = null;
            }
            if (this.dragLeaveListener) {
                this.dragLeaveListener();
                this.dragLeaveListener = null;
            }
        }
    }
    onMouseDown(event) {
        if (event.target.nodeName === 'INPUT' || event.target.nodeName === 'TEXTAREA' || findSingle(event.target, '[data-pc-section="columnresizer"]'))
            this.el.nativeElement.draggable = false;
        else
            this.el.nativeElement.draggable = true;
    }
    onDragStart(event) {
        this.tt.onColumnDragStart(event, this.el.nativeElement);
    }
    onDragOver(event) {
        event.preventDefault();
    }
    onDragEnter(event) {
        this.tt.onColumnDragEnter(event, this.el.nativeElement);
    }
    onDragLeave(event) {
        this.tt.onColumnDragLeave(event);
    }
    onDrop(event) {
        if (this.isEnabled()) {
            this.tt.onColumnDrop(event, this.el.nativeElement);
        }
    }
    isEnabled() {
        return this.ttReorderableColumnDisabled() !== true;
    }
    onDestroy() {
        this.unbindEvents();
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTReorderableColumn, deps: null, target: i0.ɵɵFactoryTarget.Directive });
    static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.1.0", version: "22.0.6", type: TTReorderableColumn, isStandalone: true, selector: "[ttReorderableColumn]", inputs: { ttReorderableColumnDisabled: { classPropertyName: "ttReorderableColumnDisabled", publicName: "ttReorderableColumnDisabled", isSignal: true, isRequired: false, transformFunction: null } }, host: { listeners: { "drop": "onDrop($event)" } }, usesInheritance: true, ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTReorderableColumn, decorators: [{
            type: Directive,
            args: [{
                    selector: '[ttReorderableColumn]',
                    standalone: true
                }]
        }], propDecorators: { ttReorderableColumnDisabled: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttReorderableColumnDisabled", required: false }] }], onDrop: [{
                type: HostListener,
                args: ['drop', ['$event']]
            }] } });

class TTSelectableRow extends BaseComponent {
    rowNode = input(undefined, { ...(ngDevMode ? { debugName: "rowNode" } : /* istanbul ignore next */ {}), alias: 'ttSelectableRow' });
    ttSelectableRowDisabled = input(undefined, { ...(ngDevMode ? { debugName: "ttSelectableRowDisabled" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    selected = signal(undefined, /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "selected" }] : /* istanbul ignore next */ []));
    _componentStyle = inject(TreeTableStyle);
    tt = inject(TREETABLE_INSTANCE);
    constructor() {
        super();
        this.tt.tableService.selectionSource$.pipe(takeUntilDestroyed()).subscribe(() => {
            this.selected.set(this.tt.isSelected(this.rowNode()?.node));
        });
    }
    onInit() {
        if (this.isEnabled()) {
            this.selected.set(this.tt.isSelected(this.rowNode()?.node));
        }
    }
    onClick(event) {
        if (this.isEnabled()) {
            this.tt.handleRowClick({
                originalEvent: event,
                rowNode: this.rowNode()
            });
        }
    }
    /**
     * Selectable rows are tab stops: Tab reaches the row (Enter/Space selects),
     * then continues to the focusables inside it — toggler, checkbox, editors.
     */
    get hostTabindex() {
        return this.isEnabled() ? 0 : null;
    }
    onKeyDown(event) {
        // Selection keys apply only while the row itself is focused. Keys from
        // focusables inside the row bubble through here — Enter/Space on the
        // toggler must stay a plain toggle, not become a selection.
        if (event.target !== this.el?.nativeElement) {
            return;
        }
        switch (event.code) {
            case 'Enter':
            case 'Space':
                this.onEnterKey(event);
                break;
            case 'Tab':
                if (event.shiftKey) {
                    this.onShiftTabKey(event);
                }
                break;
            default:
                break;
        }
    }
    /**
     * Backwards navigation treats rows as the stops: Shift+Tab from a row lands
     * on the previous row, not on the last focusable inside it (the browser's
     * reverse order would visit checkbox/toggler first). Forward Tab stays
     * native — row, then into the row's focusables, then the next row. On the
     * first row nothing is intercepted, so focus exits the table backwards.
     */
    onShiftTabKey(event) {
        let sibling = this.el?.nativeElement.previousElementSibling;
        while (sibling && sibling.getAttribute('tabindex') === null) {
            sibling = sibling.previousElementSibling;
        }
        if (sibling) {
            sibling.focus();
            event.preventDefault();
        }
    }
    onTouchEnd(event) {
        if (this.isEnabled()) {
            this.tt.handleRowTouchEnd(event);
        }
    }
    onEnterKey(event) {
        if (this.tt.selectionMode() === 'checkbox') {
            this.tt.toggleNodeWithCheckbox({
                originalEvent: event,
                rowNode: this.rowNode()
            });
        }
        else {
            this.onClick(event);
        }
        event.preventDefault();
    }
    isEnabled() {
        return this.ttSelectableRowDisabled() !== true;
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTSelectableRow, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.1.0", version: "22.0.6", type: TTSelectableRow, isStandalone: true, selector: "[ttSelectableRow]", inputs: { rowNode: { classPropertyName: "rowNode", publicName: "ttSelectableRow", isSignal: true, isRequired: false, transformFunction: null }, ttSelectableRowDisabled: { classPropertyName: "ttSelectableRowDisabled", publicName: "ttSelectableRowDisabled", isSignal: true, isRequired: false, transformFunction: null } }, host: { listeners: { "click": "onClick($event)", "keydown": "onKeyDown($event)", "touchend": "onTouchEnd($event)" }, properties: { "class": "cx(\"row\")", "attr.aria-selected": "selected()", "attr.tabindex": "hostTabindex" } }, providers: [TreeTableStyle], usesInheritance: true, ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTSelectableRow, decorators: [{
            type: Directive,
            args: [{
                    selector: '[ttSelectableRow]',
                    standalone: true,
                    host: {
                        '[class]': 'cx("row")',
                        '[attr.aria-selected]': 'selected()',
                        '[attr.tabindex]': 'hostTabindex'
                    },
                    providers: [TreeTableStyle]
                }]
        }], ctorParameters: () => [], propDecorators: { rowNode: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttSelectableRow", required: false }] }], ttSelectableRowDisabled: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttSelectableRowDisabled", required: false }] }], onClick: [{
                type: HostListener,
                args: ['click', ['$event']]
            }], onKeyDown: [{
                type: HostListener,
                args: ['keydown', ['$event']]
            }], onTouchEnd: [{
                type: HostListener,
                args: ['touchend', ['$event']]
            }] } });

class TTSelectableRowDblClick extends BaseComponent {
    rowNode = input(undefined, { ...(ngDevMode ? { debugName: "rowNode" } : /* istanbul ignore next */ {}), alias: 'ttSelectableRowDblClick' });
    ttSelectableRowDisabled = input(undefined, { ...(ngDevMode ? { debugName: "ttSelectableRowDisabled" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    selected = signal(undefined, /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "selected" }] : /* istanbul ignore next */ []));
    _componentStyle = inject(TreeTableStyle);
    tt = inject(TREETABLE_INSTANCE);
    constructor() {
        super();
        this.tt.tableService.selectionSource$.pipe(takeUntilDestroyed()).subscribe(() => {
            this.selected.set(this.tt.isSelected(this.rowNode()?.node));
        });
    }
    onInit() {
        if (this.isEnabled()) {
            this.selected.set(this.tt.isSelected(this.rowNode()?.node));
        }
    }
    onClick(event) {
        if (this.isEnabled()) {
            this.tt.handleRowClick({
                originalEvent: event,
                rowNode: this.rowNode()
            });
        }
    }
    isEnabled() {
        return this.ttSelectableRowDisabled() !== true;
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTSelectableRowDblClick, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.1.0", version: "22.0.6", type: TTSelectableRowDblClick, isStandalone: true, selector: "[ttSelectableRowDblClick]", inputs: { rowNode: { classPropertyName: "rowNode", publicName: "ttSelectableRowDblClick", isSignal: true, isRequired: false, transformFunction: null }, ttSelectableRowDisabled: { classPropertyName: "ttSelectableRowDisabled", publicName: "ttSelectableRowDisabled", isSignal: true, isRequired: false, transformFunction: null } }, host: { listeners: { "dblclick": "onClick($event)" }, properties: { "class": "cx(\"row\")" } }, providers: [TreeTableStyle], usesInheritance: true, ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTSelectableRowDblClick, decorators: [{
            type: Directive,
            args: [{
                    selector: '[ttSelectableRowDblClick]',
                    standalone: true,
                    host: {
                        '[class]': 'cx("row")'
                    },
                    providers: [TreeTableStyle]
                }]
        }], ctorParameters: () => [], propDecorators: { rowNode: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttSelectableRowDblClick", required: false }] }], ttSelectableRowDisabled: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttSelectableRowDisabled", required: false }] }], onClick: [{
                type: HostListener,
                args: ['dblclick', ['$event']]
            }] } });

class TTContextMenuRow extends BaseComponent {
    rowNode = input(undefined, { ...(ngDevMode ? { debugName: "rowNode" } : /* istanbul ignore next */ {}), alias: 'ttContextMenuRow' });
    ttContextMenuRowDisabled = input(undefined, { ...(ngDevMode ? { debugName: "ttContextMenuRowDisabled" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    selected = signal(undefined, /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "selected" }] : /* istanbul ignore next */ []));
    _componentStyle = inject(TreeTableStyle);
    tt = inject(TREETABLE_INSTANCE);
    constructor() {
        super();
        this.tt.tableService.contextMenuSource$.pipe(takeUntilDestroyed()).subscribe((node) => {
            this.selected.set(node ? this.tt.equals(this.rowNode()?.node, node) : false);
        });
    }
    onContextMenu(event) {
        if (this.isEnabled()) {
            this.tt.handleRowRightClick({
                originalEvent: event,
                rowNode: this.rowNode()
            });
            this.el.nativeElement.focus();
            event.preventDefault();
        }
    }
    get hostTabindex() {
        return this.isEnabled() ? 0 : undefined;
    }
    isEnabled() {
        return this.ttContextMenuRowDisabled() !== true;
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTContextMenuRow, deps: [], target: i0.ɵɵFactoryTarget.Directive });
    static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.1.0", version: "22.0.6", type: TTContextMenuRow, isStandalone: true, selector: "[ttContextMenuRow]", inputs: { rowNode: { classPropertyName: "rowNode", publicName: "ttContextMenuRow", isSignal: true, isRequired: false, transformFunction: null }, ttContextMenuRowDisabled: { classPropertyName: "ttContextMenuRowDisabled", publicName: "ttContextMenuRowDisabled", isSignal: true, isRequired: false, transformFunction: null } }, host: { listeners: { "contextmenu": "onContextMenu($event)" }, properties: { "class": "cx(\"contextMenuRow\")", "tabindex": "hostTabindex" } }, providers: [TreeTableStyle], usesInheritance: true, ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTContextMenuRow, decorators: [{
            type: Directive,
            args: [{
                    selector: '[ttContextMenuRow]',
                    standalone: true,
                    host: {
                        '[class]': 'cx("contextMenuRow")',
                        '[tabindex]': 'hostTabindex'
                    },
                    providers: [TreeTableStyle]
                }]
        }], ctorParameters: () => [], propDecorators: { rowNode: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttContextMenuRow", required: false }] }], ttContextMenuRowDisabled: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttContextMenuRowDisabled", required: false }] }], onContextMenu: [{
                type: HostListener,
                args: ['contextmenu', ['$event']]
            }] } });

class TTCheckbox extends BaseComponent {
    hostName = 'TreeTable';
    disabled = input(undefined, { ...(ngDevMode ? { debugName: "disabled" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    rowNode = input(undefined, { ...(ngDevMode ? { debugName: "rowNode" } : /* istanbul ignore next */ {}), alias: 'value' });
    checked = signal(undefined, /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "checked" }] : /* istanbul ignore next */ []));
    partialChecked = signal(undefined, /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "partialChecked" }] : /* istanbul ignore next */ []));
    _componentStyle = inject(TreeTableStyle);
    tt = inject(TREETABLE_INSTANCE);
    constructor() {
        super();
        this.tt.tableService.selectionSource$.pipe(takeUntilDestroyed()).subscribe(() => {
            if (this.tt._selectionKeys) {
                this.checked.set(this.tt.isNodeSelected(this.rowNode()?.node));
                this.partialChecked.set(this.tt.isNodePartialSelected(this.rowNode()?.node));
            }
            else {
                this.checked.set(this.tt.isSelected(this.rowNode()?.node));
                this.partialChecked.set(this.rowNode()?.node?.partialSelected);
            }
        });
    }
    onInit() {
        if (this.tt._selectionKeys) {
            this.checked.set(this.tt.isNodeSelected(this.rowNode()?.node));
            this.partialChecked.set(this.tt.isNodePartialSelected(this.rowNode()?.node));
        }
        else {
            this.checked.set(this.tt.isSelected(this.rowNode()?.node));
            this.partialChecked.set(this.rowNode()?.node?.partialSelected);
        }
    }
    onClick(event) {
        if (!this.disabled()) {
            if (this.tt._selectionKeys) {
                const _check = !this.checked();
                this.tt.toggleCheckbox({
                    originalEvent: event,
                    check: _check,
                    rowNode: this.rowNode()
                });
            }
            else {
                this.tt.toggleNodeWithCheckbox({
                    originalEvent: event,
                    rowNode: this.rowNode()
                });
            }
        }
        clearSelection();
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTCheckbox, deps: [], target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.0.0", version: "22.0.6", type: TTCheckbox, isStandalone: true, selector: "p-treetable-checkbox, p-tree-table-checkbox", inputs: { disabled: { classPropertyName: "disabled", publicName: "disabled", isSignal: true, isRequired: false, transformFunction: null }, rowNode: { classPropertyName: "rowNode", publicName: "value", isSignal: true, isRequired: false, transformFunction: null } }, providers: [TreeTableStyle], usesInheritance: true, ngImport: i0, template: `
        <p-checkbox [ngModel]="checked()" [pt]="ptm('pcRowCheckbox')" (onChange)="onClick($event)" [binary]="true" [disabled]="disabled()" [indeterminate]="partialChecked()" [class]="cx('pcNodeCheckbox')" [tabIndex]="-1" [unstyled]="unstyled()">
            @if (tt.checkboxIconTemplate()) {
                <ng-template #icon>
                    <ng-template *ngTemplateOutlet="tt.checkboxIconTemplate(); context: { $implicit: checked(), partialSelected: partialChecked() }"></ng-template>
                </ng-template>
            }
        </p-checkbox>
    `, isInline: true, dependencies: [{ kind: "directive", type: NgTemplateOutlet, selector: "[ngTemplateOutlet]", inputs: ["ngTemplateOutletContext", "ngTemplateOutlet", "ngTemplateOutletInjector"] }, { kind: "ngmodule", type: FormsModule }, { kind: "directive", type: i1$2.NgControlStatus, selector: "[formControlName],[ngModel],[formControl]" }, { kind: "directive", type: i1$2.NgModel, selector: "[ngModel]:not([formControlName]):not([formControl])", inputs: ["name", "disabled", "ngModel", "ngModelOptions"], outputs: ["ngModelChange"], exportAs: ["ngModel"] }, { kind: "component", type: Checkbox, selector: "p-checkbox, p-check-box", inputs: ["value", "binary", "ariaLabelledBy", "ariaLabel", "tabindex", "inputId", "inputStyle", "inputClass", "indeterminate", "formControl", "checkboxIcon", "readonly", "autofocus", "trueValue", "falseValue", "variant", "size"], outputs: ["onChange", "onFocus", "onBlur"] }, { kind: "ngmodule", type: SharedModule }], changeDetection: i0.ChangeDetectionStrategy.OnPush, encapsulation: i0.ViewEncapsulation.None });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTCheckbox, decorators: [{
            type: Component,
            args: [{
                    selector: 'p-treetable-checkbox, p-tree-table-checkbox',
                    standalone: true,
                    imports: [NgTemplateOutlet, FormsModule, Checkbox, SharedModule],
                    template: `
        <p-checkbox [ngModel]="checked()" [pt]="ptm('pcRowCheckbox')" (onChange)="onClick($event)" [binary]="true" [disabled]="disabled()" [indeterminate]="partialChecked()" [class]="cx('pcNodeCheckbox')" [tabIndex]="-1" [unstyled]="unstyled()">
            @if (tt.checkboxIconTemplate()) {
                <ng-template #icon>
                    <ng-template *ngTemplateOutlet="tt.checkboxIconTemplate(); context: { $implicit: checked(), partialSelected: partialChecked() }"></ng-template>
                </ng-template>
            }
        </p-checkbox>
    `,
                    encapsulation: ViewEncapsulation.None,
                    changeDetection: ChangeDetectionStrategy.OnPush,
                    providers: [TreeTableStyle]
                }]
        }], ctorParameters: () => [], propDecorators: { disabled: [{ type: i0.Input, args: [{ isSignal: true, alias: "disabled", required: false }] }], rowNode: [{ type: i0.Input, args: [{ isSignal: true, alias: "value", required: false }] }] } });

class TTHeaderCheckbox extends BaseComponent {
    checked = signal(undefined, /* @ts-ignore */
    ...(ngDevMode ? [{ debugName: "checked" }] : /* istanbul ignore next */ []));
    tt = inject(TREETABLE_INSTANCE);
    get isDisabled() {
        return !this.tt.value || this.tt.value.length === 0;
    }
    constructor() {
        super();
        this.tt.tableService.uiUpdateSource$.pipe(takeUntilDestroyed()).subscribe(() => {
            this.checked.set(this.updateCheckedState());
        });
        this.tt.tableService.selectionSource$.pipe(takeUntilDestroyed()).subscribe(() => {
            this.checked.set(this.updateCheckedState());
        });
    }
    onInit() {
        this.checked.set(this.updateCheckedState());
    }
    onClick(event) {
        if ((this.tt?.value || this.tt?.filteredNodes) && ((this.tt?.value && this.tt.value.length > 0) || (this.tt?.filteredNodes && this.tt.filteredNodes.length > 0))) {
            this.tt?.toggleNodesWithCheckbox(event, !this.checked());
        }
        clearSelection();
    }
    updateCheckedState() {
        let checked;
        const data = this.tt.filteredNodes || this.tt.value;
        if (data) {
            if (this.tt._selectionKeys) {
                for (let node of data) {
                    if (this.tt.isNodeSelected(node)) {
                        checked = true;
                    }
                    else {
                        checked = false;
                        break;
                    }
                }
            }
            if (!this.tt._selectionKeys) {
                // legacy selection support, will be removed in v18
                for (let node of data) {
                    if (this.tt.isSelected(node)) {
                        checked = true;
                    }
                    else {
                        checked = false;
                        break;
                    }
                }
            }
        }
        else {
            checked = false;
        }
        return checked;
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTHeaderCheckbox, deps: [], target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.0.0", version: "22.0.6", type: TTHeaderCheckbox, isStandalone: true, selector: "p-treetableheadercheckbox, p-treetable-header-checkbox", usesInheritance: true, ngImport: i0, template: `
        <p-checkbox [ngModel]="checked()" [pt]="ptm('pcHeaderCheckbox')" (onChange)="onClick($event)" [binary]="true" [disabled]="isDisabled" [unstyled]="unstyled()">
            @if (tt.headerCheckboxIconTemplate()) {
                <ng-template #icon>
                    <ng-template *ngTemplateOutlet="tt.headerCheckboxIconTemplate(); context: { $implicit: checked() }"></ng-template>
                </ng-template>
            }
        </p-checkbox>
    `, isInline: true, dependencies: [{ kind: "directive", type: NgTemplateOutlet, selector: "[ngTemplateOutlet]", inputs: ["ngTemplateOutletContext", "ngTemplateOutlet", "ngTemplateOutletInjector"] }, { kind: "ngmodule", type: FormsModule }, { kind: "directive", type: i1$2.NgControlStatus, selector: "[formControlName],[ngModel],[formControl]" }, { kind: "directive", type: i1$2.NgModel, selector: "[ngModel]:not([formControlName]):not([formControl])", inputs: ["name", "disabled", "ngModel", "ngModelOptions"], outputs: ["ngModelChange"], exportAs: ["ngModel"] }, { kind: "component", type: Checkbox, selector: "p-checkbox, p-check-box", inputs: ["value", "binary", "ariaLabelledBy", "ariaLabel", "tabindex", "inputId", "inputStyle", "inputClass", "indeterminate", "formControl", "checkboxIcon", "readonly", "autofocus", "trueValue", "falseValue", "variant", "size"], outputs: ["onChange", "onFocus", "onBlur"] }, { kind: "ngmodule", type: SharedModule }], changeDetection: i0.ChangeDetectionStrategy.OnPush, encapsulation: i0.ViewEncapsulation.None });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTHeaderCheckbox, decorators: [{
            type: Component,
            args: [{
                    selector: 'p-treetableheadercheckbox, p-treetable-header-checkbox',
                    standalone: true,
                    imports: [NgTemplateOutlet, FormsModule, Checkbox, SharedModule],
                    template: `
        <p-checkbox [ngModel]="checked()" [pt]="ptm('pcHeaderCheckbox')" (onChange)="onClick($event)" [binary]="true" [disabled]="isDisabled" [unstyled]="unstyled()">
            @if (tt.headerCheckboxIconTemplate()) {
                <ng-template #icon>
                    <ng-template *ngTemplateOutlet="tt.headerCheckboxIconTemplate(); context: { $implicit: checked() }"></ng-template>
                </ng-template>
            }
        </p-checkbox>
    `,
                    encapsulation: ViewEncapsulation.None,
                    changeDetection: ChangeDetectionStrategy.OnPush
                }]
        }], ctorParameters: () => [] });

class TTEditableColumn extends BaseComponent {
    data = input(undefined, { ...(ngDevMode ? { debugName: "data" } : /* istanbul ignore next */ {}), alias: 'ttEditableColumn' });
    field = input(undefined, { ...(ngDevMode ? { debugName: "field" } : /* istanbul ignore next */ {}), alias: 'ttEditableColumnField' });
    ttEditableColumnDisabled = input(undefined, { ...(ngDevMode ? { debugName: "ttEditableColumnDisabled" } : /* istanbul ignore next */ {}), transform: booleanAttribute });
    tt = inject(TREETABLE_INSTANCE);
    onAfterViewInit() {
        if (this.isEnabled()) {
            !this.$unstyled() && addClass(this.el.nativeElement, 'p-editable-column');
            this.el?.nativeElement.setAttribute('data-p-editable-column', 'true');
        }
    }
    onClick(event) {
        if (this.isEnabled()) {
            this.requestEdit();
        }
    }
    /**
     * Keyboard entry point: the cell editor host is in the tab order and opens
     * its editor on focus, so Tab lands on the cell and starts editing without
     * a mouse. `editingCellClick` is normally consumed by the document click
     * that follows a mouse open — a keyboard open has no such click, so it is
     * reset on the next tick to keep the first outside click closing the cell.
     */
    openCellFromFocus() {
        if (!this.isEnabled()) {
            return;
        }
        this.requestEdit();
        setTimeout(() => (this.tt.editingCellClick = false));
    }
    requestEdit() {
        this.tt.editingCellClick = true;
        if (this.tt.editingCell) {
            if (this.tt.editingCell !== this.el.nativeElement) {
                if (!this.tt.isEditingCellValid()) {
                    return;
                }
                if (this.tt.editingCell) {
                    !this.$unstyled() && removeClass(this.tt.editingCell, 'p-cell-editing');
                    // The cell being left keeps its editing marker otherwise — only
                    // Enter/Escape closes reset it, not switching to another cell.
                    this.tt.editingCell.setAttribute('data-p-cell-editing', 'false');
                }
                this.openCell();
            }
        }
        else {
            this.openCell();
        }
    }
    openCell() {
        this.tt.updateEditingCell(this.el.nativeElement, this.data(), this.field());
        !this.$unstyled() && addClass(this.el.nativeElement, 'p-cell-editing');
        this.el?.nativeElement.setAttribute('data-p-cell-editing', 'true');
        this.tt.onEditInit.emit({ field: this.field(), data: this.data() });
        this.tt.editingCellClick = true;
        setTimeout(() => {
            let focusable = findSingle(this.el.nativeElement, 'input, textarea');
            if (focusable) {
                focusable.focus();
            }
        }, 50);
    }
    closeEditingCell() {
        if (this.tt.editingCell)
            !this.$unstyled() && removeClass(this.tt.editingCell, 'p-cell-editing');
        this.tt.editingCell = null;
        this.tt.unbindDocumentEditListener();
    }
    /**
     * Keyboard closes (Enter/Escape) must hand focus back somewhere — the editor
     * input unmounts, and without this the focus falls to <body>. The cell
     * editor host is a tab stop, so focus returns to it (Tab then continues to
     * the next focusable from the same position). The suppress flag keeps that
     * programmatic focus from immediately reopening the editor it just closed.
     */
    suppressFocusOpen = false;
    consumeSuppressedFocusOpen() {
        const suppressed = this.suppressFocusOpen;
        this.suppressFocusOpen = false;
        return suppressed;
    }
    focusEditorHost() {
        const host = findSingle(this.el?.nativeElement, 'p-treetable-cell-editor, p-treetablecelleditor');
        if (host) {
            this.suppressFocusOpen = true;
            host.focus();
        }
    }
    onKeyDown(event) {
        // Only intercept keys while THIS cell's editor is open. Otherwise Enter/
        // Space must reach the cell's interactive children natively (e.g. the
        // row toggler button) and Tab must follow the normal tab order.
        const editing = !!this.tt.editingCell && event.target instanceof Node && this.tt.editingCell.contains(event.target);
        if (!editing) {
            return;
        }
        if (this.isEnabled()) {
            //enter
            if (event.keyCode == 13 && !event.shiftKey) {
                if (this.tt.isEditingCellValid()) {
                    if (this.tt.editingCell) {
                        !this.$unstyled() && removeClass(this.tt.editingCell, 'p-cell-editing');
                        this.el?.nativeElement.setAttribute('data-p-cell-editing', 'false');
                    }
                    this.closeEditingCell();
                    this.tt.onEditComplete.emit({ field: this.field(), data: this.data() });
                    this.focusEditorHost();
                }
                event.preventDefault();
            }
            //escape
            else if (event.keyCode == 27) {
                if (this.tt.isEditingCellValid()) {
                    if (this.tt.editingCell) {
                        !this.$unstyled() && removeClass(this.tt.editingCell, 'p-cell-editing');
                        this.el?.nativeElement.setAttribute('data-p-cell-editing', 'false');
                    }
                    this.closeEditingCell();
                    this.tt.onEditCancel.emit({ field: this.field(), data: this.data() });
                    this.focusEditorHost();
                }
                event.preventDefault();
            }
            // Tab is intentionally left to the browser: editable cells are real
            // tab stops (see TreeTableCellEditor.hostTabindex), so sequential
            // focus naturally reaches whatever comes next in DOM order — the
            // next cell, a row toggler between rows, or an element outside the
            // table. The focusout handler below commits the edit on the way out.
        }
    }
    /**
     * Closes the editor when focus leaves the cell (native Tab/Shift+Tab or a
     * programmatic focus move). The edit is committed — with template-driven
     * bindings the value is already applied, closing only swaps the input back
     * to output. Focus changes within the cell (input ↔ toggler ↔ host) and
     * non-focus targets (relatedTarget null, e.g. window blur) are ignored.
     */
    onFocusOut(event) {
        const cell = this.el?.nativeElement;
        const editingThisCell = this.tt.editingCell === cell;
        const target = event.relatedTarget;
        if (!editingThisCell || !(target instanceof Node) || cell.contains(target)) {
            return;
        }
        if (this.tt.isEditingCellValid()) {
            !this.$unstyled() && removeClass(cell, 'p-cell-editing');
            cell.setAttribute('data-p-cell-editing', 'false');
            this.closeEditingCell();
            this.tt.onEditComplete.emit({ field: this.field(), data: this.data() });
        }
    }
    isEnabled() {
        return this.ttEditableColumnDisabled() !== true;
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTEditableColumn, deps: null, target: i0.ɵɵFactoryTarget.Directive });
    static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.1.0", version: "22.0.6", type: TTEditableColumn, isStandalone: true, selector: "[ttEditableColumn]", inputs: { data: { classPropertyName: "data", publicName: "ttEditableColumn", isSignal: true, isRequired: false, transformFunction: null }, field: { classPropertyName: "field", publicName: "ttEditableColumnField", isSignal: true, isRequired: false, transformFunction: null }, ttEditableColumnDisabled: { classPropertyName: "ttEditableColumnDisabled", publicName: "ttEditableColumnDisabled", isSignal: true, isRequired: false, transformFunction: null } }, host: { listeners: { "click": "onClick($event)", "keydown": "onKeyDown($event)", "focusout": "onFocusOut($event)" } }, usesInheritance: true, ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTEditableColumn, decorators: [{
            type: Directive,
            args: [{
                    selector: '[ttEditableColumn]',
                    standalone: true
                }]
        }], propDecorators: { data: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttEditableColumn", required: false }] }], field: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttEditableColumnField", required: false }] }], ttEditableColumnDisabled: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttEditableColumnDisabled", required: false }] }], onClick: [{
                type: HostListener,
                args: ['click', ['$event']]
            }], onKeyDown: [{
                type: HostListener,
                args: ['keydown', ['$event']]
            }], onFocusOut: [{
                type: HostListener,
                args: ['focusout', ['$event']]
            }] } });

class TreeTableCellEditor extends BaseComponent {
    hostName = 'TreeTable';
    bindDirectiveInstance = inject(Bind, { self: true });
    onAfterViewChecked() {
        this.bindDirectiveInstance.setAttrs(this.ptm('cellEditor'));
    }
    /**
     * Closed editors render plain output text, which is not focusable — without
     * a tabindex the browser's sequential focus (Tab) would skip editable cells
     * entirely. The host joins the tab order so Tab reaches the cell, and focus
     * opens the editor (which then moves focus into the input).
     */
    get hostTabindex() {
        return this.editableColumn.isEnabled() ? 0 : null;
    }
    onHostFocus() {
        if (this.editableColumn.consumeSuppressedFocusOpen() || this.editing || !this.editableColumn.isEnabled()) {
            return;
        }
        this.editableColumn.openCellFromFocus();
    }
    inputTemplate = contentChild('input', { ...(ngDevMode ? { debugName: "inputTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    outputTemplate = contentChild('output', { ...(ngDevMode ? { debugName: "outputTemplate" } : /* istanbul ignore next */ {}), descendants: false });
    tt = inject(TREETABLE_INSTANCE);
    editableColumn = inject(TTEditableColumn);
    get editing() {
        return this.tt.editingCell === this.editableColumn.el.nativeElement;
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTableCellEditor, deps: null, target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.0.0", version: "22.0.6", type: TreeTableCellEditor, isStandalone: true, selector: "p-treetablecelleditor, p-treetable-cell-editor", host: { listeners: { "focus": "onHostFocus()" }, properties: { "attr.tabindex": "hostTabindex" } }, queries: [{ propertyName: "inputTemplate", first: true, predicate: ["input"], isSignal: true }, { propertyName: "outputTemplate", first: true, predicate: ["output"], isSignal: true }], usesInheritance: true, hostDirectives: [{ directive: i1.Bind }], ngImport: i0, template: `
        @if (editing) {
            <ng-container *ngTemplateOutlet="inputTemplate()"></ng-container>
        } @else {
            <ng-container *ngTemplateOutlet="outputTemplate()"></ng-container>
        }
    `, isInline: true, dependencies: [{ kind: "directive", type: NgTemplateOutlet, selector: "[ngTemplateOutlet]", inputs: ["ngTemplateOutletContext", "ngTemplateOutlet", "ngTemplateOutletInjector"] }], encapsulation: i0.ViewEncapsulation.None });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTableCellEditor, decorators: [{
            type: Component,
            args: [{
                    selector: 'p-treetablecelleditor, p-treetable-cell-editor',
                    standalone: true,
                    imports: [NgTemplateOutlet],
                    template: `
        @if (editing) {
            <ng-container *ngTemplateOutlet="inputTemplate()"></ng-container>
        } @else {
            <ng-container *ngTemplateOutlet="outputTemplate()"></ng-container>
        }
    `,
                    encapsulation: ViewEncapsulation.None,
                    host: {
                        '[attr.tabindex]': 'hostTabindex',
                        '(focus)': 'onHostFocus()'
                    },
                    hostDirectives: [Bind]
                }]
        }], propDecorators: { inputTemplate: [{ type: i0.ContentChild, args: ['input', { ...{ descendants: false }, isSignal: true }] }], outputTemplate: [{ type: i0.ContentChild, args: ['output', { ...{ descendants: false }, isSignal: true }] }] } });

class TTRow extends BaseComponent {
    hostName = 'TreeTable';
    bindDirectiveInstance = inject(Bind, { self: true });
    tt = inject(TREETABLE_INSTANCE);
    onAfterViewChecked() {
        this.bindDirectiveInstance.setAttrs(this.ptm('row', this.ptmOptions()));
    }
    get level() {
        return this.rowNode()?.['level'] + 1;
    }
    get hostClass() {
        return this.cn('p-element', this.rowNode()?.node['styleClass']);
    }
    get expanded() {
        return this.rowNode()?.node['expanded'];
    }
    rowNode = input(undefined, { ...(ngDevMode ? { debugName: "rowNode" } : /* istanbul ignore next */ {}), alias: 'ttRow' });
    _componentStyle = inject(TreeTableStyle);
    onKeyDown(event) {
        // Skip row/cell keyboard navigation while a cell editor is open so that keys such as
        // ArrowLeft/ArrowRight move the caret inside the editor input instead of moving focus
        // out of the open editor and discarding the in-progress edit.
        if (this.isEditingCellTarget(event.target)) {
            return;
        }
        switch (event.code) {
            case 'ArrowDown':
                this.onArrowDownKey(event);
                break;
            case 'ArrowUp':
                this.onArrowUpKey(event);
                break;
            case 'ArrowRight':
                this.onArrowRightKey(event);
                break;
            case 'ArrowLeft':
                this.onArrowLeftKey(event);
                break;
            case 'Home':
                this.onHomeKey(event);
                break;
            case 'End':
                this.onEndKey(event);
                break;
            default:
                break;
        }
    }
    isEditingCellTarget(target) {
        const editingCell = this.tt?.editingCell;
        return !!(editingCell && target instanceof Node && editingCell.contains(target));
    }
    onArrowDownKey(event) {
        this.focusSiblingToggler('next');
        event.preventDefault();
    }
    onArrowUpKey(event) {
        this.focusSiblingToggler('previous');
        event.preventDefault();
    }
    onArrowRightKey(event) {
        if (this.togglerButton(this.el?.nativeElement) && !this.expanded && this.rowNode()?.node['children']) {
            this.expand(event);
        }
        event.preventDefault();
    }
    onArrowLeftKey(event) {
        if (this.expanded) {
            this.tt.toggleRowIndex = getIndex(this.el.nativeElement);
            this.collapse(event);
            this.restoreFocus();
        }
        event.preventDefault();
    }
    onHomeKey(event) {
        const rows = find(this.tt.el?.nativeElement, `tr[aria-level="${this.level}"]`);
        for (const row of rows) {
            const toggler = this.togglerButton(row);
            if (toggler) {
                toggler.focus();
                break;
            }
        }
        event.preventDefault();
    }
    onEndKey(event) {
        const rows = find(this.tt.el?.nativeElement, `tr[aria-level="${this.level}"]`);
        for (let i = rows.length - 1; i >= 0; i--) {
            const toggler = this.togglerButton(rows[i]);
            if (toggler) {
                toggler.focus();
                break;
            }
        }
        event.preventDefault();
    }
    expand(event) {
        this.tt.toggleRowIndex = getIndex(this.el.nativeElement);
        this.rowNode().node['expanded'] = true;
        this.tt.updateSerializedValue();
        this.tt.tableService.onUIUpdate(this.tt.value);
        this.restoreFocus();
        this.tt.onNodeExpand.emit({
            originalEvent: event,
            node: this.rowNode().node
        });
    }
    collapse(event) {
        this.rowNode().node['expanded'] = false;
        this.tt.updateSerializedValue();
        this.tt.tableService.onUIUpdate(this.tt.value);
        this.tt.onNodeCollapse.emit({ originalEvent: event, node: this.rowNode().node });
    }
    /**
     * Focus model: rows are not focusable — the row's toggler button carries
     * keyboard focus (Tab reaches it, Enter/Space toggles, arrows move between
     * visible togglers). Rows never show a focus outline.
     */
    togglerButton(row) {
        if (!row)
            return null;
        const button = findSingle(row, '[data-pc-section="rowtogglebutton"]');
        return button && button.style.visibility !== 'hidden' ? button : null;
    }
    focusSiblingToggler(direction) {
        let sibling = direction === 'next' ? this.el?.nativeElement?.nextElementSibling : this.el?.nativeElement?.previousElementSibling;
        while (sibling) {
            const toggler = this.togglerButton(sibling);
            if (toggler) {
                toggler.focus();
                return;
            }
            sibling = direction === 'next' ? sibling.nextElementSibling : sibling.previousElementSibling;
        }
    }
    /** Re-focus the toggled row's toggler after the tbody re-renders. */
    restoreFocus() {
        setTimeout(() => {
            const tbody = findSingle(this.tt.el?.nativeElement, '[data-pc-section="tbody"]');
            const row = tbody?.children?.[this.tt.toggleRowIndex || 0];
            const toggler = this.togglerButton(row);
            toggler?.focus();
        }, 25);
    }
    ptmOptions() {
        return {
            context: {
                selectable: this.tt?.rowHover() || !!this.tt.selectionMode(),
                selected: this.tt.isSelected(this.rowNode()?.node),
                scrollable: this.tt?.scrollable(),
                rowNode: this.rowNode()
            }
        };
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTRow, deps: null, target: i0.ɵɵFactoryTarget.Directive });
    static ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "17.1.0", version: "22.0.6", type: TTRow, isStandalone: true, selector: "[ttRow]", inputs: { rowNode: { classPropertyName: "rowNode", publicName: "ttRow", isSignal: true, isRequired: false, transformFunction: null } }, host: { attributes: { "role": "row" }, listeners: { "keydown": "onKeyDown($event)" }, properties: { "class": "hostClass", "attr.aria-expanded": "expanded", "attr.aria-level": "level" } }, providers: [TreeTableStyle], usesInheritance: true, hostDirectives: [{ directive: i1.Bind }], ngImport: i0 });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TTRow, decorators: [{
            type: Directive,
            args: [{
                    selector: '[ttRow]',
                    standalone: true,
                    host: {
                        '[class]': 'hostClass',
                        '[attr.aria-expanded]': 'expanded',
                        '[attr.aria-level]': 'level',
                        role: 'row'
                    },
                    providers: [TreeTableStyle],
                    hostDirectives: [Bind]
                }]
        }], propDecorators: { rowNode: [{ type: i0.Input, args: [{ isSignal: true, alias: "ttRow", required: false }] }], onKeyDown: [{
                type: HostListener,
                args: ['keydown', ['$event']]
            }] } });

class TreeTableToggler extends BaseComponent {
    hostName = 'TreeTable';
    bindDirectiveInstance = inject(Bind, { self: true });
    onAfterViewChecked() {
        this.bindDirectiveInstance.setAttrs(this.ptm('toggler'));
    }
    rowNode = input(/* @ts-ignore */
    ...(ngDevMode ? [undefined, { debugName: "rowNode" }] : /* istanbul ignore next */ []));
    _componentStyle = inject(TreeTableStyle);
    tt = inject(TREETABLE_INSTANCE);
    get togglerIconContext() {
        return { $implicit: this.rowNode()?.node?.expanded };
    }
    get togglerVisibility() {
        const node = this.rowNode()?.node;
        return node?.leaf === false || (node?.children && node.children.length) ? 'visible' : 'hidden';
    }
    get togglerMarginStart() {
        return this.rowNode()?.level * 16 + 'px';
    }
    get toggleButtonAriaLabel() {
        return this.config.translation ? (this.rowNode()?.expanded ? this.config.translation?.aria?.collapseRow : this.config.translation?.aria?.expandRow) : undefined;
    }
    onClick(event) {
        // The toggle re-renders the tbody and destroys this button — remember the
        // row position so focus can be handed back to the recreated toggler
        // (keyboard flow: Tab → toggler, Enter/Space toggles, focus stays put).
        const row = this.el?.nativeElement.closest('tr');
        const tbody = row?.parentElement;
        const rowIndex = row && tbody ? Array.prototype.indexOf.call(tbody.children, row) : -1;
        this.rowNode().node.expanded = !this.rowNode().node.expanded;
        this.tt.setNodeCollapsed(this.rowNode().node, !this.rowNode().node.expanded);
        if (this.rowNode().node.expanded) {
            this.tt.onNodeExpand.emit({
                originalEvent: event,
                node: this.rowNode().node
            });
        }
        else {
            this.tt.onNodeCollapse.emit({
                originalEvent: event,
                node: this.rowNode().node
            });
        }
        this.tt.updateSerializedValue();
        this.tt.tableService.onUIUpdate(this.tt.value);
        if (rowIndex >= 0) {
            setTimeout(() => {
                const newRow = tbody?.children?.[rowIndex];
                const toggler = newRow?.querySelector('[data-pc-section="rowtogglebutton"]');
                if (toggler && toggler.style.visibility !== 'hidden') {
                    toggler.focus();
                }
            }, 25);
        }
        event.preventDefault();
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTableToggler, deps: null, target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "17.0.0", version: "22.0.6", type: TreeTableToggler, isStandalone: true, selector: "p-treetabletoggler, p-treetable-toggler", inputs: { rowNode: { classPropertyName: "rowNode", publicName: "rowNode", isSignal: true, isRequired: false, transformFunction: null } }, providers: [TreeTableStyle], usesInheritance: true, hostDirectives: [{ directive: i1.Bind }], ngImport: i0, template: `
        <button
            type="button"
            [class]="cx('toggler')"
            [pBind]="ptm('rowToggleButton')"
            (click)="onClick($event)"
            pRipple
            [style.visibility]="togglerVisibility"
            [style.marginInlineStart]="togglerMarginStart"
            [attr.data-pc-group-section]="'rowactionbutton'"
            [attr.aria-label]="toggleButtonAriaLabel"
        >
            @if (!tt.togglerIconTemplate()) {
                @if (rowNode().node.expanded) {
                    <svg data-p-icon="chevron-down" [pBind]="ptm('nodetoggleicon')" [attr.aria-hidden]="true" />
                }
                @if (!rowNode().node.expanded) {
                    <svg data-p-icon="chevron-right" [pBind]="ptm('nodetoggleicon')" [attr.aria-hidden]="true" />
                }
            }
            <ng-template *ngTemplateOutlet="tt.togglerIconTemplate(); context: togglerIconContext"></ng-template>
        </button>
    `, isInline: true, dependencies: [{ kind: "directive", type: NgTemplateOutlet, selector: "[ngTemplateOutlet]", inputs: ["ngTemplateOutletContext", "ngTemplateOutlet", "ngTemplateOutletInjector"] }, { kind: "directive", type: Bind, selector: "[pBind]", inputs: ["pBind"] }, { kind: "directive", type: Ripple, selector: "[pRipple]" }, { kind: "component", type: ChevronDown, selector: "svg[data-p-icon=\"chevron-down\"]" }, { kind: "component", type: ChevronRight, selector: "svg[data-p-icon=\"chevron-right\"]" }], encapsulation: i0.ViewEncapsulation.None });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTableToggler, decorators: [{
            type: Component,
            args: [{
                    selector: 'p-treetabletoggler, p-treetable-toggler',
                    standalone: true,
                    imports: [NgTemplateOutlet, Bind, Ripple, ChevronDown, ChevronRight],
                    template: `
        <button
            type="button"
            [class]="cx('toggler')"
            [pBind]="ptm('rowToggleButton')"
            (click)="onClick($event)"
            pRipple
            [style.visibility]="togglerVisibility"
            [style.marginInlineStart]="togglerMarginStart"
            [attr.data-pc-group-section]="'rowactionbutton'"
            [attr.aria-label]="toggleButtonAriaLabel"
        >
            @if (!tt.togglerIconTemplate()) {
                @if (rowNode().node.expanded) {
                    <svg data-p-icon="chevron-down" [pBind]="ptm('nodetoggleicon')" [attr.aria-hidden]="true" />
                }
                @if (!rowNode().node.expanded) {
                    <svg data-p-icon="chevron-right" [pBind]="ptm('nodetoggleicon')" [attr.aria-hidden]="true" />
                }
            }
            <ng-template *ngTemplateOutlet="tt.togglerIconTemplate(); context: togglerIconContext"></ng-template>
        </button>
    `,
                    encapsulation: ViewEncapsulation.None,
                    providers: [TreeTableStyle],
                    hostDirectives: [Bind]
                }]
        }], propDecorators: { rowNode: [{ type: i0.Input, args: [{ isSignal: true, alias: "rowNode", required: false }] }] } });

class TreeTableModule {
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTableModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule });
    static ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "22.0.6", ngImport: i0, type: TreeTableModule, imports: [TreeTable,
            TTBody,
            TTScrollableView,
            TTSortableColumn,
            TTSortIcon,
            TTResizableColumn,
            TTRow,
            TTReorderableColumn,
            TTSelectableRow,
            TTSelectableRowDblClick,
            TTContextMenuRow,
            TTCheckbox,
            TTHeaderCheckbox,
            TTEditableColumn,
            TreeTableCellEditor,
            TreeTableToggler,
            Scroller], exports: [TreeTable,
            SharedModule,
            TreeTableToggler,
            TTSortableColumn,
            TTSortIcon,
            TTResizableColumn,
            TTRow,
            TTReorderableColumn,
            TTSelectableRow,
            TTSelectableRowDblClick,
            TTContextMenuRow,
            TTCheckbox,
            TTHeaderCheckbox,
            TTEditableColumn,
            TreeTableCellEditor,
            Scroller] });
    static ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTableModule, imports: [TreeTable,
            TTScrollableView,
            TTSortIcon,
            TTCheckbox,
            TTHeaderCheckbox,
            TreeTableToggler,
            Scroller, SharedModule] });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "22.0.6", ngImport: i0, type: TreeTableModule, decorators: [{
            type: NgModule,
            args: [{
                    imports: [
                        TreeTable,
                        TTBody,
                        TTScrollableView,
                        TTSortableColumn,
                        TTSortIcon,
                        TTResizableColumn,
                        TTRow,
                        TTReorderableColumn,
                        TTSelectableRow,
                        TTSelectableRowDblClick,
                        TTContextMenuRow,
                        TTCheckbox,
                        TTHeaderCheckbox,
                        TTEditableColumn,
                        TreeTableCellEditor,
                        TreeTableToggler,
                        Scroller
                    ],
                    exports: [
                        TreeTable,
                        SharedModule,
                        TreeTableToggler,
                        TTSortableColumn,
                        TTSortIcon,
                        TTResizableColumn,
                        TTRow,
                        TTReorderableColumn,
                        TTSelectableRow,
                        TTSelectableRowDblClick,
                        TTContextMenuRow,
                        TTCheckbox,
                        TTHeaderCheckbox,
                        TTEditableColumn,
                        TreeTableCellEditor,
                        Scroller
                    ]
                }]
        }] });

/**
 * Generated bundle index. Do not edit.
 */

export { TREETABLE_INSTANCE, TTBody, TTCheckbox, TTContextMenuRow, TTEditableColumn, TTHeaderCheckbox, TTReorderableColumn, TTResizableColumn, TTRow, TTScrollableView, TTSelectableRow, TTSelectableRowDblClick, TTSortIcon, TTSortableColumn, TreeTable, TreeTableCellEditor, TreeTableClasses, TreeTableModule, TreeTableService, TreeTableStyle, TreeTableToggler };
//# sourceMappingURL=primeng-treetable.mjs.map
